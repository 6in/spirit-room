---
phase: quick
plan: 260420-ks5
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room-manager/skills/MR_POPO.md
autonomous: true
requirements:
  - QUICK-260420-ks5
---

<objective>
MR_POPO.md の「MISSION.md 生成ルール」内・調査観点デフォルト文に、軽量 Docker image variant (`-slim` / `-alpine`) を優先して選ばせるための 1 段落を追加する。

Purpose: 2026-04-20 の Python 2.7 Hello World 修行で判明した事実 ——
- Run 1 `python:2.7` フル版: PREPARE 19:30 (うち pull 14 分) / 全体 26 分
- Run 2 `python:2.7-slim`: PREPARE 2:46 (うち pull 2:13) / 全体 8 分
- **全体 3.2×、pull 単独では 7× の高速化**

を恒久化するため、MISSION 生成段階で MR_POPO に variant 選好を刷り込む。

Output: MR_POPO.md 1 ファイルのみを編集。調査観点デフォルト文に短い段落 (2-3 文) を追記。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@spirit-room-manager/skills/MR_POPO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MISSION 生成ルールの調査観点デフォルト文に Docker image variant 選好段落を追加</name>
  <files>spirit-room-manager/skills/MR_POPO.md</files>
  <action>
`spirit-room-manager/skills/MR_POPO.md` の **line 190-191**:

```
## 調査観点（RESEARCH フェーズで深掘りすること）
[Step 1 で聞いた「特に調査したい観点」があればそのまま。なければ「基本的な利用パターンと想定されるハマりどころ」]
```

の直後 (line 191 の後) に、空行を 1 行挟んでから以下の段落をそのまま追記する (日本語・2-3 文・実測値根拠を含む):

```
**Docker image variant の選好:** POC 用途で公式 Docker image を使う場合、可能な限り `-slim` / `-alpine` の軽量バリアントを優先して指定せよ。実測で `python:2.7` フル版 (~900 MB) の pull に 14 分かかった一方、`python:2.7-slim` (~100 MB) は 2 分で完了し、PREPARE フェーズ全体が 19:30 → 2:46 と 7× 高速化した (2026-04-20 検証)。フル版でしか動かない依存 (例: apt で GUI ライブラリを要求する等) が明確に判明した場合のみフル版にフォールバックせよ。
```

**制約:**
- 既存セクションの並び・テンプレート (line 178-213 の MISSION.md 生成ルールの markdown コードブロック構造) を破壊しないこと。
- 追記位置は `## 調査観点（RESEARCH フェーズで深掘りすること）` の説明行の直下 (line 191 と line 192 の間 = 次の `## 完了条件` セクションの前)。
- 他のセクション (Step 0/1/2/3, 界王星ヒアリング, KAIO-MISSION.md 生成ルール等) は一切触らない。
- 他のファイルは一切触らない (MR_POPO.md のみ)。
  </action>
  <verify>
    <automated>grep -n "Docker image variant の選好" /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && grep -n "python:2.7-slim" /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md</automated>
  </verify>
  <done>
- MR_POPO.md に「Docker image variant の選好」段落が 1 箇所だけ存在する
- 段落内に実測値 (19:30 → 2:46 もしくは 14 分 → 2 分) が明記されている
- 既存の Step 0/1/2/3, 界王星, KAIO 関連セクションが改変されていない (git diff で確認)
- MR_POPO.md 以外のファイルに変更がない
  </done>
</task>

</tasks>

<success_criteria>
- `git diff --stat` で変更ファイルが `spirit-room-manager/skills/MR_POPO.md` のみ
- 追加行は 4 行以内 (空行 1 + 段落 2-3 文 ≈ 1-3 行)
- 削除行は 0 (既存行を消さない)
- 段落は日本語で、実測値を根拠として含む
</success_criteria>

<output>
修行完了後、以下を報告:
- MR_POPO.md の変更箇所 (行番号)
- 追加された段落内容
- `git diff` の要約
</output>
