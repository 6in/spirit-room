---
phase: quick-260418-257
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/scripts/start-training-kaio.sh
autonomous: true
requirements:
  - QUICK-260418-257
user_setup: []

must_haves:
  truths:
    - "kaio モードの PHASE KAIO-3 (REPORT) で `claude -p \"/create-report\"` が `Unknown command` を返さず skill 解決する"
    - "CLAUDE_CONFIG_DIR が空のケース (通常モード想定外呼び出し) でもスクリプトが落ちずに継続する"
    - "再実行しても重複エラーを出さず同じ結果になる (冪等)"
    - "既存の project-level 配布 (`/workspace/.claude/commands/create-report.md`) は残り、両系統で skill が発見される"
  artifacts:
    - path: "spirit-room/base/scripts/start-training-kaio.sh"
      provides: "PHASE KAIO-1 で create-report.md を CLAUDE_CONFIG_DIR/commands/ にもコピーするブロック"
      contains: "CLAUDE_CONFIG_DIR/commands/create-report.md"
  key_links:
    - from: "spirit-room/base/scripts/start-training-kaio.sh PHASE KAIO-1"
      to: "$CLAUDE_CONFIG_DIR/commands/create-report.md"
      via: "mkdir -p + cp -f (冪等コピー)"
      pattern: "CLAUDE_CONFIG_DIR.*/commands/create-report\\.md"
    - from: "spirit-room/base/scripts/start-training-kaio.sh PHASE KAIO-3"
      to: "claude -p \"/create-report\" の skill 解決"
      via: "user-level commands ディレクトリが CLAUDE_CONFIG_DIR 配下に存在 → cwd 非依存で発見"
      pattern: "claude.*-p.*\"/create-report\""
---

<objective>
界王星モードの PHASE KAIO-3 (REPORT) で `/create-report` skill が cwd に依存せず発見されるようにする。
`start-training-kaio.sh` の PHASE KAIO-1 にて、既存の project-level (`/workspace/.claude/commands/`) に加え、
user-level (`$CLAUDE_CONFIG_DIR/commands/`) にも create-report.md を配布する。

Purpose: claude CLI が `/workspace` 以外の cwd で起動しても skill を見失わないようにし、
PHASE 3 の `Unknown command: /create-report` エラーを解消して質の高い振り返りレポート生成を成立させる。

Output: 修正済み start-training-kaio.sh (PHASE KAIO-1 の skill インストールブロックが二系統コピーに拡張されている)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/todos/completed/2026-04-18-kaio-create-report-skill-missing.md
@spirit-room/base/scripts/start-training-kaio.sh
@spirit-room/base/scripts/create-report.md
@spirit-room/base/entrypoint.sh
@spirit-room/base/Dockerfile

<interfaces>
<!-- 修正対象コードの現状 (start-training-kaio.sh L14-55 抜粋) -->
<!-- CLAUDE_CONFIG_DIR は L16 で default `/workspace/.claude-home` を設定済み、L17 で export 済み -->

```bash
# L14-17
# CLAUDE_CONFIG_DIR は entrypoint で設定されている前提。念のため default を用意
: "${CLAUDE_CONFIG_DIR:=/workspace/.claude-home}"
export CLAUDE_CONFIG_DIR

# L48-51 (今回の修正対象ブロック)
# 界王星スキルをプロジェクトの .claude/commands/ にインストール
mkdir -p /workspace/.claude/commands
cp /room/scripts/create-report.md /workspace/.claude/commands/create-report.md
log "スキル /create-report インストール完了"
```

既存の `log()` 関数 (L21): `log() { echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "$LOG_FILE"; }`
既存のスタイル規約: 4-space indent、`"$var"` 引用、`set -e`、`[INFO]/[ERROR]` prefix は log 関数経由。
</interfaces>

<build_note>
`spirit-room/base/scripts/start-training-kaio.sh` は Dockerfile L79 の `COPY scripts/ /room/scripts/` でイメージに焼き込まれる。
実際の kaio コンテナに修正を反映するには、コード修正後に `./spirit-room/build-base.sh` でベースイメージを再ビルドし、
既存の kaio コンテナは `spirit-room close` → `spirit-room kaio ...` で作り直す必要がある。
本プランでは再ビルドまでを完了条件に含める (実機 REPORT 生成確認は UAT 扱いで後工程)。
</build_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: start-training-kaio.sh の PHASE KAIO-1 に user-level skill 配布を追加</name>
  <files>spirit-room/base/scripts/start-training-kaio.sh</files>
  <action>
`spirit-room/base/scripts/start-training-kaio.sh` の PHASE KAIO-1 セクション (L48-51) を以下のように書き換える。
既存の project-level コピー (`/workspace/.claude/commands/`) はそのまま残し、その直後に
user-level コピー (`$CLAUDE_CONFIG_DIR/commands/`) を追加する。

**修正前 (L48-51):**
```bash
    # 界王星スキルをプロジェクトの .claude/commands/ にインストール
    mkdir -p /workspace/.claude/commands
    cp /room/scripts/create-report.md /workspace/.claude/commands/create-report.md
    log "スキル /create-report インストール完了"
```

**修正後:**
```bash
    # 界王星スキルを配布する。
    # 1) project-level (cwd=/workspace で起動した claude が拾う fallback)
    # 2) user-level $CLAUDE_CONFIG_DIR/commands/ (cwd に依存せず常に解決される本命)
    #    PHASE KAIO-3 の `claude -p "/create-report"` は tmux pane や goku HOME 起点で起動する
    #    ケースがあり project-level だけでは Unknown command になる。
    mkdir -p /workspace/.claude/commands
    cp -f /room/scripts/create-report.md /workspace/.claude/commands/create-report.md
    log "スキル /create-report を project-level に配置: /workspace/.claude/commands/create-report.md"

    # CLAUDE_CONFIG_DIR は L16 で default 値が入る想定だが、念のため空ガード。
    # 空であっても PHASE KAIO-1 全体を止めないよう warn のみ出して継続する。
    if [ -n "${CLAUDE_CONFIG_DIR:-}" ]; then
        mkdir -p "$CLAUDE_CONFIG_DIR/commands"
        cp -f /room/scripts/create-report.md "$CLAUDE_CONFIG_DIR/commands/create-report.md"
        log "スキル /create-report を user-level に配置: $CLAUDE_CONFIG_DIR/commands/create-report.md"
    else
        log "[WARN] CLAUDE_CONFIG_DIR が未設定のため user-level skill 配布を skip"
    fi
```

**要点:**
- `cp` を `cp -f` に変更 (既存ファイルがあっても上書き、冪等)
- `mkdir -p` も冪等なのでそのまま
- CLAUDE_CONFIG_DIR 空ガードは `[ -n "${CLAUDE_CONFIG_DIR:-}" ]` 形式 (他箇所の entrypoint.sh L101/L122 と統一)
- log メッセージは日本語 + フルパス、既存 `log()` 関数に揃える ([WARN] prefix は entrypoint.sh L50/L113 と同じスタイル)
- PREPARED_FLAG (L39) の外側にはコードを増やさない。`if [ -f "$PREPARED_FLAG" ]` ブランチの中で完結させる

**触ってはいけない箇所:**
- L16 の CLAUDE_CONFIG_DIR default 設定はそのまま
- PHASE KAIO-2 / PHASE KAIO-3 の本体は一切変更しない
- oauthAccount マージブロック (L62-75) は変更しない
  </action>
  <verify>
<automated>bash -n spirit-room/base/scripts/start-training-kaio.sh && grep -E 'CLAUDE_CONFIG_DIR.*/commands/create-report\.md' spirit-room/base/scripts/start-training-kaio.sh && grep -E '/workspace/\.claude/commands/create-report\.md' spirit-room/base/scripts/start-training-kaio.sh && grep -c 'cp -f /room/scripts/create-report\.md' spirit-room/base/scripts/start-training-kaio.sh | grep -q '^2$'</automated>
  </verify>
  <done>
- `bash -n` (構文チェック) が exit 0 で通る
- `/workspace/.claude/commands/create-report.md` へのコピー行が存在する
- `$CLAUDE_CONFIG_DIR/commands/create-report.md` へのコピー行が存在する
- `cp -f /room/scripts/create-report.md` パターンが 2 回出現する (project-level と user-level)
- CLAUDE_CONFIG_DIR 空ガード (`[ -n "${CLAUDE_CONFIG_DIR:-}" ]`) が skill 配布ブロックに存在する
  </done>
</task>

<task type="auto">
  <name>Task 2: ベースイメージを再ビルドして修正を /room/scripts/ に反映</name>
  <files>(ビルド成果物のみ、ソースファイル変更なし)</files>
  <action>
start-training-kaio.sh は Dockerfile L79 `COPY scripts/ /room/scripts/` でイメージに焼き込まれるため、
Task 1 の修正を実機に反映するには再ビルドが必須。

```bash
cd /home/parallels/workspaces/spirit-room-full/spirit-room
./build-base.sh
```

ビルド完了後、以下でイメージ内部のスクリプトが更新されたことを確認する:

```bash
# イメージから修正後スクリプトを取り出して grep (containerを起動せず docker run --rm で完結)
docker run --rm --entrypoint cat spirit-room-base:latest /room/scripts/start-training-kaio.sh \
    | grep -E 'CLAUDE_CONFIG_DIR.*/commands/create-report\.md'
```

既存の kaio コンテナがある場合は再起動が必要なことをユーザーに伝えるが、このタスクでは close/re-open までは実行しない
(実機での REPORT.md 生成確認は後工程の UAT で扱う)。
  </action>
  <verify>
<automated>docker run --rm --entrypoint cat spirit-room-base:latest /room/scripts/start-training-kaio.sh 2>/dev/null | grep -qE 'CLAUDE_CONFIG_DIR.*/commands/create-report\.md'</automated>
  </verify>
  <done>
- `./build-base.sh` が exit 0 で完了
- `spirit-room-base:latest` イメージ内の `/room/scripts/start-training-kaio.sh` に user-level コピー行が含まれている
- ユーザーには「既存 kaio コンテナは `spirit-room close` → `spirit-room kaio <folder>` で作り直して反映」と報告する
  </done>
</task>

</tasks>

<verification>
Plan 全体として:
1. `bash -n spirit-room/base/scripts/start-training-kaio.sh` が成功する
2. ソース側と焼き込み済みイメージ側の両方で「CLAUDE_CONFIG_DIR/commands/create-report.md」が確認できる
3. 既存の PHASE KAIO-2 / KAIO-3 ロジック (GSD チェーンループ、REPORT 生成) は一切変更されていない

後工程 (本プラン範囲外) の UAT:
- `spirit-room close` & `spirit-room kaio <test-folder>` で新しい kaio コンテナを起動
- PHASE KAIO-3 到達時に `Unknown command: /create-report` が出ないこと
- `/workspace/REPORT.md` が skill 経由のテンプレート通りの構造 (ミッション/成果/タイムライン/技術的な判断/...) で生成されていること
</verification>

<success_criteria>
- start-training-kaio.sh の PHASE KAIO-1 が二系統 (project-level + user-level) で /create-report skill を配布する
- CLAUDE_CONFIG_DIR 空のケースでもスクリプトが fail-fast せず WARN ログで継続する
- コピー操作は `cp -f` + `mkdir -p` で冪等化されている (再実行しても同じ状態になる)
- ベースイメージ再ビルドが完了し、新しい kaio コンテナでは cwd 非依存で `/create-report` が解決できる状態になっている
</success_criteria>

<output>
After completion, create `.planning/quick/260418-257-kaio-create-report-skill-claude-config-d/260418-257-SUMMARY.md`
with:
- 何を変更したか (diff 要点)
- build-base.sh の成功確認結果
- ユーザー側アクション (既存 kaio コンテナの close/re-open 指示)
- 次のステップ (UAT での実機 REPORT.md 生成確認)
</output>
