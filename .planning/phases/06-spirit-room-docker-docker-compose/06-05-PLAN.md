---
phase: 06-spirit-room-docker-docker-compose
plan: 05
type: execute
wave: 3
depends_on:
  - 06-04-PLAN.md
files_modified: []
autonomous: false
requirements: []
# Phase 6 には REQ-ID なし。E2E 検証で全 D-01〜D-21 の統合動作を確認する
user_setup: []

must_haves:
  truths:
    - "build-base.sh が成功しイメージに docker CLI が入っている"
    - "--docker フラグで起動した部屋内で docker ps が (sudo なしで) 通る、または sudo docker ps が通る"
    - "env に SPIRIT_ROOM_DOCKER=1 / HOST_WORKSPACE / COMPOSE_PROJECT_NAME が揃っている"
    - "docker compose up で nginx 兄弟コンテナが起動し host.docker.internal 経由で到達できる"
    - "spirit-room close で部屋本体と兄弟コンテナが両方消える"
    - "--docker なしの spirit-room open で従来通り起動し docker.sock がマウントされない"
  artifacts:
    - path: "spirit-room/base/Dockerfile"
      provides: "docker CLI 入りビルド済みイメージ"
      contains: "get.docker.com"
    - path: "spirit-room/spirit-room"
      provides: "--docker フラグ対応 CLI"
      contains: "_docker_extra_args"
    - path: "spirit-room/base/entrypoint.sh"
      provides: "docker グループ合流ロジック"
      contains: "SPIRIT_ROOM_DOCKER"
    - path: "spirit-room/base/catalog.md"
      provides: "Docker Compose モードガイダンス"
      contains: "Docker Compose モード"
  key_links:
    - from: "spirit-room open --docker"
      to: "部屋内 docker ps"
      via: "docker.sock マウント + goku docker グループ合流"
      pattern: "docker grp"
    - from: "spirit-room close"
      to: "兄弟コンテナ削除"
      via: "docker ps --filter label=com.docker.compose.project"
      pattern: "兄弟コンテナ"
---

<objective>
Phase 6 の全変更を統合して E2E 検証する。

Purpose: イメージ再ビルドから始め、--docker フラグでの起動・兄弟コンテナの compose 起動・host.docker.internal 到達・close 時の自動掃除・非回帰 (--docker なし) の 9 ステップを人間が確認する。

Output: 全 9 ステップが PASS し、CONTEXT.md の 06-DISCUSSION-LOG にある todos が完了した状態。
</objective>

<execution_context>
@/home/parallels/.claude/get-shit-done/workflows/execute-plan.md
@/home/parallels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/ROADMAP.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: イメージ再ビルドと静的検証</name>
  <files></files>
  <read_first>
    - spirit-room/base/Dockerfile (Plan 01 完了後の状態)
    - spirit-room/spirit-room (Plan 04 完了後の状態)
    - spirit-room/base/entrypoint.sh (Plan 02 完了後の状態)
    - spirit-room/base/catalog.md (Plan 03 完了後の状態)
  </read_first>
  <action>
以下の静的検証を実行し、全て PASS することを確認する。

**静的検証コマンド:**

```bash
# 1. シェルスクリプト構文チェック
bash -n spirit-room/spirit-room && echo "CLI syntax: OK"
bash -n spirit-room/base/entrypoint.sh && echo "entrypoint syntax: OK"

# 2. Dockerfile の必須パターン確認
grep -q 'get\.docker\.com' spirit-room/base/Dockerfile && echo "Dockerfile docker CLI layer: OK"
grep -q 'レイヤー5.5' spirit-room/base/Dockerfile && echo "Dockerfile layer comment: OK"

# 3. CLI の必須パターン確認
grep -q '_docker_extra_args' spirit-room/spirit-room && echo "CLI _docker_extra_args: OK"
grep -q 'docker_flag' spirit-room/spirit-room && echo "CLI docker_flag: OK"
grep -q 'com.docker.compose.project' spirit-room/spirit-room && echo "CLI sibling cleanup: OK"
grep -q 'SPIRIT_ROOM_DOCKER' spirit-room/spirit-room && echo "CLI SPIRIT_ROOM_DOCKER: OK"

# 4. entrypoint の必須パターン確認
grep -q 'SPIRIT_ROOM_DOCKER' spirit-room/base/entrypoint.sh && echo "entrypoint docker mode: OK"
grep -q 'HOST_DOCKER_GID' spirit-room/base/entrypoint.sh && echo "entrypoint HOST_DOCKER_GID: OK"
grep -q 'docker grp' spirit-room/base/entrypoint.sh && echo "entrypoint docker grp log: OK"

# 5. catalog.md の必須パターン確認
grep -q 'Docker Compose モード' spirit-room/base/catalog.md && echo "catalog Docker section: OK"
grep -q 'HOST_WORKSPACE' spirit-room/base/catalog.md && echo "catalog HOST_WORKSPACE: OK"
```

全て OK の場合、イメージをリビルドする:

```bash
cd spirit-room && ./build-base.sh
```

リビルド後に docker CLI の存在を確認:

```bash
docker run --rm spirit-room-base:latest docker --version
docker run --rm spirit-room-base:latest docker compose version
```

両方のバージョン文字列が出力されることを確認する。
  </action>
  <verify>
    <automated>
      bash -n /home/parallels/workspaces/spirit-room-full/spirit-room/spirit-room && \
      bash -n /home/parallels/workspaces/spirit-room-full/spirit-room/base/entrypoint.sh && \
      grep -q 'get\.docker\.com' /home/parallels/workspaces/spirit-room-full/spirit-room/base/Dockerfile && \
      grep -q '_docker_extra_args' /home/parallels/workspaces/spirit-room-full/spirit-room/spirit-room && \
      grep -q 'com.docker.compose.project' /home/parallels/workspaces/spirit-room-full/spirit-room/spirit-room && \
      grep -q 'SPIRIT_ROOM_DOCKER' /home/parallels/workspaces/spirit-room-full/spirit-room/base/entrypoint.sh && \
      grep -q 'Docker Compose モード' /home/parallels/workspaces/spirit-room-full/spirit-room/base/catalog.md && \
      echo "ALL STATIC CHECKS PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - 全 11 の static check が OK を返す
    - `./build-base.sh` が exit 0 で完了する
    - `docker run --rm spirit-room-base:latest docker --version` が "Docker version" 文字列を出力する
    - `docker run --rm spirit-room-base:latest docker compose version` が "Docker Compose version" 文字列を出力する
  </acceptance_criteria>
  <done>spirit-room-base:latest イメージに docker CLI + compose plugin が含まれ、静的検証が全通過</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E 動作検証 (9 ステップ)</name>
  <what-built>
    - Dockerfile に docker CLI レイヤーを追加し spirit-room-base:latest を再ビルド (Plan 01)
    - entrypoint.sh に docker グループ動的合流ロジックを追加 (Plan 02)
    - catalog.md に Docker Compose モードセクションを追加 (Plan 03)
    - spirit-room CLI に --docker フラグ / _docker_extra_args() / 兄弟掃除 / バナー警告を追加 (Plan 04)
  </what-built>
  <how-to-verify>
以下の 9 ステップを順番に実行し、各ステップの期待結果を確認してください。

**事前準備: テスト用フォルダと compose.yaml を作成**

```bash
mkdir -p /tmp/test-compose-proj/html
echo '<h1>Spirit Room Docker Test</h1>' > /tmp/test-compose-proj/html/index.html
cat > /tmp/test-compose-proj/compose.yaml << 'EOF'
services:
  web:
    image: nginx:alpine
    ports:
      - "8181:80"
    volumes:
      - ${HOST_WORKSPACE}/html:/usr/share/nginx/html:ro
EOF
```

**Step 1: docker CLI がイメージに入っているか**

```bash
docker run --rm spirit-room-base:latest docker --version
docker run --rm spirit-room-base:latest docker compose version
```

期待: 両方ともバージョン文字列が出力される

**Step 2: --docker フラグで部屋を起動**

```bash
./spirit-room/spirit-room open --docker /tmp/test-compose-proj
```

期待: バナーに `[WARN] --docker モード:` の警告行が出る

**Step 3: 起動ログに docker グループ合流が出るか**

```bash
./spirit-room/spirit-room logs /tmp/test-compose-proj
# または docker logs spirit-room-test-compose-proj
```

期待: `[INFO] docker grp: goku ∈ ...` または `[INFO] docker grp: HOST_DOCKER_GID 未取得 — sudo docker を使用せよ` のいずれかが出る

**Step 4: env 変数が揃っているか**

部屋に入って確認:
```bash
./spirit-room/spirit-room enter /tmp/test-compose-proj
# 部屋の中で:
env | grep -E "SPIRIT_ROOM_DOCKER|HOST_WORKSPACE|HOST_DOCKER_GID|COMPOSE_PROJECT_NAME"
```

期待: `SPIRIT_ROOM_DOCKER=1` / `HOST_WORKSPACE=/tmp/test-compose-proj` / `COMPOSE_PROJECT_NAME=spirit-room-test-compose-proj` が全て見える

**Step 5: docker コマンドが動くか**

部屋の中で:
```bash
docker ps
# 通らない場合:
sudo docker ps
```

期待: どちらかで exit 0 になる

**Step 6: docker compose up で nginx を起動**

部屋の中で:
```bash
cd /workspace
docker compose up -d
# または
sudo docker compose up -d
```

期待: nginx コンテナが起動する

**Step 7: host.docker.internal でサービスに到達できるか**

部屋の中で:
```bash
curl -s http://${SPIRIT_ROOM_HOST_GATEWAY:-host.docker.internal}:8181/ | head -5
```

期待: `Spirit Room Docker Test` の HTML が返ってくる

**Step 8: spirit-room close で部屋本体と兄弟が両方消えるか**

部屋から exit して、ホストで:
```bash
./spirit-room/spirit-room close /tmp/test-compose-proj
```

期待: `[INFO] 兄弟コンテナ N 件を削除しました` のログが出る

確認:
```bash
docker ps -a | grep 'test-compose-proj'
docker ps -a | grep nginx
```

期待: 両方とも表示されない

**Step 9: --docker なしで起動しても非回帰**

```bash
mkdir -p /tmp/test-noDocker
./spirit-room/spirit-room open /tmp/test-noDocker
./spirit-room/spirit-room enter /tmp/test-noDocker
# 部屋の中で:
env | grep SPIRIT_ROOM_DOCKER
docker ps   # docker.sock がないので失敗するはず
# exit して:
./spirit-room/spirit-room close /tmp/test-noDocker
```

期待:
- `SPIRIT_ROOM_DOCKER=1` が env に出ない
- `docker ps` が "Cannot connect to the Docker daemon" 等のエラーになる (sock なし)
- close は正常完了し `[INFO] 兄弟コンテナは見つかりませんでした` が出る
  </how-to-verify>
  <resume-signal>全 9 ステップが期待通りなら「approved」と入力してください。問題があれば Step 番号と症状を教えてください。</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 駆動 todo を completed へ移動</name>
  <files>.planning/todos/pending/2026-04-18-spirit-room-docker-docker-compose.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-18-spirit-room-docker-docker-compose.md (移動対象の確認)
  </read_first>
  <action>
E2E 検証 (Task 2) が approved になったら、駆動 todo を `pending/` から `completed/` に移動する。

```bash
mv .planning/todos/pending/2026-04-18-spirit-room-docker-docker-compose.md \
   .planning/todos/completed/2026-04-18-spirit-room-docker-docker-compose.md
```

ファイルの先頭に完了メタデータを追加する (ファイル先頭に挿入):

```markdown
**Completed:** 2026-04-18
**Phase:** 06-spirit-room-docker-docker-compose
**Plans:** 06-01 〜 06-05

---

```

(その後に既存の内容が続く形)
  </action>
  <verify>
    <automated>
      ls /home/parallels/workspaces/spirit-room-full/.planning/todos/completed/2026-04-18-spirit-room-docker-docker-compose.md && \
      ! ls /home/parallels/workspaces/spirit-room-full/.planning/todos/pending/2026-04-18-spirit-room-docker-docker-compose.md 2>/dev/null && \
      echo "OK: todo moved to completed"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/todos/completed/2026-04-18-spirit-room-docker-docker-compose.md` が存在する
    - `.planning/todos/pending/2026-04-18-spirit-room-docker-docker-compose.md` が存在しない
  </acceptance_criteria>
  <done>駆動 todo が completed に移動された</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E テスト環境 → ホスト docker daemon | /tmp テストフォルダで nginx を起動するため、テスト中は docker.sock が実際にアクセスされる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Elevation of Privilege | E2E テスト中の docker.sock アクセス | accept | テスト用途のみ。nginx:alpine という既知のイメージを使う。テスト完了後は spirit-room close で全コンテナを削除する |
</threat_model>

<verification>
9 ステップの human-verify が全て PASS
駆動 todo が completed に移動
</verification>

<success_criteria>
- spirit-room-base:latest に docker CLI が入っている (docker --version が動く)
- --docker フラグで起動した部屋から docker compose up で nginx が起動する
- host.docker.internal:8181 で nginx に到達できる
- spirit-room close で部屋と兄弟コンテナが両方消える
- --docker なし起動は従来通り動き docker.sock がマウントされない
- 駆動 todo が completed に移動
</success_criteria>

<output>
After completion, create `.planning/phases/06-spirit-room-docker-docker-compose/06-05-SUMMARY.md`
</output>
