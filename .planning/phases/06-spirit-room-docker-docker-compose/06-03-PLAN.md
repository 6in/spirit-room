---
phase: 06-spirit-room-docker-docker-compose
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/catalog.md
autonomous: true
requirements: []
# Phase 6 には REQ-ID なし
# Implements: D-20, D-21
# (catalog.md 末尾に Docker Compose モードセクション追加。/workspace > /room 優先順位不変)
user_setup: []

must_haves:
  truths:
    - "catalog.md 末尾に '## Docker Compose モード (SPIRIT_ROOM_DOCKER=1 時)' セクションが存在する"
    - "セクションに HOST_WORKSPACE を使うボリューム記法の説明が含まれる"
    - "セクションに host.docker.internal でサービスに到達する方法が含まれる"
    - "セクションに COMPOSE_PROJECT_NAME を compose.yaml に書かない旨の警告が含まれる"
    - "セクションに docker グループ未取得時の sudo フォールバック説明が含まれる"
    - "セクションにセキュリティ注意 (sock = ホスト root 相当) が含まれる"
    - "既存の catalog.md 内容 (Claude Code / opencode / 標準ツール等) が変更されていない"
  artifacts:
    - path: "spirit-room/base/catalog.md"
      provides: "Docker Compose モードガイダンスセクション"
      contains: "Docker Compose モード"
  key_links:
    - from: "spirit-room/base/catalog.md"
      to: "HOST_WORKSPACE env var"
      via: "ボリューム記法の説明"
      pattern: "HOST_WORKSPACE"
---

<objective>
catalog.md 末尾に Docker Compose モード向けガイダンスセクションを追加する。

Purpose: `--docker` モードで起動した部屋内の Claude が compose.yaml を正しく書けるように。ホスト dockerd がボリュームパスを解釈するため `/workspace/...` ではなく `${HOST_WORKSPACE}/...` を使う必要があることと、サービスへのアクセス方法、COMPOSE_PROJECT_NAME の制約、sudo フォールバック、セキュリティ注意を明記する。

Output: `spirit-room/base/catalog.md` の末尾に新セクションが追加された状態。既存コンテンツは変更なし。
</objective>

<execution_context>
@/home/parallels/.claude/get-shit-done/workflows/execute-plan.md
@/home/parallels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/ROADMAP.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: catalog.md 末尾に Docker Compose モードセクションを追加</name>
  <files>spirit-room/base/catalog.md</files>
  <read_first>
    - spirit-room/base/catalog.md (現行全体 — 末尾の行と既存セクション構造を確認)
    - .planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md §decisions D-20, D-21 および §specifics catalog.md 追加セクションの骨子
  </read_first>
  <action>
`spirit-room/base/catalog.md` の現在の末尾 (最終 ``` 行の後) に、以下のセクションを追記する。前後に空行を 1 行入れる。

**追記内容:**

```markdown

---

## Docker Compose モード (SPIRIT_ROOM_DOCKER=1 時)

> **このセクションは `SPIRIT_ROOM_DOCKER=1` 環境変数が設定されている時のみ適用される。**
> `env | grep SPIRIT_ROOM_DOCKER` で確認せよ。未設定ならこのセクションは無視してよい。

### 使える挙動

- ホスト側の docker daemon が叩ける (`/var/run/docker.sock` マウント済み)
- `docker compose up` でホスト上に兄弟コンテナを起動できる
- 部屋から兄弟コンテナへは `host.docker.internal:<PORT>` でアクセス可能

### 重要なルール

#### ボリュームパスは `${HOST_WORKSPACE}` を使う

compose.yaml は**ホスト dockerd** が解釈するので、コンテナ内パス (`/workspace/data`) ではなく**ホスト側の絶対パス**が必要。

```yaml
# ❌ これは動かない: コンテナ内の相対パスをホスト dockerd が解釈できない
services:
  app:
    volumes:
      - ./data:/app/data

# ✅ これを使う: HOST_WORKSPACE がホスト側の絶対パスを保持している
services:
  app:
    volumes:
      - ${HOST_WORKSPACE}/data:/app/data
```

#### `COMPOSE_PROJECT_NAME` は compose.yaml に書かない

既に `$COMPOSE_PROJECT_NAME` が部屋名 (`spirit-room-<フォルダ名>`) にセットされている。

compose.yaml に `name:` を書くと上書きされ、`spirit-room close` 時の兄弟コンテナ自動掃除が効かなくなる。**書かないこと**。

#### サービスへのアクセスは `host.docker.internal`

部屋内から compose サービスの `localhost:PORT` は見えない (兄弟コンテナはホストから見た別ネームスペース)。

```bash
# 部屋内からアクセスする場合
curl http://${SPIRIT_ROOM_HOST_GATEWAY:-host.docker.internal}:8080/
```

#### docker グループが使えない場合は `sudo`

起動ログに `docker grp: 合流失敗` または `HOST_DOCKER_GID 未取得` とあれば、`sudo docker ...` / `sudo docker compose ...` を使う (Phase 5 で goku に `NOPASSWD:ALL` 付与済み)。

### compose.yaml の最小例

```yaml
# nginx で動作確認する最小 compose.yaml
# /workspace/compose.yaml に置いて docker compose up -d で起動
services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ${HOST_WORKSPACE}/html:/usr/share/nginx/html:ro
```

起動確認:
```bash
docker compose up -d
curl http://${SPIRIT_ROOM_HOST_GATEWAY:-host.docker.internal}:8080/
```

掃除は部屋の外から `spirit-room close` を実行するだけ (兄弟コンテナも自動削除される)。

### セキュリティ注意

`/var/run/docker.sock` マウント = ホスト root 相当の権限。以下の操作はしないこと:

- 他の部屋 (spirit-room-\*) を stop / rm する
- ホストのシステムコンテナを操作する
- ミッション範囲を超えた docker 操作全般
```

**禁止事項:**
- 既存の `## Claude Code` / `## opencode` / `## 標準ツール` / `## ディレクトリ規約` / `## 追加ツールのインストール` セクションを変更しない
- セクションを既存コンテンツの途中に挿入しない (末尾追記のみ)
  </action>
  <verify>
    <automated>
      grep -n 'Docker Compose モード' /home/parallels/workspaces/spirit-room-full/spirit-room/base/catalog.md && \
      grep -n 'HOST_WORKSPACE' /home/parallels/workspaces/spirit-room-full/spirit-room/base/catalog.md && \
      grep -n 'COMPOSE_PROJECT_NAME' /home/parallels/workspaces/spirit-room-full/spirit-room/base/catalog.md && \
      grep -n 'host.docker.internal' /home/parallels/workspaces/spirit-room-full/spirit-room/base/catalog.md && \
      echo "OK: catalog.md has Docker Compose section"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Docker Compose モード' spirit-room/base/catalog.md` が 1 を返す
    - `grep -c 'HOST_WORKSPACE' spirit-room/base/catalog.md` が 3 以上を返す (説明・禁止例・推奨例に登場)
    - `grep -c 'COMPOSE_PROJECT_NAME' spirit-room/base/catalog.md` が 1 以上を返す
    - `grep -c 'host.docker.internal' spirit-room/base/catalog.md` が 2 以上を返す
    - `grep -c 'SPIRIT_ROOM_DOCKER' spirit-room/base/catalog.md` が 2 以上を返す (冒頭警告 + 見出し)
    - `grep -c 'sudo docker' spirit-room/base/catalog.md` が 1 以上を返す (フォールバック説明)
    - `grep -c 'ホスト root 相当' spirit-room/base/catalog.md` が 1 以上を返す (セキュリティ注意)
    - `grep -c '## Claude Code' spirit-room/base/catalog.md` が 1 を返す (既存セクションが消えていない)
    - `grep -c '## opencode' spirit-room/base/catalog.md` が 1 を返す (既存セクションが消えていない)
  </acceptance_criteria>
  <done>catalog.md 末尾に Docker Compose モードガイダンスが追加され、既存コンテンツは変更されていない</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog.md → Claude エージェント | エージェントがこのドキュメントを読んで docker 操作を行う |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | catalog.md セキュリティ注意 | mitigate | sock マウント = ホスト root 相当の明示警告を記載。エージェントがミッション範囲外の操作をしないよう明文化 |
</threat_model>

<verification>
`grep -c 'Docker Compose モード' spirit-room/base/catalog.md` が 1 を返す
既存セクション (`## Claude Code`, `## opencode`) が消えていない
</verification>

<success_criteria>
- catalog.md に Docker Compose モードセクションが存在する
- HOST_WORKSPACE / host.docker.internal / COMPOSE_PROJECT_NAME の使い方が明記されている
- セキュリティ注意が含まれる
</success_criteria>

<output>
After completion, create `.planning/phases/06-spirit-room-docker-docker-compose/06-03-SUMMARY.md`
</output>
