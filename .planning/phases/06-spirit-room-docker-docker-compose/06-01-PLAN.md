---
phase: 06-spirit-room-docker-docker-compose
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/Dockerfile
autonomous: true
requirements: []
# Phase 6 には REQ-ID なし。非回帰要件: BUILD-01, BUILD-02 (--docker 未指定時も image build が通ること)
# Implements: D-01, D-02, D-03 (docker CLI + compose plugin を常時同梱、get.docker.com | sh、Node/Bun 後・SSH 前レイヤー)
user_setup: []

must_haves:
  truths:
    - "spirit-room-base:latest に docker CLI が常時インストールされている"
    - "docker compose plugin が docker compose サブコマンドとして使える"
    - "dockerd はコンテナ内で起動しない (CLI のみ存在)"
    - "Dockerfile の既存レイヤー (SSH 設定以降) のキャッシュが壊れない"
  artifacts:
    - path: "spirit-room/base/Dockerfile"
      provides: "docker CLI + compose plugin レイヤー (レイヤー5.5)"
      contains: "get.docker.com"
  key_links:
    - from: "spirit-room/base/Dockerfile"
      to: "docker CLI"
      via: "curl -fsSL https://get.docker.com | sh"
      pattern: "get\\.docker\\.com"
---

<objective>
Dockerfile に docker CLI + compose plugin レイヤーを追加する。

Purpose: コンテナ内で `docker` コマンドと `docker compose` サブコマンドを使えるようにする。`--docker` フラグ未使用時もイメージに同梱されているが、docker.sock がマウントされないため実害はない (d-01 の判断)。

Output: `spirit-room/base/Dockerfile` に `# ── レイヤー5.5: Docker CLI ──` として新規レイヤーが追加された状態。
</objective>

<execution_context>
@/home/parallels/.claude/get-shit-done/workflows/execute-plan.md
@/home/parallels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/ROADMAP.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile にレイヤー5.5 (docker CLI + compose plugin) を追加</name>
  <files>spirit-room/base/Dockerfile</files>
  <read_first>
    - spirit-room/base/Dockerfile (現行全体 — レイヤー境界を確認してから編集)
    - .planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md §decisions D-01, D-02, D-03 および §code_context Integration Points
  </read_first>
  <action>
`spirit-room/base/Dockerfile` の `# ── レイヤー5: opencode ──` ブロック (line 50-51) と `# ── SSH設定 ──` ブロック (line 53) の間に、以下の新規レイヤーを挿入する。

**挿入内容 (空行で前後を区切る):**

```dockerfile
# ── レイヤー5.5: Docker CLI (compose plugin 込み) ─────────────
# dockerd は起動しない。CLI と compose plugin だけ使う (DooD 用)。
# --docker フラグ未使用時も常時同梱 (実害: CLI があるだけ。+~100MB)
RUN curl -fsSL https://get.docker.com | sh \
    && docker --version
```

**挿入場所 (before/after):**

挿入前:
```
# ── レイヤー5: opencode ──────────────────────────────────────
RUN npm install -g opencode-ai

# ── SSH設定 ─────────────────────────────────────────────────
```

挿入後:
```
# ── レイヤー5: opencode ──────────────────────────────────────
RUN npm install -g opencode-ai

# ── レイヤー5.5: Docker CLI (compose plugin 込み) ─────────────
# dockerd は起動しない。CLI と compose plugin だけ使う (DooD 用)。
# --docker フラグ未使用時も常時同梱 (実害: CLI があるだけ。+~100MB)
RUN curl -fsSL https://get.docker.com | sh \
    && docker --version

# ── SSH設定 ─────────────────────────────────────────────────
```

**禁止事項:**
- dockerd を起動する `ENTRYPOINT` や `CMD` を追加しない
- `DOCKER_HOST` 環境変数を Dockerfile に書かない (実行時に渡す)
- `groupadd docker` を Dockerfile に書かない (GID がホストごとに異なるため entrypoint 側で動的処理する)
- SSH 設定以降のレイヤーに変更を加えない
  </action>
  <verify>
    <automated>
      grep -n 'get\.docker\.com' /home/parallels/workspaces/spirit-room-full/spirit-room/base/Dockerfile && \
      grep -n 'docker --version' /home/parallels/workspaces/spirit-room-full/spirit-room/base/Dockerfile && \
      bash -n /home/parallels/workspaces/spirit-room-full/spirit-room/base/Dockerfile 2>/dev/null || true && \
      echo "OK: Dockerfile has docker CLI layer"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'get\.docker\.com' spirit-room/base/Dockerfile` が 1 を返す
    - `grep -c 'docker --version' spirit-room/base/Dockerfile` が 1 を返す
    - `grep -n 'レイヤー5.5' spirit-room/base/Dockerfile` がヒットする (レイヤーコメントが存在)
    - 新レイヤーは line 50-51 (opencode RUN) の直後、SSH 設定より前に位置する
    - `! grep -q 'groupadd docker' spirit-room/base/Dockerfile` — Dockerfile に docker グループ作成が入っていない
    - `! grep -q 'DOCKER_HOST' spirit-room/base/Dockerfile` — DOCKER_HOST env は入っていない
    - 既存の `# ── SSH設定 ──` コメント行が消えていない
  </acceptance_criteria>
  <done>spirit-room/base/Dockerfile に docker CLI レイヤーが挿入され、SSH 設定以降のレイヤー順序が維持されている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| イメージビルド → docker daemon | get.docker.com からのインストールスクリプトは HTTPS 経由で取得。MITM リスクはあるが既存の nodesource / bun.sh と同等の脅威レベル |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | get.docker.com install script | accept | HTTPS + Docker 公式ドメイン。他の curl | sh インストール (nodesource, bun) と同等リスクで既に許容済み |
| T-06-01-02 | Elevation of Privilege | docker CLI in image | mitigate | docker.sock は `--docker` フラグ opt-in 時のみマウント。デフォルトでは sock がないため CLI があっても特権操作不可 |
</threat_model>

<verification>
ビルドが通ること: `cd spirit-room && ./build-base.sh` (Plan 05 の E2E で実施)
単体確認: `docker run --rm spirit-room-base:latest docker --version` で docker CLI が応答する
</verification>

<success_criteria>
- spirit-room/base/Dockerfile に `# ── レイヤー5.5` ブロックが存在する
- get.docker.com インストール行が含まれる
- SSH 設定以降のレイヤー順序が変わっていない
</success_criteria>

<output>
After completion, create `.planning/phases/06-spirit-room-docker-docker-compose/06-01-SUMMARY.md`
</output>
