---
phase: 06-spirit-room-docker-docker-compose
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/entrypoint.sh
autonomous: true
requirements: []
# Phase 6 には REQ-ID なし。非回帰: Phase 5 の goku UID/GID 作成フローを壊さないこと
# Implements: D-04, D-05, D-06, D-07, D-08, D-12
# (entrypoint で HOST_DOCKER_GID を受け取り、goku を docker グループに動的合流させる)
user_setup: []

must_haves:
  truths:
    - "SPIRIT_ROOM_DOCKER=1 かつ HOST_DOCKER_GID が非空の時、goku がホストの docker GID に対応するグループに追加される"
    - "GID が既存グループと衝突する場合は既存グループ名を取得して usermod する (groupadd しない)"
    - "GID が既存グループに存在しない場合は docker という名前でグループを作成する"
    - "SPIRIT_ROOM_DOCKER=0 (未設定) の時は docker グループ合流ロジックがスキップされる"
    - "HOST_DOCKER_GID が空の時は sudo フォールバックメッセージを出してスキップする"
    - "entrypoint が出力する [INFO] ログに docker grp 合流結果が 1 行含まれる"
    - "Phase 5 の goku 冪等作成フロー (id goku / useradd / usermod) が壊れていない"
  artifacts:
    - path: "spirit-room/base/entrypoint.sh"
      provides: "docker グループ動的合流ロジック"
      contains: "SPIRIT_ROOM_DOCKER"
  key_links:
    - from: "spirit-room/base/entrypoint.sh"
      to: "docker group"
      via: "getent group $HOST_DOCKER_GID → groupadd or usermod"
      pattern: "HOST_DOCKER_GID"
---

<objective>
entrypoint.sh に docker グループ動的合流ロジックを追加する。

Purpose: `--docker` フラグで起動した部屋の中で goku ユーザーが `/var/run/docker.sock` を sudo なしで触れるようにする。ホストの docker GID はホストごとに異なるため、ランタイムで動的に解決する。

Output: `spirit-room/base/entrypoint.sh` の goku 冪等作成ブロック (line 38-60) の直後に `SPIRIT_ROOM_DOCKER=1` 分岐が追加された状態。
</objective>

<execution_context>
@/home/parallels/.claude/get-shit-done/workflows/execute-plan.md
@/home/parallels/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/ROADMAP.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: entrypoint.sh に docker グループ動的合流ロジックを追加</name>
  <files>spirit-room/base/entrypoint.sh</files>
  <read_first>
    - spirit-room/base/entrypoint.sh (現行全体 — goku 冪等作成ブロックの終端 line を確認)
    - .planning/phases/06-spirit-room-docker-docker-compose/06-CONTEXT.md §decisions D-04〜D-08, D-12 および §specifics entrypoint.sh 追加差分
    - .planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-CONTEXT.md §decisions D-16 (HOST_UID/GID の -e 受け渡し手法 — HOST_DOCKER_GID を同じパターンで追加)
  </read_first>
  <action>
`spirit-room/base/entrypoint.sh` の goku 冪等作成ブロック (`fi` で閉じる line 60) の直後、`# ── 認証ボリュームと /workspace を goku 所有に ──` (line 62) の直前に以下を挿入する。

**挿入内容:**

```bash
# ── docker グループ合流 (--docker モードのみ) ─────────────────
# D-04〜D-08: SPIRIT_ROOM_DOCKER=1 時に HOST_DOCKER_GID に対応する
# グループに goku を追加し、docker.sock を sudo なしで触れるようにする。
# goku 冪等作成ブロックの直後に実行すること (usermod の前に goku が必要)。
if [ "${SPIRIT_ROOM_DOCKER:-}" = "1" ]; then
    if [ -n "${HOST_DOCKER_GID:-}" ]; then
        # 既存グループ優先: GID が既にどこかのグループに使われている場合はその名前を使う
        # (D-07: groupadd -o で名前衝突させない。getent で既存グループを探す)
        _dgrp_name=$(getent group "$HOST_DOCKER_GID" 2>/dev/null | cut -d: -f1 || true)
        if [ -z "$_dgrp_name" ]; then
            # 既存グループがない → docker という名前で新規作成
            if groupadd -g "$HOST_DOCKER_GID" docker 2>/dev/null; then
                _dgrp_name=docker
            else
                echo "[WARN] docker グループ (gid=$HOST_DOCKER_GID) の作成に失敗 — sudo docker を使用せよ"
            fi
        fi
        if [ -n "$_dgrp_name" ]; then
            usermod -aG "$_dgrp_name" goku 2>/dev/null || true
            echo "[INFO] docker grp: goku ∈ ${_dgrp_name}(gid=${HOST_DOCKER_GID})"
        else
            echo "[INFO] docker grp: 合流失敗 — sudo docker を使用せよ (Phase 5 NOPASSWD 前提)"
        fi
    else
        # HOST_DOCKER_GID が空 = stat が失敗した環境 (Mac 等) または --docker 未指定時の誤起動
        echo "[INFO] docker grp: HOST_DOCKER_GID 未取得 — sudo docker を使用せよ"
    fi
fi
```

**挿入場所の確認 (before/after スニペット):**

挿入前:
```bash
    else
        echo "[INFO] goku ユーザーは既に存在 (UID/GID 一致 / skip)"
    fi
fi

# ── 認証ボリュームと /workspace を goku 所有に ───────────────
```

挿入後:
```bash
    else
        echo "[INFO] goku ユーザーは既に存在 (UID/GID 一致 / skip)"
    fi
fi

# ── docker グループ合流 (--docker モードのみ) ─────────────────
# D-04〜D-08: SPIRIT_ROOM_DOCKER=1 時に HOST_DOCKER_GID に対応する
# グループに goku を追加し、docker.sock を sudo なしで触れるようにする。
# goku 冪等作成ブロックの直後に実行すること (usermod の前に goku が必要)。
if [ "${SPIRIT_ROOM_DOCKER:-}" = "1" ]; then
    if [ -n "${HOST_DOCKER_GID:-}" ]; then
        _dgrp_name=$(getent group "$HOST_DOCKER_GID" 2>/dev/null | cut -d: -f1 || true)
        if [ -z "$_dgrp_name" ]; then
            if groupadd -g "$HOST_DOCKER_GID" docker 2>/dev/null; then
                _dgrp_name=docker
            else
                echo "[WARN] docker グループ (gid=$HOST_DOCKER_GID) の作成に失敗 — sudo docker を使用せよ"
            fi
        fi
        if [ -n "$_dgrp_name" ]; then
            usermod -aG "$_dgrp_name" goku 2>/dev/null || true
            echo "[INFO] docker grp: goku ∈ ${_dgrp_name}(gid=${HOST_DOCKER_GID})"
        else
            echo "[INFO] docker grp: 合流失敗 — sudo docker を使用せよ (Phase 5 NOPASSWD 前提)"
        fi
    else
        echo "[INFO] docker grp: HOST_DOCKER_GID 未取得 — sudo docker を使用せよ"
    fi
fi

# ── 認証ボリュームと /workspace を goku 所有に ───────────────
```

**禁止事項:**
- `groupadd -o` (名前重複を許可する方式) は使わない (D-07)
- `chmod 666 /var/run/docker.sock` は書かない (ホスト perms を変える行為)
- SPIRIT_ROOM_DOCKER 分岐の外で docker グループ操作をしない (opt-in 原則)
- 既存の goku 冪等作成ブロック (line 38-60) を変更しない
  </action>
  <verify>
    <automated>
      grep -n 'SPIRIT_ROOM_DOCKER' /home/parallels/workspaces/spirit-room-full/spirit-room/base/entrypoint.sh && \
      grep -n 'HOST_DOCKER_GID' /home/parallels/workspaces/spirit-room-full/spirit-room/base/entrypoint.sh && \
      grep -n 'docker grp' /home/parallels/workspaces/spirit-room-full/spirit-room/base/entrypoint.sh && \
      bash -n /home/parallels/workspaces/spirit-room-full/spirit-room/base/entrypoint.sh && \
      echo "OK: entrypoint.sh syntax valid"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SPIRIT_ROOM_DOCKER' spirit-room/base/entrypoint.sh` が 1 以上を返す
    - `grep -c 'HOST_DOCKER_GID' spirit-room/base/entrypoint.sh` が 3 以上を返す (受け取り・非空チェック・ログ出力)
    - `grep -c 'docker grp' spirit-room/base/entrypoint.sh` が 3 を返す (成功 / 失敗 / 未取得 の 3 パターン)
    - `grep -q 'getent group' spirit-room/base/entrypoint.sh` がヒットする (GID 衝突回避ロジックが存在)
    - `! grep -q 'chmod 666' spirit-room/base/entrypoint.sh` — sock perms 変更が入っていない
    - `! grep -q 'groupadd -o' spirit-room/base/entrypoint.sh` — groupadd -o が使われていない
    - `bash -n spirit-room/base/entrypoint.sh` が exit 0 (構文エラーなし)
    - 新しいブロックは goku 冪等作成の `fi` (line 60 付近) の直後、認証ボリューム chown の直前に位置する
  </acceptance_criteria>
  <done>entrypoint.sh に docker グループ合流ロジックが追加され、SPIRIT_ROOM_DOCKER=1 時のみ動作する分岐が存在する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → entrypoint (-e HOST_DOCKER_GID) | docker GID は整数値。entrypoint で getent/groupadd の引数になる |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | HOST_DOCKER_GID env var | mitigate | getent/groupadd の引数として使うだけ。シェルメタ文字を含む値でも getent/groupadd はシェル展開しないため injection リスクは低い。また SPIRIT_ROOM_DOCKER=1 の opt-in 経路でのみ実行 |
| T-06-02-02 | Elevation of Privilege | goku docker group membership | accept | docker.sock のマウント自体が --docker フラグ opt-in。sock アクセスはホスト root 相当であることをバナーで警告済み (D-19) |
</threat_model>

<verification>
`bash -n spirit-room/base/entrypoint.sh` が exit 0
`grep -c 'SPIRIT_ROOM_DOCKER' spirit-room/base/entrypoint.sh` が 1 以上
E2E (Plan 05) で `docker ps` が sudo なしで通ることを確認
</verification>

<success_criteria>
- entrypoint.sh に SPIRIT_ROOM_DOCKER=1 分岐が存在する
- HOST_DOCKER_GID の 3 パターン処理 (合流成功 / groupadd 失敗 / 空) が全てカバーされている
- 構文エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/06-spirit-room-docker-docker-compose/06-02-SUMMARY.md`
</output>
