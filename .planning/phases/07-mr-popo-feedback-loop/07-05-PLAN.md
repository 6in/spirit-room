---
phase: 07-mr-popo-feedback-loop
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-04-PLAN.md
files_modified:
  - spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md
  - spirit-room-manager/CLAUDE.md
  - spirit-room-manager/skills/MR_POPO.md
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
# Implements:
# - D-07 (Mr.ポポ本体は起動時に feedback を自動参照しない。人間明示起動のレビューコマンドで進める)
# - D-08 (レビュースキルの実装先は spirit-room-manager/skills/ — D-08 は planner 判断。spirit-room-manager は既に
#         skill 読み込みの仕組みがあるためここに置くのが自然。ルート `/mr-popo-review-feedback` スラッシュコマンド化は
#         別プロジェクト横断が必要なので本 phase では skill 側で完結させる)
# - D-09 (採用された feedback は .planning/mr-popo-memory/{mission_type}/applied/ に移動する)
user_setup: []

must_haves:
  truths:
    - "spirit-room-manager/skills/ に MR_POPO_REVIEW_FEEDBACK.md という新しいスキルファイルが存在する"
    - "spirit-room-manager/CLAUDE.md に『ユーザーが \"feedback をレビューしたい\" と言ったら MR_POPO_REVIEW_FEEDBACK.md を読め』という導線が追加されている"
    - "spirit-room-manager/skills/MR_POPO.md にもレビューコマンドの存在を示す 1-2 行の言及が追加されている (ヒアリング本体は変えない)"
    - "レビュースキルの手順は: ①.planning/mr-popo-memory/{type}/ 配下で pending 件数を集計 → ②ユーザーに mission_type を選ばせる → ③各 feedback を 1 件ずつ提示 → ④ユーザーが承認した diff を MISSION.md.template に反映 → ⑤採用済み feedback を applied/ に移動 → ⑥ヘッダの Review status を pending → applied に更新、の 6 ステップを明示している"
    - "各ステップで AskUserQuestion ツールを使うよう指示されている (自由記述プロンプトの乱発ではない)"
    - "既存の MR_POPO.md の Step 0〜Step 3 / K1〜K5 / 部屋起動手順は一切変更されていない"
    - "Mr.ポポが feedback を自動読み込みしないこと (D-07) が明記されている"
  artifacts:
    - path: "spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md"
      provides: "feedback レビューと template 更新の対話フロー"
      contains: "AskUserQuestion"
    - path: "spirit-room-manager/CLAUDE.md"
      provides: "レビュースキルへの導線 (明示トリガ)"
      contains: "MR_POPO_REVIEW_FEEDBACK"
    - path: "spirit-room-manager/skills/MR_POPO.md"
      provides: "レビューコマンドの存在通知 (ヒアリング本体は不変)"
      contains: "MR_POPO_REVIEW_FEEDBACK"
  key_links:
    - from: "ユーザーの明示トリガ (例: 『feedback レビューして』)"
      to: "spirit-room-manager/CLAUDE.md の導線"
      via: "トリガ語を検知して MR_POPO_REVIEW_FEEDBACK.md を Read"
      pattern: "MR_POPO_REVIEW_FEEDBACK"
    - from: "MR_POPO_REVIEW_FEEDBACK.md の Step 5 (移動ステップ)"
      to: ".planning/mr-popo-memory/{type}/applied/{date}-{slug}.md"
      via: "git mv + Review status 書き換え"
      pattern: "applied/"
    - from: "MR_POPO_REVIEW_FEEDBACK.md の Step 4 (承認された diff)"
      to: "spirit-room/base/scripts/MISSION.md.template"
      via: "Edit ツール経由の上書き"
      pattern: "MISSION.md.template"
---

<objective>
ユーザーが明示的に呼び出して使う feedback レビュースキル `spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md` を作成し、`.planning/mr-popo-memory/` に溜まった feedback を対話的にレビュー → 採用された差分を MISSION.md.template に反映 → 処理済みを `applied/` に移動する 6 ステップフローを実装する。併せて `spirit-room-manager/CLAUDE.md` にトリガ導線を追加し、`MR_POPO.md` 本体にもレビューコマンドの存在を一言明記する (ヒアリング本体は変更しない)。

Purpose: D-07 の原則 — Mr.ポポは起動時に feedback を自動読まない。読むのは人間明示トリガ時のみ。このスキルがその「明示レビューモード」の実装。D-09 の承認済み移動と組み合わせて、レビュー済みと未レビューを区別する。Phase 7 のループがここで閉じる。

Output: 3 ファイル変更。(1) 新規スキル MR_POPO_REVIEW_FEEDBACK.md、(2) spirit-room-manager/CLAUDE.md にトリガ記述、(3) MR_POPO.md 末尾にレビューコマンド存在の 1 段落追加。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-01-PLAN.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-04-PLAN.md
</context>

<interfaces>
<!-- 触るファイルの現行構造と改修の当たり所 -->

### spirit-room-manager/CLAUDE.md (現行)
- 「あなたはミスター・ポポです。」で始まり、「起動したら必ず `skills/MR_POPO.md` を読み、その手順に従って動いてください。」と続く
- 「使えるコマンド」節に spirit-room CLI 例が列挙されている
- ユーザーから最初のメッセージを受けた時の挨拶「よく来たな。ここは精神と時の部屋だ。」と Step 0 への誘導指示がある
- 末尾の `AskUserQuestion` 呼び出し雛形の後

改修: 末尾近く (「詳細手順は `skills/MR_POPO.md` を参照。」の直後) に "## feedback レビュー" 節を追加する。

### spirit-room-manager/skills/MR_POPO.md (現行)
- line 455 あたりに KAIO-MISSION.md の 報告フォーマット (line 449-463) がある
- line 464 が EOF

改修: ファイル末尾に新規節 "## feedback レビューコマンド" を 1 段落だけ追加する。Step 0〜3, K1〜K5, 起動手順など既存節は一切変更しない。

### リポジトリレイアウト前提 (重要)
- `spirit-room-manager/` と `spirit-room/` は **同一 git リポジトリ** (`spirit-room-full`) 内の兄弟ディレクトリ
- Mr.ポポが起動する場所は通常 `spirit-room-manager/` ディレクトリ内 (CLAUDE.md がそこに置かれているため)
- Mr.ポポ が MISSION.md.template を Edit するときはリポジトリ内のパス `spirit-room/base/scripts/MISSION.md.template` を操作する
- リポルートは実行時に `git rev-parse --show-toplevel` で確定できる (環境依存のハードコードパスは使わない)
- したがって Step R5 の commit 手順は:
  1. `REPO_ROOT=$(git rev-parse --show-toplevel)` でリポルートを取得
  2. `cd "$REPO_ROOT"` で移動してから git add / commit
  3. spirit-room-manager と spirit-room が同居しているので `git add spirit-room/base/scripts/MISSION.md.template` で正しく ステージできる

### Plan 07-04 が作る feedback ファイルのフォーマット (既知)
保存ファイル例 `/workspace/.planning/mr-popo-memory/poc/2026-04-23-spirit-room-langgraph-poc.md`:

```markdown
# Mr.ポポ feedback memory

- **Room:** spirit-room-langgraph-poc
- **Date:** 2026-04-23 15:42:10 JST
- **mission_type:** poc
- **completion_status:** completed
- **feedback_schema_version:** 1
- **Source:** /workspace/REPORT.md (at extraction time, room=spirit-room-langgraph-poc)
- **Review status:** pending

## Feedback YAML (抽出結果)

\`\`\`yaml
feedback_schema_version: 1
completion_status: completed
mission_type: poc
ambiguous_in_brief: |
  - ...
overspecified_in_brief: |
  - ...
missing_from_catalog: |
  - ...
completion_signal_mismatch: |
  - ...
suggested_template_diff: |
  - ...
---
\`\`\`
```

レビュースキルはこのフォーマットを前提に動く。

### ホスト側の `.planning/mr-popo-memory/` の置かれ方
- spirit-room open 時の `/workspace/` マウントは **ホストのプロジェクトフォルダ** → 部屋完了時に生成される `.planning/mr-popo-memory/` は **ホストのそのプロジェクトフォルダ配下** に残る
- Mr.ポポ (spirit-room-manager) は別フォルダ (`spirit-room-manager/`) で動くため、どのプロジェクトの feedback を見るかは **ユーザーがレビュー起動時に指定する** 必要がある
- ただし簡単な運用として、スキル内では「現在のカレント (`$(pwd)`) から `.planning/mr-popo-memory/` を探す / 見つからなければ ~/projects/*/.planning/mr-popo-memory/ を glob で列挙」のフォールバック戦略を取る

### MISSION.md.template の反映先
- Plan 07-01 で先頭に frontmatter が入った `spirit-room/base/scripts/MISSION.md.template`
- レビュー Step 4 で承認された diff は Edit ツール経由でこのファイルを直接書き換える
- Dockerfile 再ビルドは必須ではない (scripts/ は COPY されるだけなので次回 build 時に反映) ← ただし基本はユーザーが rebuild コマンドを叩く前提でスキルはそれを案内する
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MR_POPO_REVIEW_FEEDBACK.md スキルを新規作成</name>
  <files>spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md</files>
  <read_first>
    - spirit-room-manager/skills/MR_POPO.md (line 1-60 を参照し、既存スキルの書きぶり / AskUserQuestion の使い方 / 命令口調のトーンを把握)
    - .planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md §decisions D-07, D-08, D-09
    - .planning/phases/07-mr-popo-feedback-loop/07-04-PLAN.md (保存ファイルフォーマットとヘッダの Review status フィールド)
  </read_first>
  <action>
新規ファイル `spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md` を作成する。内容は以下をそのまま書き込む。

```markdown
# MR. POPO - feedback レビュースキル (明示起動のみ)

> D-07: このスキルは **人間が明示的にトリガしたときだけ** 読む。Mr.ポポ起動時に自動で読むことは禁止。
> このスキルが存在するのは、部屋の REPORT.md から抽出された feedback を Mr.ポポと対話しながら MISSION.md テンプレートに反映するため。

---

## トリガ

ユーザーが以下のいずれかを言ったら、このスキルを起動する:

- "feedback レビューして"
- "MISSION テンプレ更新したい"
- "溜まった feedback 見せて"
- "mr-popo-review-feedback"
- 類似の明示的リクエスト

それ以外の通常のヒアリング導線では、このスキルを自動で読み込まない。

---

## 責務

1. `.planning/mr-popo-memory/` に溜まった pending feedback を集計
2. mission_type 別に整理して提示
3. 1 件ずつユーザーにレビューさせ、採用する差分を決める
4. 採用された差分を `spirit-room/base/scripts/MISSION.md.template` に反映
5. 処理済み feedback を `applied/` に移動し、ヘッダの Review status を `pending → applied` に更新
6. 変更をコミット (ユーザー確認のうえ)

---

## リポジトリ前提 (Step R5 で使う)

このスキルは `spirit-room-manager/` (spirit-room-full リポジトリ内) で起動される。spirit-room-manager と spirit-room は **同一 git リポジトリの兄弟ディレクトリ** なので、commit 時のリポルートは実行時に解決する:

```bash
# リポルート自動解決 (ハードコードのパスは使わない)
REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null)
if [ -z "$REPO_ROOT" ]; then
    echo "[ERROR] spirit-room-full リポジトリ内で実行すること" >&2
    exit 1
fi
```

MISSION.md.template のパスは常に `$REPO_ROOT/spirit-room/base/scripts/MISSION.md.template`。Mr.ポポの Edit ツール呼び出し時もこのパスを組み立てて使う。

代替として、spirit-room-manager から見た **相対パス `../spirit-room/base/scripts/MISSION.md.template`** も兄弟配置前提で常に成立する。どちらで解決しても良いが、ハードコード絶対パス (`/path/to/spirit-room-full` 等) は使わない。

---

## 対話フロー (6 ステップ)

**全てのユーザー向け問いかけは `AskUserQuestion` ツールで行え。** フリーテキストの質問を並べるな。

### Step R1: 対象プロジェクト確定

ユーザーがどのプロジェクトの feedback をレビューしたいか確定する。カレントディレクトリに `.planning/mr-popo-memory/` があればそれをデフォルト候補にする。

1. `bash` で `find $HOME/projects -maxdepth 3 -type d -name "mr-popo-memory" 2>/dev/null` を実行し、候補を列挙
2. `AskUserQuestion` で「どのプロジェクトの feedback をレビューする?」と聞く。選択肢は上で見つかったパス (最大 5 件) + "パスを手入力" + "現在のディレクトリ" の混合

\`\`\`
AskUserQuestion(
  question: "どのプロジェクトの feedback をレビューする?",
  header: "レビュー対象",
  multiSelect: false,
  options: [
    { label: "~/projects/langgraph-poc/.planning/mr-popo-memory/", description: "見つかった候補 1" },
    { label: "~/projects/other-project/.planning/mr-popo-memory/", description: "見つかった候補 2" },
    { label: "現在のディレクトリ",     description: "$(pwd)/.planning/mr-popo-memory/" },
    { label: "パスを手入力 (自由記述)", description: "上記にないパスを直接指定" }
  ]
)
\`\`\`

確定したパスを `$MEMORY_DIR` とする。

### Step R2: pending feedback の集計

`$MEMORY_DIR` 配下を mission_type 別にスキャンする。applied/ サブディレクトリは除外する。

```bash
for type_dir in "$MEMORY_DIR"/*/; do
    type_name=$(basename "$type_dir")
    [ "$type_name" = "applied" ] && continue
    count=$(find "$type_dir" -maxdepth 1 -type f -name "*.md" 2>/dev/null | wc -l)
    echo "${type_name}: ${count} 件"
done
```

結果をユーザーに提示 (普通の echo で可。選択肢に多すぎる type がある場合のみ AskUserQuestion にする)。

### Step R3: レビューする mission_type を選ばせる

`AskUserQuestion` で 1 つ選ばせる。0 件の type は選択肢から除外する。

\`\`\`
AskUserQuestion(
  question: "どの mission_type の feedback からレビューする?",
  header: "mission_type 選択",
  multiSelect: false,
  options: [
    { label: "poc (3件)",         description: "POC 系の feedback 3件" },
    { label: "refactoring (2件)", description: "リファクタ系 2件" },
    { label: "すべてまとめて",      description: "mission_type をまたいで順に見る" },
    { label: "終了",              description: "今日は見ないで終わる" }
  ]
)
\`\`\`

### Step R4: 1 件ずつ feedback を提示してレビュー

選んだ mission_type のディレクトリ内のファイルを 1 件ずつ開き、内容を表示してからレビューを聞く。

```bash
for f in "$MEMORY_DIR/$selected_type/"*.md; do
    [ ! -f "$f" ] && continue
    cat "$f"   # ユーザーに中身を見せる
    # この後 AskUserQuestion で処理を選ばせる
done
```

各ファイルに対して `AskUserQuestion`:

\`\`\`
AskUserQuestion(
  question: "この feedback をどう扱う?",
  header: "feedback 処理",
  multiSelect: false,
  options: [
    {
      label: "採用 (diff をテンプレに反映)",
      description: "suggested_template_diff を MISSION.md.template に適用 → applied/ に移動"
    },
    {
      label: "部分採用 (編集してから反映)",
      description: "diff を人間が編集してから反映 → applied/ に移動"
    },
    {
      label: "却下 (採用しない)",
      description: "反映せずに applied/ に移動 (レビュー済みとして除外)"
    },
    {
      label: "保留 (次回に回す)",
      description: "何もせず次の feedback へ (Review status は pending のまま)"
    },
    {
      label: "このセッションを終える",
      description: "残りは次回レビュー"
    }
  ]
)
\`\`\`

### Step R5: 採用された diff をテンプレに反映

「採用」または「部分採用」が選ばれた場合のみ:

1. feedback ファイルの `suggested_template_diff` ブロックを取り出す (yq または awk で)
2. 「部分採用」の場合はユーザーに編集版を `AskUserQuestion` (自由記述) で入力させる
3. `$REPO_ROOT/spirit-room/base/scripts/MISSION.md.template` の該当箇所を Edit ツールで書き換える
   - 反映対象箇所の特定が曖昧な場合は、**Edit 実行前に必ず具体的な変更内容を提示してユーザーに承認させる** (AskUserQuestion: "この編集内容で反映してよいか? [yes / 編集する / キャンセル]")
   - `$REPO_ROOT` は `git rev-parse --show-toplevel` で解決する (ハードコードパス禁止)
4. 反映後、コミット:
   ```bash
   # リポルートを実行時解決 (ハードコードしない。spirit-room-manager と spirit-room は同居前提)
   REPO_ROOT=$(git rev-parse --show-toplevel)
   if [ -z "$REPO_ROOT" ]; then
       echo "[ERROR] git リポジトリ外で実行されている — commit スキップ" >&2
       # ここで処理終了させずに Step R6 へは進む (ファイル編集自体は成功しているため)
   else
       cd "$REPO_ROOT"
       git add spirit-room/base/scripts/MISSION.md.template
       git commit -m "feat(phase-07): apply feedback from {room-slug} to MISSION.md.template"
   fi
   ```

### Step R6: 処理済み feedback を applied/ に移動

「採用」「部分採用」「却下」のいずれかが選ばれた場合:

1. `$MEMORY_DIR/$selected_type/applied/` ディレクトリを mkdir -p で作成
2. feedback ファイルのヘッダ `- **Review status:** pending` を `- **Review status:** applied (YYYY-MM-DD)` に書き換え
   - 却下の場合は `- **Review status:** rejected (YYYY-MM-DD)`
3. `git mv` で `applied/` 配下に移動 (git 履歴を保つため `mv` ではなく `git mv`)
4. コミット:
   ```bash
   # ここもリポルートを実行時解決する (プロジェクトルートがハードコードされないように)
   PROJECT_ROOT=$(cd "$MEMORY_DIR" && git rev-parse --show-toplevel 2>/dev/null)
   if [ -n "$PROJECT_ROOT" ]; then
       cd "$PROJECT_ROOT"
       git add -A
       git commit -m "chore(mr-popo-memory): move reviewed feedback to applied/"
   fi
   ```

---

## ベースイメージ再ビルドの案内

MISSION.md.template を変更した場合、次回の部屋起動でテンプレが反映されるために rebuild が必要。レビューセッション終了時にユーザーに案内する:

> MISSION.md.template を更新した。次回の部屋起動でテンプレを反映するため、ベースイメージを再ビルドせよ:
>   REPO_ROOT=$(git rev-parse --show-toplevel) && cd "$REPO_ROOT" && ./spirit-room/build-base.sh

(rebuild を忘れると古いテンプレで部屋が立つ。必須手順として毎回案内。リポルートも `git rev-parse` で解決する)

---

## 終了時の報告フォーマット

```
feedback レビュー完了 (session summary):

  レビュー済み: 5 件
  採用       : 3 件 (→ MISSION.md.template に反映済)
  部分採用   : 1 件 (→ MISSION.md.template に反映済、diff は編集あり)
  却下       : 1 件
  保留       : 0 件

  変更ファイル: spirit-room/base/scripts/MISSION.md.template
  次回の部屋起動前に ./spirit-room/build-base.sh で rebuild を忘れるな。
```

---

## 禁止事項

- Mr.ポポの通常ヒアリング (`MR_POPO.md` の Step 0〜3 / K1〜K5) を自動で起動しない — このスキルは独立したフロー
- 過去の applied/ フォルダの feedback を再レビュー対象にしない
- MISSION.md.template 以外のテンプレ (KAIO-MISSION.md.template / catalog.md 等) は本 phase ではスコープ外。suggested_template_diff で言及があっても「本 phase では MISSION.md.template のみ」とユーザーに伝える
- ユーザーの明示承認なく Edit / git commit を実行しない
- feedback ファイルを削除しない。必ず applied/ に移動 (履歴を残す)
- **パスをハードコードしない** (`/path/to/spirit-room-full` のような絶対パスは禁止)。リポルートは常に `git rev-parse --show-toplevel` で解決すること
```

**禁止事項 (この Task 自身に対する禁止):**
- MR_POPO.md 本体のヒアリング手順を書き換えない (D-07: ヒアリング起動経路と feedback レビュー経路を混ぜない)
- レビューコマンドを `/mr-popo-review-feedback` スラッシュコマンド化しない (D-08 は planner 判断 → スキルファイルで完結させると決めた)
- 過去 feedback の自動ロード手順を書かない
- feedback ファイル削除の選択肢を増やさない (applied/ 移動で履歴保持)
- Step R5 / R6 / rebuild 案内で `/path/to/...` 形式のプレースホルダ絶対パスを **絶対に** 書かない
  </action>
  <verify>
    <automated>
test -f /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'D-07' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q '人間が明示的にトリガしたときだけ' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Step R1' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Step R2' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Step R3' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Step R4' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Step R5' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Step R6' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'AskUserQuestion' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'applied/' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'MISSION.md.template' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'build-base.sh' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'suggested_template_diff' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'Review status' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
grep -q 'git rev-parse --show-toplevel' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
! grep -q '/path/to/spirit-room-full' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md && \
echo "OK: MR_POPO_REVIEW_FEEDBACK.md 作成成功 (ハードコードパス除去済)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f spirit-room-manager/skills/MR_POPO_REVIEW_FEEDBACK.md` が成功
    - `grep -c 'Step R[1-6]' MR_POPO_REVIEW_FEEDBACK.md` が 6 (6 ステップ明示)
    - `grep -c 'AskUserQuestion' MR_POPO_REVIEW_FEEDBACK.md` が 4 以上 (R1 / R3 / R4 / R5 の承認プロンプト)
    - `grep -c 'applied/' MR_POPO_REVIEW_FEEDBACK.md` が 3 以上 (ディレクトリ設計言及 + 移動ステップ + 除外ロジック)
    - `grep -c 'MISSION.md.template' MR_POPO_REVIEW_FEEDBACK.md` が 4 以上 (目的 / Step R5 / 報告 / 禁止事項)
    - `grep -c 'suggested_template_diff' MR_POPO_REVIEW_FEEDBACK.md` が 2 以上
    - `grep -c 'build-base.sh' MR_POPO_REVIEW_FEEDBACK.md` が 2 以上 (rebuild 案内 + 終了報告)
    - `grep -c 'Review status' MR_POPO_REVIEW_FEEDBACK.md` が 3 以上 (pending / applied / rejected の全状態)
    - `grep -c 'rejected' MR_POPO_REVIEW_FEEDBACK.md` が 1 以上
    - `grep -c 'git mv' MR_POPO_REVIEW_FEEDBACK.md` が 1 (履歴保持)
    - `grep -c 'Mr.ポポ起動時に自動で読むことは禁止' MR_POPO_REVIEW_FEEDBACK.md` が 1 (D-07 遵守明示)
    - `grep -c 'KAIO-MISSION.md.template' MR_POPO_REVIEW_FEEDBACK.md` が 1 以上 (スコープ外として除外を明記)
    - `grep -c 'git rev-parse --show-toplevel' MR_POPO_REVIEW_FEEDBACK.md` が 3 以上 (リポ前提節 + Step R5 + R6 / rebuild 案内で複数箇所)
    - `grep -c '/path/to/spirit-room-full' MR_POPO_REVIEW_FEEDBACK.md` が 0 (ハードコード絶対パスが残っていない)
    - `grep -c '/path/to/' MR_POPO_REVIEW_FEEDBACK.md` が 0 (任意の `/path/to/*` プレースホルダも残っていない)
    - `grep -c 'REPO_ROOT=' MR_POPO_REVIEW_FEEDBACK.md` が 2 以上 (リポルート変数代入を Step R5 + リポ前提節で複数回)
  </acceptance_criteria>
  <done>MR_POPO_REVIEW_FEEDBACK.md が作成され、R1〜R6 の 6 ステップ対話フローと AskUserQuestion 使用指示 / applied/ 移動 / rebuild 案内 / D-07 遵守明記がすべて含まれ、**リポルートは `git rev-parse --show-toplevel` で実行時解決され、`/path/to/spirit-room-full` のようなハードコード絶対パスが一切含まれていない**</done>
</task>

<task type="auto">
  <name>Task 2: spirit-room-manager/CLAUDE.md にレビュースキル導線を追加し、MR_POPO.md 末尾にレビュー存在通知を追加</name>
  <files>
    - spirit-room-manager/CLAUDE.md
    - spirit-room-manager/skills/MR_POPO.md
  </files>
  <read_first>
    - spirit-room-manager/CLAUDE.md (全体を把握。特に末尾の AskUserQuestion 雛形のあと、ファイル終端までの構造)
    - spirit-room-manager/skills/MR_POPO.md (Plan 07-01 実装後の状態。末尾 line 460-464 付近の KAIO 報告フォーマットと EOF の位置)
  </read_first>
  <action>
2 つのファイルに導線を追加する。

**変更 1: spirit-room-manager/CLAUDE.md の末尾に「feedback レビュー」節を追加**

現行ファイル末尾 (最後の閉じタグ含む) の**下**に、以下の節を追記する。既存の内容は一切変更しない。

```markdown

---

## feedback レビュー (明示起動のみ)

ユーザーが以下のいずれかを発言したら、**通常のヒアリング導線 (Step 0) には進まず**、代わりに `skills/MR_POPO_REVIEW_FEEDBACK.md` を Read で読み込んで指示に従え:

- "feedback レビューして"
- "feedback を見せて"
- "MISSION テンプレ更新したい"
- "溜まった feedback 見せて"
- "mr-popo-review-feedback"
- 類似の明示的なレビュー依頼

`MR_POPO_REVIEW_FEEDBACK.md` は `.planning/mr-popo-memory/` に溜まった feedback を対話的にレビューし、採用された差分を `spirit-room/base/scripts/MISSION.md.template` に反映するスキル。**通常の部屋起動では一切使わない (D-07: Mr.ポポは起動時に feedback を自動参照しない)**。

レビュースキル起動後は、そのスキル内の手順が完了するまで Step 0〜3 や K1〜K5 のヒアリングには戻らない。
```

**変更 2: spirit-room-manager/skills/MR_POPO.md の末尾に「feedback レビューコマンドの存在」節を追加**

現行ファイル末尾 (KAIO 報告フォーマットの閉じ ``` の後、EOF の前) に以下の節を追記する。既存の Step / K / 起動手順節は一切変更しない。

```markdown

---

## feedback レビューコマンド (参考・別スキル)

Mr.ポポは通常のヒアリング本体では過去 feedback を自動で参照しない (D-07)。しかしユーザーが **明示的にレビューを依頼した場合のみ**、別スキル `MR_POPO_REVIEW_FEEDBACK.md` が呼ばれる。その流れは以下の通り (詳細はそのファイル参照):

1. `.planning/mr-popo-memory/{mission_type}/` に pending feedback が溜まっている
2. ユーザーが "feedback レビューして" 等を発言
3. `MR_POPO_REVIEW_FEEDBACK.md` が起動し、対話的に差分を確認
4. 承認された diff が `MISSION.md.template` に反映
5. 処理済み feedback は `applied/` に移動

**このヒアリング (MR_POPO.md) 側では過去 feedback を参照しない。** そうしないと Mr.ポポが暴走してテンプレと違う案内をし始めるリスクがあるため、意図的にヒアリングとレビューを分離している。
```

**禁止事項:**
- spirit-room-manager/CLAUDE.md の既存の「挨拶」「Step 0 誘導」「使えるコマンド一覧」「プロジェクトフォルダ」説明を削除・改変しない
- MR_POPO.md の既存の Step 0 / 0.5 (Plan 07-01 で追加) / Step 1〜3 / K1〜K5 / MISSION.md 生成ルール / 部屋起動手順 / KAIO 生成ルール / 報告フォーマット を削除・改変しない
- 追記する節の中に「起動時に自動でレビュースキルを読め」という指示を入れない (D-07 違反)
- MR_POPO.md 側に R1〜R6 の詳細手順をコピペしない (レビュースキル本体は MR_POPO_REVIEW_FEEDBACK.md に閉じる)
  </action>
  <verify>
    <automated>
grep -q '## feedback レビュー (明示起動のみ)' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/CLAUDE.md && \
grep -q 'MR_POPO_REVIEW_FEEDBACK.md' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/CLAUDE.md && \
grep -q '通常のヒアリング導線 (Step 0) には進まず' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/CLAUDE.md && \
grep -q 'feedback レビューして' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/CLAUDE.md && \
grep -q 'mr-popo-review-feedback' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/CLAUDE.md && \
grep -q '## feedback レビューコマンド (参考・別スキル)' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'D-07' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'MR_POPO_REVIEW_FEEDBACK.md' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'よく来たな。ここは精神と時の部屋だ。' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/CLAUDE.md && \
grep -q 'よく来たな。ここは精神と時の部屋だ。' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
echo "OK: CLAUDE.md + MR_POPO.md 導線追加成功"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## feedback レビュー (明示起動のみ)' spirit-room-manager/CLAUDE.md` が 1
    - `grep -c 'MR_POPO_REVIEW_FEEDBACK' spirit-room-manager/CLAUDE.md` が 1 以上
    - `grep -c '通常のヒアリング導線 (Step 0) には進まず' spirit-room-manager/CLAUDE.md` が 1
    - `grep -c 'feedback レビューして' spirit-room-manager/CLAUDE.md` が 1 (トリガ語の 1 つ)
    - `grep -c 'mr-popo-review-feedback' spirit-room-manager/CLAUDE.md` が 1 (トリガ語)
    - `grep -c 'よく来たな。ここは精神と時の部屋だ。' spirit-room-manager/CLAUDE.md` が 1 (既存の挨拶が消えていない)
    - `grep -c '使えるコマンド' spirit-room-manager/CLAUDE.md` が 1 (既存の使えるコマンド節が残っている)
    - `grep -c 'spirit-room open' spirit-room-manager/CLAUDE.md` が 1 以上 (既存 CLI 例が残っている)
    - `grep -c '## feedback レビューコマンド (参考・別スキル)' spirit-room-manager/skills/MR_POPO.md` が 1
    - `grep -c 'MR_POPO_REVIEW_FEEDBACK' spirit-room-manager/skills/MR_POPO.md` が 1 以上
    - `grep -c '意図的にヒアリングとレビューを分離' spirit-room-manager/skills/MR_POPO.md` が 1 (D-07 意図明記)
    - `grep -c 'Step 0' spirit-room-manager/skills/MR_POPO.md` が 3 以上 (Step 0 / Step 0.5 / Step 0.5 内記述 — 既存が保持されている)
    - `grep -c '## ヒアリング手順' spirit-room-manager/skills/MR_POPO.md` が 1 (既存構造保持)
    - `grep -c '界王星ヒアリング' spirit-room-manager/skills/MR_POPO.md` が 1 以上 (既存構造保持)
    - `grep -c '起動したら必ず' spirit-room-manager/CLAUDE.md` が 1 以上 (既存 skill 読み込み指示保持)
  </acceptance_criteria>
  <done>CLAUDE.md に明示トリガ語と MR_POPO_REVIEW_FEEDBACK.md への導線が追加され、MR_POPO.md 末尾にレビューコマンドの存在通知が 1 段落追加された状態。既存のヒアリング本体・挨拶・CLI コマンド例は一切改変されていない</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー発言 → Mr.ポポのスキル分岐 | "feedback レビューして" 等の文字列マッチで別スキルに入る |
| feedback YAML → MISSION.md.template への Edit 適用 | 信頼できない内容がプロダクトテンプレに入る経路 |
| applied/ 移動の git mv | 履歴と ACL の扱い |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | suggested_template_diff の内容が悪意あり | mitigate | Step R5 で「Edit 実行前に必ず具体的な変更内容を提示してユーザーに承認させる」と明示。AskUserQuestion で人間ゲート |
| T-07-05-02 | Elevation of Privilege | レビュースキルが通常起動時に自動で走る | mitigate | D-07 を複数箇所で明記 + 通常の MR_POPO.md 側でも「自動で読まない」を強調 |
| T-07-05-03 | Information Disclosure | feedback ファイル内の情報 | accept | すべてローカルプロジェクト内で閉じる。Mr.ポポは spirit-room-manager 側で動くが、実体の feedback は別プロジェクトフォルダ配下 |
| T-07-05-04 | Denial of Service | MISSION.md.template が連続反映で壊れる | mitigate | Step R5 で 1 件ずつ AskUserQuestion を挟む + Edit 承認 + git commit を都度実行。壊れたら git で戻せる |
| T-07-05-05 | Repudiation | 採用した feedback の履歴が消える | mitigate | `git mv` で applied/ に移動 (履歴保持)、Review status を書き換えて日付を残す |
</threat_model>

<verification>
- 3 ファイルの変更が完了し、新規 MR_POPO_REVIEW_FEEDBACK.md が存在
- 既存 MR_POPO.md の Step 0-3 / K1-K5 / 起動手順 / Plan 07-01 で追加された Step 0.5 / MISSION.md 生成テンプレ frontmatter がすべて保持されている (grep 検証)
- spirit-room-manager/CLAUDE.md の挨拶と使えるコマンド節が保持されている
- レビュースキルに R1-R6 の 6 ステップが全て含まれ、AskUserQuestion 使用指示が各ステップに散りばめられている
- 採用後の applied/ 移動と Review status 更新が明記されている
- rebuild 案内が終了時報告に含まれている
- `grep -c '/path/to/spirit-room-full' MR_POPO_REVIEW_FEEDBACK.md` が 0 (ハードコードパス除去)
- `grep -c 'git rev-parse --show-toplevel' MR_POPO_REVIEW_FEEDBACK.md` が 3 以上 (実行時解決が複数箇所で使われている)
</verification>

<success_criteria>
- Mr.ポポは通常ヒアリングでは feedback を参照しない (D-07 保持)
- ユーザー明示トリガでのみレビュースキルが起動する
- レビュースキルは 6 ステップ対話フローで pending → applied の状態遷移を管理
- 採用された diff は MISSION.md.template に Edit 経由で反映 (ユーザー承認必須)
- 処理済み feedback は applied/ に git mv で移動し履歴保持
- rebuild 忘れ防止の案内が必ず表示される
- **リポルートはスキル内の全コマンドで `git rev-parse --show-toplevel` によって実行時解決され、ハードコード絶対パスが存在しない**
</success_criteria>

<output>
After completion, create `.planning/phases/07-mr-popo-feedback-loop/07-05-SUMMARY.md`
</output>
