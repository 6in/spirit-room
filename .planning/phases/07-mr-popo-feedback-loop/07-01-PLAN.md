---
phase: 07-mr-popo-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/scripts/MISSION.md.template
  - spirit-room/base/scripts/KAIO-MISSION.md.template
  - spirit-room-manager/skills/MR_POPO.md
autonomous: true
requirements:
  - D-02
  - D-11
  - D-14
# Implements:
# - D-02 (mission_type フィールドを MISSION テンプレに導入 — feedback YAML の同名キーと対応)
# - D-11 (MISSION.md 内 max_iterations フィールド導入 — start-training.sh の上書きソース②)
# - D-14 (新設 planner 追加 decision): Mr.ポポ hiring workflow に mission_type 判定質問を追加し、
#         生成 MISSION.md の frontmatter に mission_type / max_iterations を書き込ませる
user_setup: []

must_haves:
  truths:
    - "MISSION.md.template と KAIO-MISSION.md.template の先頭に YAML frontmatter ブロックが存在し mission_type と max_iterations の 2 フィールドが定義されている"
    - "初期語彙は poc / refactoring / testdata / investigation の 4 種 (KAIO-MISSION.md.template は kaio を追加した 5 種でもよい)"
    - "MR_POPO.md の Step 0 モード選択直後に mission_type を AskUserQuestion で確認する質問が追加されている"
    - "MR_POPO.md の MISSION.md 生成ルール節で frontmatter (mission_type / max_iterations) を書き込むテンプレが明記されている"
    - "KAIO-MISSION.md.template も同様に mission_type を持ち、デフォルト max_iterations は 100 (界王星モードは長丁場) とする"
  artifacts:
    - path: "spirit-room/base/scripts/MISSION.md.template"
      provides: "frontmatter (mission_type / max_iterations) 付き MISSION.md 雛形"
      contains: "mission_type:"
    - path: "spirit-room/base/scripts/KAIO-MISSION.md.template"
      provides: "frontmatter 付き KAIO-MISSION.md 雛形"
      contains: "mission_type:"
    - path: "spirit-room-manager/skills/MR_POPO.md"
      provides: "mission_type ヒアリング + frontmatter 書き込み手順を含む Mr.ポポ skill"
      contains: "mission_type"
  key_links:
    - from: "spirit-room-manager/skills/MR_POPO.md (ヒアリング結果)"
      to: "生成された MISSION.md の frontmatter"
      via: "AskUserQuestion 回答を mission_type フィールドに転写"
      pattern: "mission_type:"
    - from: "spirit-room/base/scripts/MISSION.md.template frontmatter"
      to: "spirit-room/base/scripts/start-training.sh (Plan 07-03) の max_iterations 上書きソース②"
      via: "MISSION.md 内 max_iterations フィールドを yq/grep で取り出す"
      pattern: "max_iterations:"
---

<objective>
MISSION テンプレート 2 本 (`MISSION.md.template` / `KAIO-MISSION.md.template`) の先頭に YAML frontmatter ブロックを追加し、`mission_type` と `max_iterations` の 2 フィールドを標準化する。併せて Mr.ポポの hiring workflow (`spirit-room-manager/skills/MR_POPO.md`) に mission_type ヒアリング質問を追加し、生成する MISSION.md の先頭に必ず frontmatter を書き込ませる。

Purpose: mission_type は (D-04) feedback 蓄積ディレクトリの分類キー、(Plan 07-02) REPORT.md 内 YAML frontmatter の同名キー、(Plan 07-05) レビュー時の絞り込みキー、の 3 役をこなすため、テンプレとヒアリングの両面で最初に型を決めておく必要がある。max_iterations は MISSION.md 側に記述可能にしておくと (Plan 07-03) のガードで per-mission チューニングが効く。

Output: 3 ファイルすべてが更新済み。MISSION.md.template は frontmatter 5 行 + 既存本文、KAIO-MISSION.md.template は frontmatter 5 行 + 既存本文、MR_POPO.md は Step 0.5 (mission_type 質問) + MISSION.md 生成ブロックの frontmatter 書き込み指示が追加された状態。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/ROADMAP.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md
@/home/parallels/workspaces/spirit-room-full/CLAUDE.md
</context>

<interfaces>
<!-- 改修 3 ファイルそれぞれの現行構造のうち、Plan 07-01 が触る箇所のピンポイント抜粋。
     executor はこれを参照すれば余計なファイル探索不要。 -->

spirit-room/base/scripts/MISSION.md.template (現行 line 1-5):
```markdown
# MISSION: [フレームワーク名] POC

## 修行フェーズ
この部屋は 4 フェーズで修行が進む:
1. **PHASE 0 (RESEARCH)**: ...
```
改修後は line 1 から frontmatter を始め、既存の `# MISSION:` 見出しは frontmatter 閉じ `---` の直後に来る。

spirit-room/base/scripts/KAIO-MISSION.md.template (現行 line 1-5):
```markdown
# KAIO-MISSION: [プロジェクト名]

> 界王星モードの修行課題。...

## 修行スタイル
```
同じく frontmatter を line 1 に差し込む。

spirit-room-manager/skills/MR_POPO.md の構造:
- Step 0 (line 30-56): モード選択 AskUserQuestion (精神と時の部屋 / 界王星)
- Step 1 〜 Step 3 (line 59-168): 精神と時の部屋モード用ヒアリング
- MISSION.md 生成ルール節 (line 172-215): MISSION.md の出力テンプレ
- 界王星ヒアリング K1〜K5 (line 276-405)
- KAIO-MISSION.md 生成ルール節 (line 409-413)

改修ポイント (4 箇所):
1. Step 0 直後 (line 56 の後、Step 1 の前) に **Step 0.5: mission_type 選択** を挿入
2. MISSION.md 生成ルール節 (line 178-215 のテンプレ) の先頭に frontmatter を追加
3. 界王星ヒアリングの K1 直前に mission_type は kaio 固定である旨の注記を追加
4. KAIO-MISSION.md 生成ルール節 (line 409-413) に frontmatter 書き込み指示を追加
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MISSION.md.template / KAIO-MISSION.md.template に YAML frontmatter を追加</name>
  <files>
    - spirit-room/base/scripts/MISSION.md.template
    - spirit-room/base/scripts/KAIO-MISSION.md.template
  </files>
  <read_first>
    - spirit-room/base/scripts/MISSION.md.template (line 1-10 を確認し、先頭に frontmatter が未存在であることを確定)
    - spirit-room/base/scripts/KAIO-MISSION.md.template (line 1-10 を確認)
    - .planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md §decisions D-02 (初期スキーマ 6 フィールド) / §specifics (mission_type 初期語彙 poc/refactoring/testdata/investigation)
  </read_first>
  <action>
`MISSION.md.template` と `KAIO-MISSION.md.template` それぞれのファイル先頭 (line 1) に、以下のフォーマットで YAML frontmatter ブロックを **挿入** する (既存内容は削除せず、line 1 の前に `---` で始まる 5 行を差し込む)。

**変更 1: spirit-room/base/scripts/MISSION.md.template の先頭に以下を挿入**

```markdown
---
mission_type: poc           # poc / refactoring / testdata / investigation — Mr.ポポが Step 0.5 で確定する
max_iterations: 50          # TRAINING フェーズの安全網 (start-training.sh が読む)。長丁場なら上書きよ
feedback_schema_version: 1
---

```

frontmatter ブロックの後ろに空行 1 つを置き、その下に既存の `# MISSION: [フレームワーク名] POC` の見出しが続く状態にする。

**変更 2: spirit-room/base/scripts/KAIO-MISSION.md.template の先頭に以下を挿入**

```markdown
---
mission_type: kaio          # 界王星モードは常に kaio 固定 (将来 kaio-api / kaio-cli 等に細分化可能)
max_iterations: 100         # 界王星モードは GSD 段階開発で長丁場になるためデフォルト 2 倍
feedback_schema_version: 1
---

```

frontmatter ブロックの後ろに空行 1 つを置き、その下に既存の `# KAIO-MISSION: [プロジェクト名]` の見出しが続く状態にする。

**禁止事項:**
- 既存の本文 (4 フェーズ説明 / 目的 / 完了条件 等の見出しと内容) を削除・改変しない
- frontmatter に上記 3 フィールド以外を追加しない (feedback YAML 本体のスキーマは Plan 07-02 で REPORT.md 側に定義、MISSION.md 側は最小で留める)
- コメント行 (`#` で始まる YAML コメント) の日本語テキストを変えない — grep での検証に使う文字列を入れてある
- `feedback_schema_version` を削除しない — D-03 スキーマ変更時の非互換検知の根拠になる
  </action>
  <verify>
    <automated>
head -1 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/MISSION.md.template | grep -q '^---$' && \
head -5 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/MISSION.md.template | grep -q '^mission_type: poc' && \
head -5 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/MISSION.md.template | grep -q '^max_iterations: 50' && \
head -5 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/MISSION.md.template | grep -q '^feedback_schema_version: 1' && \
head -1 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/KAIO-MISSION.md.template | grep -q '^---$' && \
head -5 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/KAIO-MISSION.md.template | grep -q '^mission_type: kaio' && \
head -5 /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/KAIO-MISSION.md.template | grep -q '^max_iterations: 100' && \
grep -q '^# MISSION:' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/MISSION.md.template && \
grep -q '^# KAIO-MISSION:' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/KAIO-MISSION.md.template && \
echo "OK: frontmatter 挿入成功"
    </automated>
  </verify>
  <acceptance_criteria>
    - `MISSION.md.template` の line 1 が `---`
    - `MISSION.md.template` 内に `mission_type: poc` が 1 行存在する (デフォルト値として)
    - `MISSION.md.template` 内に `max_iterations: 50` が 1 行存在する
    - `MISSION.md.template` 内に `feedback_schema_version: 1` が 1 行存在する
    - `MISSION.md.template` の frontmatter ブロックを除いた本文に、既存見出し `# MISSION:` / `## 修行フェーズ` / `## 目的` / `## 完了条件（これを全て満たすまで繰り返せ）` / `## 実装スコープ` / `## 制約` / `## 繰り返しのルール` / `## 参考情報` がすべて残っている (`grep -c '^## '` が 6 以上 + `grep -c '^# MISSION:'` が 1)
    - `KAIO-MISSION.md.template` の line 1 が `---`
    - `KAIO-MISSION.md.template` 内に `mission_type: kaio` が 1 行存在する
    - `KAIO-MISSION.md.template` 内に `max_iterations: 100` が 1 行存在する
    - `KAIO-MISSION.md.template` 内に既存見出し `# KAIO-MISSION:` / `## 修行スタイル` / `## プロジェクトの目的` / `## 機能要件 (大まか)` / `## 成功条件 (テスト可能な形で)` / `## 制約` / `## 参考情報` / `## GSD への指示 (固定・変更不要)` がすべて残っている (`grep -c '^## '` が 6 以上)
  </acceptance_criteria>
  <done>MISSION.md.template と KAIO-MISSION.md.template の先頭に mission_type / max_iterations / feedback_schema_version を持つ YAML frontmatter が挿入され、既存本文はすべて保持されている</done>
</task>

<task type="auto">
  <name>Task 2: MR_POPO.md に mission_type ヒアリングと frontmatter 書き込み指示を追加</name>
  <files>spirit-room-manager/skills/MR_POPO.md</files>
  <read_first>
    - spirit-room-manager/skills/MR_POPO.md (Task 1 着手前でよい — 全体の節構造と各 Step の行番号を確認、特に line 30-56 Step 0, line 172-215 MISSION.md 生成ルール, line 276-343 界王星ヒアリング K1-K4, line 409-413 KAIO-MISSION.md 生成ルール節の位置)
    - .planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md §specifics (mission_type 初期語彙), §decisions D-07 (Mr.ポポ本体は feedback を自動読まない — ヒアリング時点では過去 feedback を参照しない)
  </read_first>
  <action>
`spirit-room-manager/skills/MR_POPO.md` に 4 箇所の追加変更を加える。既存のテキストは削除しない。

**変更 1: Step 0 の直後に "Step 0.5: mission_type の選択" を挿入**

現在の line 52-55 付近 (Step 0 の選択結果分岐の直後 `---` 区切りの手前) の「選択結果でモードを分岐する:」以降のブロックの直後に、以下を丸ごと挿入する。

```markdown

### Step 0.5: mission_type の選択 (精神と時の部屋モード選択後のみ)

Step 0 で **「精神と時の部屋」** を選んだ場合のみ、Step 1 に進む前に mission_type を **`AskUserQuestion`** で確定する。界王星を選んだ場合は固定で `kaio` とするのでスキップする。

```
AskUserQuestion(
  question: "どんな種類の修行だ? 生成する MISSION.md の種別を確定する",
  header: "mission_type",
  multiSelect: false,
  options: [
    {
      label: "poc",
      description: "フレームワーク POC。動く実装を最短で作る (デフォルト)"
    },
    {
      label: "refactoring",
      description: "既存コードのリファクタ修行。ダメコード → キレイコードへ"
    },
    {
      label: "testdata",
      description: "テストデータ / seed / fixture 生成。データ品質重視"
    },
    {
      label: "investigation",
      description: "調査・検証・ベンチマーク。コード成果物より知見が主"
    }
  ]
)
```

選択結果を `mission_type` 変数として記憶し、後の MISSION.md 生成時に frontmatter に書き込むこと。
```

**変更 2: `## MISSION.md 生成ルール` 節のテンプレ (line 178 付近の `# MISSION:` で始まる markdown コードブロック) の先頭に frontmatter を明示する**

line 178 付近の以下の箇所:

```markdown
ヒアリング完了後、以下のテンプレートに従って `/room/scripts/MISSION.md.template` を参考に生成する。

**完了条件は必ず具体的なコマンドで書くこと。**

\`\`\`markdown
# MISSION: [フレームワーク名] - [一言で何をするか]
```

のテンプレ冒頭に、以下のように frontmatter 節を追加する (既存行は残す):

```markdown
ヒアリング完了後、以下のテンプレートに従って `/room/scripts/MISSION.md.template` を参考に生成する。

**完了条件は必ず具体的なコマンドで書くこと。**

**先頭の YAML frontmatter は必須。** Step 0.5 で確定した `mission_type` と、デフォルト `max_iterations: 50` (難易度高いと判断したら 80-100 に調整してよい) を書き込む。

\`\`\`markdown
---
mission_type: {poc|refactoring|testdata|investigation}
max_iterations: 50
feedback_schema_version: 1
---

# MISSION: [フレームワーク名] - [一言で何をするか]
```

**変更 3: 界王星ヒアリング節 (`## 界王星ヒアリング` — line 276 付近) の直前もしくは直後に、mission_type は kaio 固定である旨の一文を追加**

line 276 付近の見出し `## 界王星ヒアリング (Step 0 で 界王星を選んだときのみ)` のすぐ下 (次の段落の前) に、以下の一文を挿入する。

```markdown

**mission_type の扱い:** 界王星モードは `mission_type: kaio` 固定。Step 0.5 の質問はスキップしてよい (Mr.ポポは自動で kaio を割り当てる)。将来サブタイプが必要になった時点でここに分岐を追加する。

```

**変更 4: `## KAIO-MISSION.md 生成ルール` 節 (line 409 付近) にも frontmatter 書き込み指示を追加**

line 409-413 付近の以下の箇所:

```markdown
## KAIO-MISSION.md 生成ルール

界王星ヒアリング完了後、`/room/scripts/KAIO-MISSION.md.template` のフォーマットに沿って `KAIO-MISSION.md` を生成する。GSD の `/gsd-new-project` がこのファイルを非対話で読んで答えにするので、**各項目は明確な一文で書く**。
```

のあとに、以下の一段落を追記する:

```markdown

**先頭の YAML frontmatter は必須。** `mission_type: kaio` と `max_iterations: 100` (GSD 段階開発は長丁場なのでデフォルト 2 倍) を書き込む:

\`\`\`yaml
---
mission_type: kaio
max_iterations: 100
feedback_schema_version: 1
---
\`\`\`

```

**禁止事項:**
- 既存の Step 1〜3 / K1〜K5 の AskUserQuestion を改変しない
- 最初の挨拶「よく来たな。ここは精神と時の部屋だ。」の一文を変えない
- 既存の部屋起動手順 (`spirit-room open` / `spirit-room kaio`) のコマンド例を変えない
- 胡蝶の夢モード (Step 3-c / K5-d) のヒアリングを削除・改変しない
- Mr.ポポに「過去 feedback を起動時に自動で参照せよ」と書かない (D-07 に反する — レビューは人間明示起動のみ)
  </action>
  <verify>
    <automated>
grep -q 'Step 0.5' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'header: "mission_type"' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'label: "poc"' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'label: "refactoring"' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'label: "testdata"' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'label: "investigation"' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'mission_type: {poc|refactoring|testdata|investigation}' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'mission_type: kaio' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'max_iterations: 100' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'feedback_schema_version: 1' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
grep -q 'よく来たな。ここは精神と時の部屋だ。' /home/parallels/workspaces/spirit-room-full/spirit-room-manager/skills/MR_POPO.md && \
echo "OK: MR_POPO.md 追記成功"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Step 0.5' spirit-room-manager/skills/MR_POPO.md` が 1 以上
    - `grep -c 'header: "mission_type"' spirit-room-manager/skills/MR_POPO.md` が 1
    - `grep -c 'label: "poc"' spirit-room-manager/skills/MR_POPO.md` が 1
    - `grep -c 'label: "refactoring"' spirit-room-manager/skills/MR_POPO.md` が 1
    - `grep -c 'label: "testdata"' spirit-room-manager/skills/MR_POPO.md` が 1
    - `grep -c 'label: "investigation"' spirit-room-manager/skills/MR_POPO.md` が 1
    - `grep -c 'mission_type: kaio' spirit-room-manager/skills/MR_POPO.md` が 1 以上 (界王星節の frontmatter サンプル)
    - `grep -c 'max_iterations: 100' spirit-room-manager/skills/MR_POPO.md` が 1 以上
    - `grep -c 'max_iterations: 50' spirit-room-manager/skills/MR_POPO.md` が 1 以上 (通常モードの frontmatter サンプル)
    - `grep -c 'feedback_schema_version: 1' spirit-room-manager/skills/MR_POPO.md` が 2 以上 (通常 + kaio)
    - `grep -c 'よく来たな。ここは精神と時の部屋だ。' spirit-room-manager/skills/MR_POPO.md` が 1 (挨拶は不変)
    - `grep -c 'AskUserQuestion' spirit-room-manager/skills/MR_POPO.md` が 10 以上 (Step 0 / 0.5 / 1a / 1b / 1c / 2 / 3a / 3b / 3c / K1a / K1b / K2 / K3 / K4 / K5a / K5b / K5c / K5d — 最低でも 10 は残る)
    - `grep -c '--kochou' spirit-room-manager/skills/MR_POPO.md` が 4 以上 (胡蝶の夢モード関連コマンド例が削除されていない)
    - `grep -c '過去 feedback を自動で参照' spirit-room-manager/skills/MR_POPO.md` が 0 (D-07 違反文字列が入っていない)
  </acceptance_criteria>
  <done>MR_POPO.md に Step 0.5 (mission_type 選択質問) が追加され、MISSION.md 生成ルールと KAIO-MISSION.md 生成ルールの両方に frontmatter 書き込み指示が明記されており、既存の挨拶・Step 1-3・K1-K5・胡蝶の夢フローはすべて保持されている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー回答 → MISSION.md frontmatter | `mission_type` は AskUserQuestion の固定選択肢から選ばれるため自由入力は来ない |
| MISSION.md frontmatter → start-training.sh (Plan 07-03) | max_iterations の値が数値パースされる経路 — 非数値は Plan 07-03 のガードで弾く |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | mission_type 値 | mitigate | AskUserQuestion は固定 4 択 (poc/refactoring/testdata/investigation) + Other なし。Mr.ポポが手書きで誤字を入れる余地は `mission_type: refactorng` 等の typo のみ。Plan 07-04 の抽出側で小文字正規化と既知値フォールバック (不明なら "unknown" ディレクトリ) を実装する前提で accept 相当に低減 |
| T-07-01-02 | Information Disclosure | MISSION.md の frontmatter | accept | mission_type と max_iterations は非機微。credentials や API key を書かせる仕様ではない |
| T-07-01-03 | Spoofing | KAIO-MISSION の mission_type | accept | 界王星モードは常に固定 `kaio`。ユーザーが書き換えても Plan 07-05 のレビュースキル側で mission_type を再確認するため運用影響は限定的 |
</threat_model>

<verification>
- `MISSION.md.template` / `KAIO-MISSION.md.template` の line 1 が `---` で始まる
- 2 テンプレ合計で `mission_type:` が 2 箇所 (各 1)、`max_iterations:` が 2 箇所、`feedback_schema_version: 1` が 2 箇所
- `MR_POPO.md` に Step 0.5 節が追加され、mission_type の 4 択が存在
- `MR_POPO.md` の MISSION.md 生成テンプレと KAIO-MISSION.md 生成テンプレの両方に frontmatter サンプルが含まれる
- 既存の挨拶 / Step 1-3 / K1-K5 / 胡蝶の夢フローは削除・改変されていない
</verification>

<success_criteria>
- 2 本の template の先頭に frontmatter (mission_type / max_iterations / feedback_schema_version) が存在する
- MR_POPO.md が mission_type を AskUserQuestion で聞き、MISSION.md の先頭に frontmatter を書き込む手順になっている
- 界王星モードの mission_type は `kaio` 固定で、hiring workflow では質問をスキップすることが明記されている
- `grep -c '^## '` の数と既存見出しの grep で既存本文の欠損が検知されない
</success_criteria>

<output>
After completion, create `.planning/phases/07-mr-popo-feedback-loop/07-01-SUMMARY.md`
</output>
