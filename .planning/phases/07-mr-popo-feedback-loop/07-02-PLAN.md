---
phase: 07-mr-popo-feedback-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/scripts/create-report.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-13
# Implements:
# - D-01 (MISSION_TEMPLATE_FEEDBACK は YAML frontmatter で REPORT.md に埋め込む。markdown bullet / JSON block は採用しない)
# - D-02 (6 フィールド初期スキーマ: mission_type / ambiguous_in_brief / overspecified_in_brief / missing_from_catalog / completion_signal_mismatch / suggested_template_diff)
# - D-03 (feedback_schema_version: 1 を埋め込む)
# - D-13 (部分 REPORT.md も同スキーマ + completion_status フィールド。後続 Plan 07-03 の MAX_ITERATIONS ガードが呼ぶプロンプトと合わせる)
user_setup: []

must_haves:
  truths:
    - "create-report.md の指示に従って生成される REPORT.md は、先頭に YAML frontmatter ブロックを持つ"
    - "frontmatter に 6 つの必須フィールド (mission_type / ambiguous_in_brief / overspecified_in_brief / missing_from_catalog / completion_signal_mismatch / suggested_template_diff) が必ず出力される"
    - "frontmatter に feedback_schema_version: 1 と completion_status (completed|interrupted|failed) が含まれる"
    - "frontmatter 配置は REPORT.md の先頭 (line 1 から ---) に固定する"
    - "suggested_template_diff は YAML の block scalar (|) で自由記述 (unified diff or plain text) を許容する"
    - "既存の 界王星モード / 精神と時の部屋モード 分岐と各セクション (サマリ / タイムライン / 技術的な判断 / ハマりポイント / コード品質 / 改善点 / 総評) はすべて保持される"
  artifacts:
    - path: "spirit-room/base/scripts/create-report.md"
      provides: "REPORT.md 生成インストラクション (frontmatter 必須化)"
      contains: "MISSION_TEMPLATE_FEEDBACK"
  key_links:
    - from: "spirit-room/base/scripts/create-report.md (生成 agent のプロンプト)"
      to: "生成される /workspace/REPORT.md 先頭の YAML frontmatter"
      via: "プロンプト内の明示テンプレと 'frontmatter は必須' 指示"
      pattern: "feedback_schema_version"
    - from: "REPORT.md の frontmatter"
      to: "Plan 07-04 の yq 抽出 → .planning/mr-popo-memory/{mission_type}/ への保存"
      via: "mission_type キーで保存先ディレクトリが決まる"
      pattern: "mission_type:"
---

<objective>
REPORT.md 生成用インストラクション `spirit-room/base/scripts/create-report.md` を改修し、生成される REPORT.md の**先頭**に構造化された YAML frontmatter ブロックを必ず書き出させる。この frontmatter には D-02 の 6 フィールド (mission_type / ambiguous_in_brief / overspecified_in_brief / missing_from_catalog / completion_signal_mismatch / suggested_template_diff) に加え、D-03 のスキーマバージョンと D-13 の completion_status を含める。

Purpose: これが Phase 7 feedback loop の**情報源の入口**。ここで YAML が吐かれなければ Plan 07-04 の自動抽出も Plan 07-05 のレビューも動かない。agent がサボって本文だけ書く／bullet 形式で書く等の逸脱を防ぐため、プロンプトにはフォーマット厳守・例示・検証サンプルをすべて含める。

Output: `create-report.md` が 2 層構成 (①先頭の YAML frontmatter 必須指示 + 具体例、②既存の REPORT.md 本文 Markdown 節) になり、executor 側は既存の生成プロセスを壊すことなく frontmatter を吐くようになる。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md
@/home/parallels/workspaces/spirit-room-full/CLAUDE.md
</context>

<interfaces>
<!-- create-report.md の現行構造 (line 番号は改修前時点) -->

- line 1: イントロ文 "Create a retrospective report..."
- line 3-4: `## Steps` 見出し + **1. Detect project type**
- line 18-26: **2. Infer the topic** (ARGUMENTS ヒントと fallback)
- line 28-49: **3. Research autonomously** (界王星 / 精神と時の部屋 モード別の読むべきファイル列挙)
- line 51: "Goal: answer ..."
- line 53-118: **4. Write REPORT.md** — 本文 Markdown テンプレ (サマリ / 成果 / タイムライン / 技術的な判断 / ハマりポイント / コード品質 / 改善点・次のステップ / 総評)
- line 120: 日本語指示 "日本語で書くこと。..."
- line 122-126: **5. Commit**
- line 128: レポート: ... 終端

改修対象: **step 4 の "Write REPORT.md" の冒頭** に frontmatter 指示を差し込み、既存の markdown テンプレより先に YAML frontmatter を書くよう明示する。step 1/2/3/5 は一切変更しない。

参考: 生成される REPORT.md の期待フォーマット例
```
---
feedback_schema_version: 1
completion_status: completed  # completed | interrupted | failed
mission_type: poc             # MISSION.md の frontmatter と一致させる
ambiguous_in_brief: |
  - 完了条件「動けばいい」は曖昧。エラーハンドリングも含むか不明
  - RESEARCH 観点「API の癖」の粒度が指示書から読み取れなかった
overspecified_in_brief: |
  - 「必ず Python 3 で書け」は POC 目的に対して固すぎ。Node でも OK だった
missing_from_catalog: |
  - LangGraph の streaming API (astream_events) の使い方が catalog.md にない
completion_signal_mismatch: |
  - MISSION は .done で終わると書いてあるが実装では .done 作成後に cleanup が走っていない
suggested_template_diff: |
  - Step 2 の「完了条件」オプションに "REST API なら curl で確認" を追加
  - RESEARCH 観点に「streaming 対応」を追加
---

# REPORT: [プロジェクト名 / ミッション名]
...既存の本文テンプレ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: create-report.md の Step 4 先頭に MISSION_TEMPLATE_FEEDBACK frontmatter 指示を挿入</name>
  <files>spirit-room/base/scripts/create-report.md</files>
  <read_first>
    - spirit-room/base/scripts/create-report.md (全体。特に line 53-120 の "Write REPORT.md" 節の構造と、既存 markdown テンプレの見出しを把握)
    - .planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md §decisions D-01, D-02, D-03, D-13 (YAML frontmatter / 6 フィールド / schema version / completion_status)
    - .planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md §specifics (mission_type 初期語彙)
  </read_first>
  <action>
`spirit-room/base/scripts/create-report.md` の **step 4 "Write REPORT.md"** 節の先頭 (現行 line 53 `**4. Write REPORT.md**` の直後、既存の markdown コードブロック `` ```markdown `` より前) に、新しいサブ節 **"4a. Frontmatter (必須)"** を挿入し、既存 markdown テンプレのセクション見出しを "4b. Body (既存セクション)" にリネームする。

**変更 1: line 53 `**4. Write REPORT.md**` の直後に以下を挿入**

```markdown
**4. Write REPORT.md**

REPORT.md は **2 層構成** で書く。(4a) 先頭 YAML frontmatter (機械読み取り用) → (4b) 本文 Markdown (人間読み用)。

### 4a. Frontmatter (必須・ファイル先頭に固定)

`/workspace/REPORT.md` の **line 1** から `---` で始まる YAML frontmatter ブロックを必ず書き出す。次の Phase 7 feedback loop (Mr.ポポが過去知見を蓄積するパイプライン) がこのブロックを `yq` で機械抽出するため、フォーマット逸脱は許されない。**bullet 形式や JSON ブロックは禁止。必ず YAML frontmatter**。

#### 必須フィールド (計 8 つ)

| フィールド | 型 | 意味 |
|-----------|-----|------|
| `feedback_schema_version` | int | 固定で `1` (D-03 スキーマ変更時に上げる) |
| `completion_status` | string | `completed` / `interrupted` / `failed` のいずれか。`.done` が存在すれば completed、`.interrupted` なら interrupted、それ以外は failed |
| `mission_type` | string | MISSION.md / KAIO-MISSION.md の frontmatter と同じ値 (`poc` / `refactoring` / `testdata` / `investigation` / `kaio`)。読み取って転写する |
| `ambiguous_in_brief` | string (YAML block scalar `\|`) | MISSION.md で曖昧だった項目を箇条書きで。なければ空文字または `"(なし)"` |
| `overspecified_in_brief` | string (block scalar) | MISSION.md で過剰指定だった項目。なければ `"(なし)"` |
| `missing_from_catalog` | string (block scalar) | catalog.md に載っていなかったが必要だったツール・API。なければ `"(なし)"` |
| `completion_signal_mismatch` | string (block scalar) | 完了判定 (`.done` / test exit 等) と実装の乖離。なければ `"(なし)"` |
| `suggested_template_diff` | string (block scalar) | MISSION.md.template / catalog.md への具体改善提案。unified diff でも自由記述でも可。**必ず書く** (このフィールドが Mr.ポポのテンプレ更新の原資なので「なし」は極力避ける) |

#### 情報源の拾い方

- `mission_type`: `/workspace/MISSION.md` または `/workspace/KAIO-MISSION.md` の先頭 frontmatter を読んで取る。見つからなければ `unknown`
- `completion_status`: `/workspace/.done` があれば `completed`、`/workspace/.interrupted` があれば `interrupted`、どちらもなく修行途中なら `failed`
- `ambiguous_in_brief` / `overspecified_in_brief` / `missing_from_catalog`: `.journal.md` の `[STUCK]` / `[PIVOT]` エントリと git log の試行錯誤から抽出
- `completion_signal_mismatch`: MISSION.md の「完了条件」と実装ファイル構成を突き合わせて乖離を書く
- `suggested_template_diff`: 上記 4 項目を踏まえて Mr.ポポに送る「次回こうしろ」の差分

#### フォーマット例 (これを忠実に踏襲すること)

\`\`\`yaml
---
feedback_schema_version: 1
completion_status: completed
mission_type: poc
ambiguous_in_brief: |
  - 完了条件「動けばいい」は曖昧 — エラーハンドリング要否が読み取れない
  - RESEARCH 観点「API の癖」の粒度指示がない
overspecified_in_brief: |
  - (なし)
missing_from_catalog: |
  - LangGraph の astream_events API が catalog.md に載っていなかった
completion_signal_mismatch: |
  - MISSION は .done だけで終わる想定だが、実装では stream cleanup が漏れた
suggested_template_diff: |
  - Step 2 完了条件オプションに "REST API なら curl 200 確認" を追加
  - RESEARCH 観点に "streaming 対応" 選択肢を追加
---
\`\`\`

**注意点:**
- block scalar `|` の後ろには **スペースで改行しない** — 直後に改行し、内容はインデント付きで書く
- 値が存在しない場合は空にせず `"(なし)"` 文字列を入れる (yq 抽出時に空行判定を回避)
- `completion_status: interrupted` の場合は本文側 (4b) でも未完了箇所を明示する
- **frontmatter の末尾 `---` の直後に空行を 1 つ**置いてから本文 Markdown に入る

### 4b. Body (既存の Markdown テンプレ)

frontmatter のあとに、これまで通り人間読み用の Markdown 本文を書く。フォーマットは以下のまま:

```

**変更 2: 既存の `` ```markdown ... ``` `` テンプレブロック (現行 line 55-118) はそのまま保持する**

line 55 の `` ```markdown `` から line 118 の 閉じ ` ``` ` までは一切変更しない。変更 1 で挿入した新規節 "### 4b. Body (既存の Markdown テンプレ)" の直後にそのまま続く形。

**変更 3: line 120 "日本語で書くこと。..." の一文の前に補足追加**

line 120 付近の:

```
日本語で書くこと。簡潔に、しかし具体的に。推測より事実を優先。
```

を以下に変更:

```
日本語で書くこと。簡潔に、しかし具体的に。推測より事実を優先。

**最重要:** frontmatter ブロック (4a) は**機械読み取り用**なのでフォーマット厳守。yq/jq で解析されるため、バッククォート・行頭以外のコロン・typo は許されない。本文 (4b) は人間読み用なので自由度あり。
```

**禁止事項:**
- step 1 / 2 / 3 / 5 の既存内容を改変しない (検出ロジック / 情報源読み込み / commit 手順はそのまま)
- 既存の界王星 / 精神と時の部屋モード分岐の読み込みファイル一覧を削除しない
- 既存の本文 Markdown テンプレ (サマリ / 成果 / タイムライン / 技術的な判断 / ハマりポイント / コード品質 / 改善点 / 総評) の見出しを削除・改変しない
- frontmatter 例の中で 6 フィールド未満にしない (D-02 で 6 フィールド固定、schema_version と completion_status を足して 8)
- `suggested_template_diff` を optional と書かない (D-02 で必須指定、ここで optional にすると Plan 07-05 のレビューが空振りする)
  </action>
  <verify>
    <automated>
grep -q '4a. Frontmatter' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q '4b. Body' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'feedback_schema_version: 1' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'completion_status' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'mission_type' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'ambiguous_in_brief' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'overspecified_in_brief' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'missing_from_catalog' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'completion_signal_mismatch' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'suggested_template_diff' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q '## サマリ' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q '## タイムライン' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q '## 技術的な判断' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q '## ハマりポイント' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q '## 改善点・次のステップ' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
grep -q 'git commit -m "docs: add retrospective report' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/create-report.md && \
echo "OK: create-report.md 改修成功"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '4a. Frontmatter' create-report.md` が 1 以上
    - `grep -c '4b. Body' create-report.md` が 1 以上
    - `grep -c 'feedback_schema_version: 1' create-report.md` が 1 以上 (フォーマット例内で明示)
    - `grep -c 'completion_status' create-report.md` が 2 以上 (説明 + 例の計 2 つ)
    - `grep -c 'mission_type' create-report.md` が 2 以上
    - `grep -c 'ambiguous_in_brief' create-report.md` が 2 以上 (表 + 例)
    - `grep -c 'overspecified_in_brief' create-report.md` が 2 以上
    - `grep -c 'missing_from_catalog' create-report.md` が 2 以上
    - `grep -c 'completion_signal_mismatch' create-report.md` が 2 以上
    - `grep -c 'suggested_template_diff' create-report.md` が 2 以上
    - `grep -c '## サマリ' create-report.md` が 1 以上 (既存本文テンプレが保持)
    - `grep -c '## タイムライン' create-report.md` が 1 以上
    - `grep -c '## 技術的な判断' create-report.md` が 1 以上
    - `grep -c '## ハマりポイント' create-report.md` が 1 以上
    - `grep -c '## コード品質' create-report.md` が 1 以上
    - `grep -c '## 改善点・次のステップ' create-report.md` が 1 以上
    - `grep -c '## 総評' create-report.md` が 1 以上
    - `grep -c 'git commit -m "docs: add retrospective report' create-report.md` が 1 (既存 step 5 の commit 指示が残っている)
    - `grep -c '1. Detect project type' create-report.md` が 1 (step 1 保持)
    - `grep -c '2. Infer the topic' create-report.md` が 1 (step 2 保持)
    - `grep -c '3. Research autonomously' create-report.md` が 1 (step 3 保持)
    - `grep -c '界王星' create-report.md` が 3 以上 (モード分岐文言保持)
    - `grep -c '精神と時の部屋' create-report.md` が 2 以上
  </acceptance_criteria>
  <done>create-report.md の step 4 が (4a) frontmatter + (4b) body の 2 層構成になり、frontmatter に 8 フィールド (schema_version / completion_status + D-02 の 6 フィールド) の指示が明示され、既存 step 1-3/5 と本文テンプレは保持されている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| create-report.md (プロンプト指示) → 生成される /workspace/REPORT.md | agent が指示を逸脱した場合に frontmatter が欠落するリスク |
| /workspace/REPORT.md → yq (Plan 07-04) | 下流の YAML パース段階での injection リスク |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | 生成された frontmatter フォーマット | mitigate | "フォーマット厳守" と明示 + 具体例を 1 件提示。Plan 07-04 側で yq パース失敗を検知したらログ警告 + スキップ (落とさない) |
| T-07-02-02 | Information Disclosure | suggested_template_diff に機密混入 | accept | feedback は .planning/ 配下ローカル保存のみ。外部送信経路なし。ユーザーが明示的に commit した場合のみ git に乗る |
| T-07-02-03 | Denial of Service | agent が frontmatter を書かず本文のみ出力 | mitigate | プロンプトで "機械読み取り用なので厳守" と明示。Plan 07-04 の抽出側で欠落時は unknown ディレクトリ + 警告ログ。ループは止めない |
</threat_model>

<verification>
- create-report.md 内に 4a/4b 節が存在
- 8 必須フィールド (schema_version / completion_status / mission_type + D-02 の 6 フィールド のうち mission_type は共有、計 8) すべての名前が grep で検出可能
- 既存 step 1-3 と step 5、本文テンプレ見出しは保持
- 界王星 / 精神と時の部屋モード分岐の指示が残っている
</verification>

<success_criteria>
- create-report.md に従って生成される REPORT.md は先頭に YAML frontmatter を持つ (本 Plan では指示書改修のみ、実 run 検証は Plan 07-04 の E2E で実施)
- 8 フィールドのキー名が仕様通り (タイポなし)
- 既存の 4 モード分岐 / 本文 8 セクションが保持
- `suggested_template_diff` が必須 (required) として明示
</success_criteria>

<output>
After completion, create `.planning/phases/07-mr-popo-feedback-loop/07-02-SUMMARY.md`
</output>
