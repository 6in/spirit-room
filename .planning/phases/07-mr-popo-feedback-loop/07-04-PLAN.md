---
phase: 07-mr-popo-feedback-loop
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01-PLAN.md
  - 07-02-PLAN.md
  - 07-03-PLAN.md
files_modified:
  - spirit-room/base/scripts/start-training.sh
  - spirit-room/base/scripts/extract-feedback.sh
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
# Implements:
# - D-04 (保存先は .planning/mr-popo-memory/{mission_type}/{date}-{slug}.md 形式)
# - D-05 (自動蓄積 — 部屋完了時 start-training.sh が走らせる)
# - D-06 (1 ファイル = 1 部屋分の feedback。room 名 + 日時 + feedback YAML 抜粋をヘッダに持つ)
user_setup: []

must_haves:
  truths:
    - "REPORT.md の生成完了直後 (.reported フラグ作成後) に start-training.sh が extract-feedback.sh を実行する"
    - "extract-feedback.sh が /workspace/REPORT.md の YAML frontmatter を yq で抽出する"
    - "yq パース失敗時はエラーログを吐くが部屋の終了フロー自体は止めない (safe failure)"
    - "抽出した frontmatter は /workspace/.planning/mr-popo-memory/{mission_type}/{YYYY-MM-DD}-{room-slug}.md に書き出される"
    - "mission_type が空や未知の場合は unknown/ ディレクトリに保存される"
    - "保存ファイルの先頭に room name / 日時 / mission_type / completion_status / source REPORT.md のパスを含むヘッダが付く"
    - "保存ファイル本体に YAML frontmatter がそのままコピーされる (後工程 Plan 07-05 の yq 再抽出で使える)"
    - "同名ファイルが既に存在する場合は上書きせず -NN サフィックスを付ける"
    - "bash -n extract-feedback.sh が exit 0"
  artifacts:
    - path: "spirit-room/base/scripts/extract-feedback.sh"
      provides: "REPORT.md の YAML frontmatter を抽出し mr-popo-memory に保存するスクリプト"
      contains: "mr-popo-memory"
    - path: "spirit-room/base/scripts/start-training.sh"
      provides: "PHASE 3 完了後に extract-feedback.sh を呼び出すフック"
      contains: "extract-feedback.sh"
  key_links:
    - from: "start-training.sh の PHASE 3 ループ直後 (.reported フラグ作成後)"
      to: "extract-feedback.sh (同ディレクトリにあるスクリプト)"
      via: "直接 /room/scripts/extract-feedback.sh 実行"
      pattern: "extract-feedback.sh"
    - from: "/workspace/REPORT.md の YAML frontmatter"
      to: "/workspace/.planning/mr-popo-memory/{mission_type}/{date}-{slug}.md"
      via: "yq '.mission_type' で保存先を決定、sed で frontmatter ブロック抜き出し → ヘッダ付加してファイル書き出し"
      pattern: "mission_type"
---

<objective>
部屋完了時 (PHASE 3 REPORT フェーズ終了後) に、生成された `/workspace/REPORT.md` の YAML frontmatter を機械抽出し、`/workspace/.planning/mr-popo-memory/{mission_type}/{YYYY-MM-DD}-{room-slug}.md` に自動保存する仕組みを実装する。

Purpose: D-05 の「自動蓄積」を実現する配管。Mr.ポポが起動時に自動で feedback を読まない (D-07 の暴走防止) ため、ここで単に「溜める」だけ。溜まった feedback をいつ/誰が見るかは Plan 07-05 の人間明示レビュー。この Plan は配管の堅牢性 (失敗しても部屋は終わる / 破損 YAML をスキップ / 同名衝突は上書きせず) に責任を持つ。

Output: (1) 新規スクリプト `spirit-room/base/scripts/extract-feedback.sh` が `$ROOM_NAME` を env から受け取り yq で抽出して保存、(2) `start-training.sh` の PHASE 3 ループ直後の完了バナー直前でこのスクリプトを呼ぶフック、の 2 点が実装された状態。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-01-PLAN.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-02-PLAN.md
@/home/parallels/workspaces/spirit-room-full/.planning/phases/07-mr-popo-feedback-loop/07-03-PLAN.md
</context>

<interfaces>
<!-- 改修対象と参照する既存実装のインタフェース -->

### start-training.sh の現行構造 (Plan 07-03 実装後の想定)
- line 262-326: PHASE 3 (REPORT) ループ。終わると `phase_commit "docs: add REPORT.md"` (line 326)
- line 328-332: 完了バナー (`修行完了！部屋から出よ`)

改修対象: line 326 の `phase_commit "docs: add REPORT.md"` と line 328 の完了バナーの**間**に `/room/scripts/extract-feedback.sh` の呼び出しを入れる。

### extract-feedback.sh に渡す情報
- 環境変数 `$ROOM_NAME` (entrypoint.sh で既に export 済み、start-training.sh 内でも利用可)
- `$REPORT_FILE` は start-training.sh 内の定数 `/workspace/REPORT.md`

### yq コマンド (コンテナ内にインストール済み・Go 版 v4 系)
- frontmatter 抽出: `yq '.mission_type' REPORT.md` は markdown frontmatter を直接読めない。先に sed で `---` 〜 `---` を切り出して `yq` にパイプする必要がある。
  ```bash
  sed -n '/^---$/,/^---$/{ /^---$/!p }' "$REPORT_FILE" | head -200 | yq '.mission_type'
  ```
  ただし 2 個目の `---` を含めるには範囲調整必要。より安全には:
  ```bash
  awk '/^---$/{c++; next} c==1' "$REPORT_FILE" | yq '.mission_type'
  ```
  awk で「1 つ目の --- と 2 つ目の --- の間だけ」を出力し、yq に YAML として食わせる。

### folder_to_name 規則 (spirit-room CLI 側で既に確立)
- `spirit-room-$(basename $folder)` の小文字化 + 英数ハイフン以外を `-` 置換 (spirit-room line 38-42)
- 本 Plan では `$ROOM_NAME` (entrypoint 正規化済み、英数アンハイフン のみ) をそのまま slug として使う

### 保存ファイルフォーマット (D-06)
```markdown
# Mr.ポポ feedback memory

- **Room:** spirit-room-langgraph-poc
- **Date:** 2026-04-23 15:42:10 JST
- **mission_type:** poc
- **completion_status:** completed
- **Source:** /workspace/REPORT.md (in this room) / reference only
- **Review status:** pending  # pending | applied (Plan 07-05 が applied/ に移動する)

## Feedback YAML (抽出結果)

\`\`\`yaml
feedback_schema_version: 1
completion_status: completed
mission_type: poc
ambiguous_in_brief: |
  - ...
...
\`\`\`
```

### ディレクトリ設計 (D-04)
```
/workspace/.planning/mr-popo-memory/
├── poc/
│   ├── 2026-04-23-spirit-room-langgraph-poc.md
│   └── 2026-04-24-spirit-room-fastapi-hello.md
├── refactoring/
│   └── 2026-04-25-spirit-room-java-refactor-demo.md
├── testdata/
├── investigation/
├── kaio/
└── unknown/     # mission_type 抽出失敗時のフォールバック
```

注: 部屋内 (`/workspace/`) に書き出す設計。ホストプロジェクトの `.planning/mr-popo-memory/` に格納されるため、後で Plan 07-05 のレビューコマンドがホスト側で読める。界王星モードは元々 `.planning/` を GSD が使うので衝突しないよう `mr-popo-memory/` サブディレクトリに閉じ込める。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: extract-feedback.sh 新規スクリプトを作成</name>
  <files>spirit-room/base/scripts/extract-feedback.sh</files>
  <read_first>
    - spirit-room/base/scripts/start-training.sh (Plan 07-03 適用後の状態。$ROOM_NAME / $REPORT_FILE / $INTERRUPTED_FLAG の使われ方と env 露出範囲を確認)
    - spirit-room/base/entrypoint.sh (line 4-9: ROOM_NAME 正規化ロジック。ここで `[^a-zA-Z0-9_-]` が `_` に置換されるため slug として信頼してよい)
    - .planning/phases/07-mr-popo-feedback-loop/07-CONTEXT.md §decisions D-04, D-05, D-06
    - spirit-room/base/Dockerfile (line 41-43: yq は Go 版 v4 系で /usr/local/bin/yq にある)
  </read_first>
  <action>
新規ファイル `spirit-room/base/scripts/extract-feedback.sh` を作成する。このスクリプトは start-training.sh から呼ばれ、/workspace/REPORT.md の YAML frontmatter を抽出して `.planning/mr-popo-memory/` 配下に保存する。

**ファイル全体のコード:**

```bash
#!/bin/bash
# extract-feedback.sh - REPORT.md の YAML frontmatter を抽出し Mr.ポポ memory に保存する
#
# Phase 7 の D-04/D-05/D-06 実装。部屋完了時に start-training.sh から呼ばれる。
# yq で frontmatter をパースし、mission_type に応じたディレクトリに保存する。
# パース失敗や frontmatter 欠落時は WARN ログのみで正常終了する (部屋の終了フローは止めない)。

set -u  # set -e は付けない (yq 失敗等でも部屋終了フローは続行させたい)

LOG_DIR="/workspace/.logs"
LOG_FILE="$LOG_DIR/progress.log"
REPORT_FILE="/workspace/REPORT.md"
MEMORY_BASE="/workspace/.planning/mr-popo-memory"

mkdir -p "$LOG_DIR" 2>/dev/null || true

log() { echo "[$(date '+%Y-%m-%d %H:%M:%S')] [extract-feedback] $*" | tee -a "$LOG_FILE"; }

# ── ガード: REPORT.md が無ければ何もしない ─────────────
if [ ! -f "$REPORT_FILE" ]; then
    log "[WARN] REPORT.md が見つからない — feedback 抽出スキップ"
    exit 0
fi

# ── frontmatter ブロックだけ切り出す ────────────────────
# awk で最初の --- と 2 番目の --- の間を出力 (YAML 本体のみ、--- 自体は含まない)
FRONTMATTER=$(awk '/^---$/{c++; if(c==2) exit; next} c==1' "$REPORT_FILE")

if [ -z "$FRONTMATTER" ]; then
    log "[WARN] REPORT.md に YAML frontmatter が見つからない — feedback 抽出スキップ"
    exit 0
fi

# ── yq でフィールド抽出 (失敗時も継続、値は空になる) ────
_yq() {
    # $1 = yq expression (e.g. '.mission_type')
    # 失敗や null は空文字を返す
    local _val
    _val=$(echo "$FRONTMATTER" | yq "$1" 2>/dev/null || echo "")
    if [ "$_val" = "null" ]; then
        echo ""
    else
        echo "$_val"
    fi
}

MISSION_TYPE=$(_yq '.mission_type')
COMPLETION_STATUS=$(_yq '.completion_status')
SCHEMA_VERSION=$(_yq '.feedback_schema_version')

# ── mission_type の正規化 ──────────────────────────────
# 空 / null / 未知の値は unknown ディレクトリにフォールバック
if [ -z "$MISSION_TYPE" ]; then
    MISSION_TYPE="unknown"
fi
# 小文字化 + 英数アンハイフン以外は _ に置換 (パス安全)
MISSION_TYPE=$(echo "$MISSION_TYPE" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9_-]/_/g')

# ── ファイル名組み立て (D-06: {date}-{room-slug}.md) ───
DATE_SLUG=$(date '+%Y-%m-%d')
ROOM_SLUG="${ROOM_NAME:-unknown-room}"
# ROOM_NAME は entrypoint で [^a-zA-Z0-9_-] が _ に正規化済みだが二重で保険
ROOM_SLUG=$(echo "$ROOM_SLUG" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9_-]/_/g')

TARGET_DIR="${MEMORY_BASE}/${MISSION_TYPE}"
mkdir -p "$TARGET_DIR" 2>/dev/null || {
    log "[ERROR] $TARGET_DIR の作成に失敗 — 抽出スキップ"
    exit 0
}

TARGET_FILE="${TARGET_DIR}/${DATE_SLUG}-${ROOM_SLUG}.md"

# ── 同名衝突時は -02 / -03 ... と suffix を付ける ──────
_n=2
while [ -e "$TARGET_FILE" ]; do
    TARGET_FILE="${TARGET_DIR}/${DATE_SLUG}-${ROOM_SLUG}-$(printf '%02d' $_n).md"
    _n=$((_n + 1))
    if [ "$_n" -gt 99 ]; then
        log "[ERROR] 同名ファイル衝突が 99 を超えた — 抽出スキップ"
        exit 0
    fi
done

# ── 保存ファイル本体を生成 (D-06 ヘッダ付き) ───────────
{
    echo "# Mr.ポポ feedback memory"
    echo ""
    echo "- **Room:** ${ROOM_SLUG}"
    echo "- **Date:** $(date '+%Y-%m-%d %H:%M:%S %Z')"
    echo "- **mission_type:** ${MISSION_TYPE}"
    echo "- **completion_status:** ${COMPLETION_STATUS:-unknown}"
    echo "- **feedback_schema_version:** ${SCHEMA_VERSION:-unknown}"
    echo "- **Source:** /workspace/REPORT.md (at extraction time, room=${ROOM_SLUG})"
    echo "- **Review status:** pending"
    echo ""
    echo "## Feedback YAML (抽出結果)"
    echo ""
    echo '```yaml'
    echo "$FRONTMATTER"
    echo '```'
} > "$TARGET_FILE" 2>/dev/null || {
    log "[ERROR] $TARGET_FILE への書き込み失敗"
    exit 0
}

log "[INFO] feedback を保存: $TARGET_FILE (mission_type=${MISSION_TYPE}, status=${COMPLETION_STATUS:-unknown})"

exit 0
```

**ポイントと禁止事項:**

- `set -e` は **使わない** — yq 失敗や mkdir 失敗でも部屋の正常終了フローを止めたくない (D-05 の "自動蓄積は邪魔しない" 原則)
- `$ROOM_NAME` は entrypoint.sh L4-9 で `[^a-zA-Z0-9_-]` → `_` に正規化済みだが、保険でここでも再正規化
- yq 抽出で `null` が返ることがある (フィールド欠落時) のでその場合は空文字扱いに
- mission_type が `poc` / `refactoring` / `testdata` / `investigation` / `kaio` のいずれでも、上記のディレクトリ方式で機械的に該当ディレクトリへ保存される (存在しないならホワイトリスト判定は不要 — 文字列が安全化されていれば mkdir -p で勝手にできる)
- Phase 0 (RESEARCH) 途中で中断したケース等で REPORT.md が存在しないなら何もせず exit 0
- 同名衝突時の -02 サフィックスは `printf '%02d'` で 2 桁 padding
- 99 件を超えたら諦めて exit 0 (実運用では 1 日 99 回の同じ部屋完了はあり得ない)
- log() で `tee -a` しているので /workspace/.logs/progress.log にも出る
- shebang は `#!/bin/bash` (既存スクリプト群と揃える)
- 実行権限 (`chmod +x`) は Task 2 (start-training からの呼び出し) で `bash /room/scripts/extract-feedback.sh` として呼ぶため実行ビット不要だが、`chmod +x` はビルド時ではなく Dockerfile COPY 後に scripts ディレクトリ全体へ付与される (必要なら Task 2 で明示)
  </action>
  <verify>
    <automated>
test -f /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
bash -n /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q '#!/bin/bash' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q '/workspace/.planning/mr-popo-memory' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q '/workspace/REPORT.md' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q 'awk .*c==2.*c==1' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q 'mission_type' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q 'completion_status' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q 'feedback_schema_version' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q 'Review status: pending' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
grep -q 'unknown' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
! grep -q '^set -e$' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/extract-feedback.sh && \
echo "OK: extract-feedback.sh 作成成功"
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f spirit-room/base/scripts/extract-feedback.sh` が成功
    - `bash -n spirit-room/base/scripts/extract-feedback.sh` が exit 0
    - `grep -c '#!/bin/bash' extract-feedback.sh` が 1
    - `grep -c '/workspace/.planning/mr-popo-memory' extract-feedback.sh` が 1 以上
    - `grep -c '/workspace/REPORT.md' extract-feedback.sh` が 1 以上
    - `grep -c 'awk .*c==2.*c==1' extract-feedback.sh` が 1 (frontmatter 切り出し)
    - `grep -c 'yq' extract-feedback.sh` が 1 以上
    - `grep -c 'mission_type' extract-feedback.sh` が 4 以上 (変数名 / yq query / 正規化 / パス組み立て)
    - `grep -c 'ROOM_SLUG' extract-feedback.sh` が 3 以上 (代入 / 正規化 / ファイル名組み立て)
    - `grep -c 'Review status: pending' extract-feedback.sh` が 1 (D-09 連携: Plan 07-05 で applied に変える)
    - `grep -c '^set -e$' extract-feedback.sh` が 0 (set -e は使わない)
    - `grep -c 'set -u' extract-feedback.sh` が 1 (未定義変数検知は on)
    - `grep -c 'exit 0' extract-feedback.sh` が 4 以上 (各 safe failure 経路)
    - `grep -c "printf '%02d'" extract-feedback.sh` が 1 (同名衝突 suffix)
  </acceptance_criteria>
  <done>extract-feedback.sh が作成され、bash -n 通過、safe failure 設計 (set -e なし / yq 失敗で空文字 / mkdir 失敗で exit 0) で実装されている</done>
</task>

<task type="auto">
  <name>Task 2: start-training.sh の PHASE 3 完了後に extract-feedback.sh を呼び出すフックを追加</name>
  <files>spirit-room/base/scripts/start-training.sh</files>
  <read_first>
    - spirit-room/base/scripts/start-training.sh (Task 1 完了後 + Plan 07-03 適用後の状態。PHASE 3 ループ末尾 phase_commit "docs: add REPORT.md" の行番号と完了バナーの位置を把握)
    - spirit-room/base/scripts/extract-feedback.sh (Task 1 で作成したばかり。呼び出し時に渡す必要のある env は ROOM_NAME のみで、それは entrypoint で既に export 済み)
  </read_first>
  <action>
`start-training.sh` の PHASE 3 (REPORT) ループ終了直後 (`phase_commit "docs: add REPORT.md"` の後) に extract-feedback.sh を呼び出すフックを追加する。

**変更 1: `phase_commit "docs: add REPORT.md"` の直後に feedback 抽出呼び出しを追加**

現行 (Plan 07-03 適用後):
```bash
phase_commit "docs: add REPORT.md"

echo "
╔══════════════════════════════════════════════╗
║              修行完了！部屋から出よ          ║
╚══════════════════════════════════════════════╝
" | tee -a "$LOG_FILE"
```

変更後:
```bash
phase_commit "docs: add REPORT.md"

# ── Phase 7 D-05: feedback 自動蓄積 (REPORT.md frontmatter を抽出して mr-popo-memory に保存) ─
# 失敗しても部屋の終了バナーは必ず出す (set -e を有効にしているため || true で吸収)
if [ -x /room/scripts/extract-feedback.sh ] || [ -f /room/scripts/extract-feedback.sh ]; then
    log "feedback 抽出開始 (extract-feedback.sh)"
    bash /room/scripts/extract-feedback.sh || log "[WARN] extract-feedback.sh が非ゼロ終了 — 続行"
    # 抽出結果を git に残す (.planning/mr-popo-memory/ 配下は git 管理対象)
    phase_commit "docs: add Mr.ポポ feedback memory"
else
    log "[WARN] /room/scripts/extract-feedback.sh が見つからない — feedback 抽出をスキップ"
fi

echo "
╔══════════════════════════════════════════════╗
║              修行完了！部屋から出よ          ║
╚══════════════════════════════════════════════╝
" | tee -a "$LOG_FILE"
```

**変更 2: .gitignore で `.planning/` が除外されないことの確認 (必要なら修正)**

start-training.sh の `init_git_workspace()` 関数内 (現行 line 60-71 の `.gitignore` ヒアドキュメント) を確認する。現状:

```bash
cat > .gitignore << 'EOF'
.logs/
.researched
.prepared
.done
.reported
node_modules/
__pycache__/
*.pyc
.venv/
EOF
```

この `.gitignore` は `.planning/` を除外していないので **変更不要**。ただし Plan 07-05 で `.planning/mr-popo-memory/applied/` への移動を想定するため、ここで明示的にコメントを足す:

```bash
cat > .gitignore << 'EOF'
.logs/
.researched
.prepared
.done
.reported
.interrupted
node_modules/
__pycache__/
*.pyc
.venv/
# Phase 7 feedback loop: .planning/mr-popo-memory/ は git 管理対象 (除外しない)
EOF
```

(`.interrupted` を追加したのは Phase 07-03 D-12 の安全網フラグがコミットノイズにならないように)

**禁止事項:**
- extract-feedback.sh の exit code が非ゼロでも部屋を落とさない (log で警告しつつ続行)
- extract-feedback.sh 呼び出し前に `cd` で場所を変えない (extract-feedback.sh は絶対パスでファイルを扱うので PWD 非依存)
- ROOM_NAME を明示的に export する必要はない (entrypoint.sh の親プロセスから継承されているため)
- 完了バナーの echo を削除・変更しない
- PHASE 3 ループ本体 (プロンプト) を変更しない — Plan 07-03 の状態を壊さない
- `.gitignore` から既存のエントリ (.logs/ / .researched / .prepared / .done / .reported / node_modules/ 等) を削除しない
  </action>
  <verify>
    <automated>
grep -q 'extract-feedback.sh' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh && \
grep -q 'feedback 抽出開始' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh && \
grep -q 'docs: add Mr.ポポ feedback memory' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh && \
grep -q '.interrupted' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh && \
grep -q '修行完了！部屋から出よ' /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh && \
bash -n /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh && \
grep -F "EFFECTIVE_MAX_ITERATIONS" /home/parallels/workspaces/spirit-room-full/spirit-room/base/scripts/start-training.sh | head -1 && \
echo "OK: start-training.sh フック追加成功 (+ 07-03 結合チェック)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `bash -n spirit-room/base/scripts/start-training.sh` が exit 0
    - `grep -c 'extract-feedback.sh' start-training.sh` が 2 以上 (存在チェック + 実行呼び出し)
    - `grep -c 'feedback 抽出開始' start-training.sh` が 1
    - `grep -c 'docs: add Mr.ポポ feedback memory' start-training.sh` が 1
    - `grep -c '|| log "\[WARN\] extract-feedback.sh' start-training.sh` が 1 (safe failure)
    - `grep -c '修行完了！部屋から出よ' start-training.sh` が 1 (既存完了バナー保持)
    - `grep -q '^\.interrupted$' <(grep -A 15 "cat > .gitignore" start-training.sh)` — .gitignore の中に `.interrupted` が入っている (`.` は正規表現メタ文字なのでエスケープ)
    - `! grep -qF '.planning/' <(grep -A 15 "cat > .gitignore" start-training.sh)` — .planning/ が ignore されていない (固定文字列 `-F` で確実にマッチ)
    - `grep -c '.logs/' start-training.sh` が 1 以上 (既存 .gitignore エントリ保持)
    - `grep -c '.reported' start-training.sh` が 2 以上 (定数 REPORTED_FLAG + .gitignore)
    - **07-03 結合チェック**: `grep -cF 'EFFECTIVE_MAX_ITERATIONS' start-training.sh` が 5 以上 (Plan 07-03 で追加された変数名が残存しており、07-04 の編集で誤って破壊されていないことの早期検出。bash -n の直後で確認することで変数名変更の組み合わせ不整合を弾く)
  </acceptance_criteria>
  <done>start-training.sh が PHASE 3 完了後に extract-feedback.sh を bash で呼び出し、失敗を log で吸収しつつ .planning/mr-popo-memory/ を git に commit するフックが実装され、.gitignore に .interrupted が追加されている。併せて Plan 07-03 の EFFECTIVE_MAX_ITERATIONS が無傷で残っていることも結合チェックで確認される</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REPORT.md (agent 生成) → yq パース | 信頼できない YAML content が来る可能性 |
| yq 出力 (mission_type) → ファイルシステムパス | パストラバーサル経路 |
| $ROOM_NAME → ファイル名 | entrypoint で正規化済みだが 2 段バリデート |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | mission_type による Path Traversal (`../../etc/shadow` 等) | mitigate | `tr '[:upper:]' '[:lower:]' \| sed 's/[^a-z0-9_-]/_/g'` で英小文字数字アンダースコアハイフンのみに正規化。`/` や `..` が入ってもハイフン等に置換される |
| T-07-04-02 | Denial of Service | yq パース失敗 / mkdir 失敗 / ディスク満杯 | mitigate | set -e を使わず各段階で `|| exit 0` / `\|\| log WARN` で吸収。部屋の終了バナーは必ず出る |
| T-07-04-03 | Information Disclosure | feedback ファイルに MISSION.md / REPORT.md 由来の情報が入る | accept | すべて /workspace 配下 (ホストマウント) に閉じる。外部送信経路なし |
| T-07-04-04 | Tampering | frontmatter に巨大な blob を入れられて yq が OOM | accept | REPORT.md サイズは generate 側が自制 (Claude Code の出力上限でバウンド)。実用上は数 KB |
| T-07-04-05 | Tampering | 同名衝突で上書きされ履歴喪失 | mitigate | -02 / -03... suffix で上書き回避。99 超えで諦める |
</threat_model>

<verification>
- `bash -n` が両ファイルで exit 0
- extract-feedback.sh が単独で起動可能 (`ROOM_NAME=test bash spirit-room/base/scripts/extract-feedback.sh` が REPORT.md 無しで exit 0 を返す — 実 E2E は別途部屋を回して確認)
- start-training.sh のフックが完了バナーの前にある
- .gitignore に `.interrupted` が追加されている
</verification>

<success_criteria>
- extract-feedback.sh が safe failure 設計で実装されている
- start-training.sh の PHASE 3 ループ後に extract-feedback.sh を bash 経由で呼ぶフックが入っている
- .planning/mr-popo-memory/{mission_type}/{date}-{slug}.md への保存パスが仕様通り
- 同名衝突は -02 / -03 で回避
- yq パース失敗やファイル書き込み失敗は WARN のみで部屋終了は止めない
</success_criteria>

<output>
After completion, create `.planning/phases/07-mr-popo-feedback-loop/07-04-SUMMARY.md`
</output>
