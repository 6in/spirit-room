---
phase: 05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - spirit-room/base/entrypoint.sh
autonomous: false
requirements:
  - PHASE5-ENTRYPOINT-GOKU-01       # goku 冪等作成 (D-01, D-02)
  - PHASE5-ENTRYPOINT-FALLBACK-01   # HOST_UID/GID fallback 1000:1000 (D-03)
  - PHASE5-ENTRYPOINT-SUDOERS-01    # goku NOPASSWD sudo 付与 (D-05)
  - PHASE5-ENTRYPOINT-CHOWN-01      # /workspace と認証ボリュームの chown (D-10, D-11)
  - PHASE5-ENTRYPOINT-KAIO-SYMLINK-01 # kaio credentials symlink の chown -h (D-14)
  - PHASE5-ENTRYPOINT-GITCONFIG-01  # goku HOME への git config ミラー (D-12)
  - PHASE5-ENTRYPOINT-TMUX-GOKU-01  # tmux を su - goku -c で起動 (D-08)
  - PHASE5-ENTRYPOINT-PID1-ROOT-01  # PID 1 tail -f は root のまま (D-09 ⑥)
  - NON_REGRESSION-AUTH-01          # 既存 AUTH-01/02 非回帰 (共有ボリュームからの認証継続)
  - NON_REGRESSION-LOOP-01          # 既存 LOOP-01〜04 非回帰 (start-training が goku で完走)
  - NON_REGRESSION-PHASE4-KAIO-01   # Phase 4 kaio 設計 (CLAUDE_CONFIG_DIR + credentials symlink) 非破壊
user_setup: []

must_haves:
  truths:
    - "HOST_UID / HOST_GID が未指定でも entrypoint が止まらず 1000:1000 で起動する"
    - "goku ユーザーが存在しない場合は作成し、存在すればスキップする (冪等)"
    - "起動ごとに /workspace, /root/.claude, /root/.config/opencode, /root/.claude-shared (存在する場合) が $HOST_UID:$HOST_GID に chown されている"
    - "kaio モード時、$CLAUDE_CONFIG_DIR/.credentials.json symlink が goku 所有 (chown -h) になっている"
    - "tmux セッション spirit-room が goku ユーザーで起動している (whoami が goku を返す)"
    - "PID 1 (tail -f /dev/null) は root のまま"
    - "goku の HOME に safe.directory '*' + user.email/name/init.defaultBranch が設定されている"
  artifacts:
    - path: "spirit-room/base/entrypoint.sh"
      provides: "goku 冪等作成 + chown + goku 化 tmux + kaio symlink chown 統合"
      contains: "useradd -m -u"
  key_links:
    - from: "entrypoint.sh 新規セクション (goku 作成)"
      to: "/etc/sudoers.d/goku"
      via: "echo 'goku ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/goku && chmod 0440 /etc/sudoers.d/goku"
      pattern: "sudoers.d/goku"
    - from: "entrypoint.sh tmux 起動部"
      to: "/usr/bin/su (login shell)"
      via: "su - goku -c 'tmux new-session -d ...'"
      pattern: "su - goku -c"
    - from: "entrypoint.sh kaio 分岐"
      to: "$CLAUDE_CONFIG_DIR/.credentials.json (symlink)"
      via: "chown -h $HOST_UID:$HOST_GID $CLAUDE_CONFIG_DIR/.credentials.json"
      pattern: "chown -h"
---

<objective>
精神と時の部屋コンテナの `entrypoint.sh` を改修し、root で起動したサービスの後に goku ユーザーをランタイム作成、/workspace と認証ボリュームを goku 所有に chown、kaio モードの credentials symlink も含めて所有権を切り替え、tmux セッションを goku ユーザーとして起動するよう変更する。PID 1 (`tail -f /dev/null`) は root のまま残す。

**Purpose:** Phase 5 のコア。root 実行による /workspace 所有権問題を解消する。ホスト側で `sudo chown -R` が不要になり、コンテナ内成果物がホストユーザー所有で直接見える。既存の「精神と時の部屋モード」「界王星 (kaio) モード」の両方で効く。

**Output:** D-09 の順序を厳密に守った entrypoint.sh。Plan 03 (CLI) で `-e HOST_UID/-e HOST_GID` が渡されれば期待動作する。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-CONTEXT.md
@.planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-PATTERNS.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md
@spirit-room/base/entrypoint.sh
@spirit-room/base/scripts/start-training.sh
@CLAUDE.md

<interfaces>
<!-- 現行 entrypoint.sh の全体 (改修対象) — L1-83。D-09 の順序に沿って新規ブロックを挿入する。 -->

現行 entrypoint.sh 全体 (83 行):
```bash
#!/bin/bash
set -e

ROOM_NAME="${ROOM_NAME:-$(basename /workspace)}"

echo "
╔══════════════════════════════════════════════╗
║         精神と時の部屋 - Spirit Room         ║
║  部屋: $ROOM_NAME
╚══════════════════════════════════════════════╝
"

# ── サービス起動 ─────────────────────────────────────────────
service ssh start
service redis-server start
echo "[INFO] Redis起動完了"

# ── 界王星モード: CLAUDE_CONFIG_DIR 分岐 ─────────────────────
# CLAUDE_CONFIG_DIR が設定されている = 界王星モード。
# 共有認証ボリュームは /root/.claude-shared にマウントされている前提 (spirit-room kaio 側で -v 指定)。
# その中の .credentials.json を $CLAUDE_CONFIG_DIR/.credentials.json に symlink することで、
# トークンリフレッシュが共有ボリュームに戻り、他の部屋にも伝播する。
if [ -n "${CLAUDE_CONFIG_DIR:-}" ]; then
    echo "[INFO] 界王星モード: CLAUDE_CONFIG_DIR=$CLAUDE_CONFIG_DIR"
    mkdir -p "$CLAUDE_CONFIG_DIR"

    if [ -f /root/.claude-shared/.credentials.json ]; then
        ln -sf /root/.claude-shared/.credentials.json "$CLAUDE_CONFIG_DIR/.credentials.json"
        echo "[INFO] 認証情報を symlink: $CLAUDE_CONFIG_DIR/.credentials.json → /root/.claude-shared/.credentials.json"
    else
        echo "[WARN] /root/.claude-shared/.credentials.json が見つからない。spirit-room auth を実行せよ"
    fi
fi

# ── 認証チェック ─────────────────────────────────────────────
if ! claude auth status &>/dev/null 2>&1; then
    echo "
╔══════════════════════════════════════════════╗
║  未認証: SSH接続後に以下を実行してください   ║
║  $ claude auth login                         ║
╚══════════════════════════════════════════════╝
"
else
    echo "[INFO] Claude Code 認証済み"
fi

# ── catalog.mdの優先順位: /workspace > /room ─────────────────
if [ -f /workspace/catalog.md ]; then
    echo "[INFO] catalog.md: /workspace/catalog.md を使用"
else
    echo "[INFO] catalog.md: デフォルト(/room/catalog.md)を使用"
fi

# ── tmuxセッション ───────────────────────────────────────────
SESSION="spirit-room"
tmux new-session -d -s "$SESSION" -x 220 -y 50

tmux rename-window -t "$SESSION:0" "training"
if [ -n "${CLAUDE_CONFIG_DIR:-}" ] && [ -f /workspace/KAIO-MISSION.md ] && [ ! -f /workspace/.kaio-done ]; then
    # 界王星モード: GSD 駆動チェーン
    tmux send-keys -t "$SESSION:training" "start-training-kaio" C-m
elif [ -f /workspace/MISSION.md ] && [ ! -f /workspace/.done ]; then
    # 精神と時の部屋モード (既存)
    tmux send-keys -t "$SESSION:training" "start-training" C-m
else
    tmux send-keys -t "$SESSION:training" \
        "echo '部屋[$ROOM_NAME] 準備完了 | start-training(-kaio) で修行開始 | status で確認'" C-m
fi

tmux new-window -t "$SESSION" -n "logs"
tmux send-keys -t "$SESSION:logs" \
    "tail -f /workspace/.logs/progress.log 2>/dev/null || (echo 'ログ待機中...'; while true; do sleep 2; tail -f /workspace/.logs/progress.log 2>/dev/null && break; done)" C-m

tmux new-window -t "$SESSION" -n "workspace"
tmux send-keys -t "$SESSION:workspace" \
    "watch -n 2 'tree /workspace -L 3 -I .logs 2>/dev/null || ls -la /workspace'" C-m

tmux select-window -t "$SESSION:training"

echo "[INFO] tmux '$SESSION' 起動完了"
echo "[INFO] 接続: tmux attach -t $SESSION"

tail -f /dev/null
```

<!-- D-09 に従う最終順序: ① service 起動 → ② HOST_UID/GID fallback → ③ goku 作成 → ④ chown (auth volumes + /workspace) → ⑤ kaio 分岐で credentials symlink + chown -h → ⑥ goku 用 git config → ⑦ 認証チェック (既存のまま) → ⑧ catalog 判定 (既存のまま) → ⑨ tmux を su - goku -c でラップ → ⑩ tail -f /dev/null (root のまま)。 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 2.1: entrypoint.sh に HOST_UID/GID 受け取り + goku 冪等作成 + chown ブロックを挿入</name>
  <files>spirit-room/base/entrypoint.sh</files>

  <read_first>
    - spirit-room/base/entrypoint.sh (現行全体 L1-83 — 改修対象)
    - .planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-CONTEXT.md の <decisions> D-01〜D-15 と <specifics> L131-170 (entrypoint.sh 改修骨子の疑似コード)
    - .planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-PATTERNS.md の Pattern B1 (冪等チェック分岐) / Pattern B2 (service 起動ブロック) / "Shared Patterns" (冪等化フラグ / エラー許容 || true / [INFO][WARN] メッセージ / セクションセパレータ)
    - .planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md (kaio の CLAUDE_CONFIG_DIR / credentials symlink 設計 — 破壊しないこと)
    - CLAUDE.md の Code Style (4 space インデント / quote / set -e / セクションセパレータ `# ── ... ────`)
  </read_first>

  <action>
現行の entrypoint.sh L16 (`echo "[INFO] Redis起動完了"`) と L18 (`# ── 界王星モード: CLAUDE_CONFIG_DIR 分岐 ─────`) の**間**に新規 3 ブロック (HOST_UID 受け取り / goku 冪等作成 / chown) を挿入する。さらに既存の kaio 分岐 (L23-33) 内部に `chown -h` を 2 行追加する。

挿入位置 = L17 の直後 (既存 `echo "[INFO] Redis起動完了"` の次の空行位置)。

**挿入する完成ブロック (このまま貼る):**

```bash

# ── HOST_UID/GID 受け取り (fallback: 1000:1000) ─────────────
# 手動 docker run や CI 経由で HOST_UID/GID が渡されない場合も落ちないようにする。
HOST_UID="${HOST_UID:-1000}"
HOST_GID="${HOST_GID:-1000}"
if [ "${HOST_UID}" = "1000" ] && [ -z "${HOST_UID_WAS_SET:-}" ]; then
    echo "[INFO] HOST_UID/GID=${HOST_UID}:${HOST_GID} (fallback or explicit)"
else
    echo "[INFO] HOST_UID/GID=${HOST_UID}:${HOST_GID}"
fi

# ── goku ユーザーの冪等作成 ─────────────────────────────────
# D-01: ビルド時ではなくランタイムで作る (HOST_UID がホストごとに異なるため)
# D-02: 冪等。既に goku が居れば skip。GID も既存 group がなければ作る
# D-04: パスワードは root と同じ spiritroom
# D-05: NOPASSWD:ALL の sudo を付与 (POC 用途のため粒度は絞らない)
# useradd の -o は UID 重複許可 (既存ユーザー _apt 等と HOST_UID が衝突した場合の保険)
if ! id goku &>/dev/null; then
    getent group "$HOST_GID" >/dev/null || groupadd -g "$HOST_GID" goku
    useradd -m -u "$HOST_UID" -g "$HOST_GID" -s /bin/bash -o goku
    echo 'goku:spiritroom' | chpasswd
    echo 'goku ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/goku
    chmod 0440 /etc/sudoers.d/goku
    echo "[INFO] goku ユーザーを作成 (UID=$HOST_UID GID=$HOST_GID)"
else
    echo "[INFO] goku ユーザーは既に存在 (skip)"
fi

# ── 認証ボリュームと /workspace を goku 所有に ───────────────
# D-10/D-11: 起動ごとに chown を走らせ、既存の root 所有ファイルを一度で正常化する
# 対象: 認証ボリュームのマウント先 (通常モード: /root/.claude、kaio モード: /root/.claude-shared、opencode: /root/.config/opencode) および /workspace 本体
chown -R "$HOST_UID:$HOST_GID" /root/.claude 2>/dev/null || true
chown -R "$HOST_UID:$HOST_GID" /root/.config/opencode 2>/dev/null || true
[ -d /root/.claude-shared ] && chown -R "$HOST_UID:$HOST_GID" /root/.claude-shared 2>/dev/null || true
chown -R "$HOST_UID:$HOST_GID" /workspace 2>/dev/null || true
echo "[INFO] /workspace と認証ボリュームを $HOST_UID:$HOST_GID 所有に切替"
```

**既存 kaio 分岐 (L23-33) への追記:**

現行:
```bash
if [ -n "${CLAUDE_CONFIG_DIR:-}" ]; then
    echo "[INFO] 界王星モード: CLAUDE_CONFIG_DIR=$CLAUDE_CONFIG_DIR"
    mkdir -p "$CLAUDE_CONFIG_DIR"

    if [ -f /root/.claude-shared/.credentials.json ]; then
        ln -sf /root/.claude-shared/.credentials.json "$CLAUDE_CONFIG_DIR/.credentials.json"
        echo "[INFO] 認証情報を symlink: $CLAUDE_CONFIG_DIR/.credentials.json → /root/.claude-shared/.credentials.json"
    else
        echo "[WARN] /root/.claude-shared/.credentials.json が見つからない。spirit-room auth を実行せよ"
    fi
fi
```

差し替え後 (CLAUDE_CONFIG_DIR 自身と symlink を chown 追加):
```bash
if [ -n "${CLAUDE_CONFIG_DIR:-}" ]; then
    echo "[INFO] 界王星モード: CLAUDE_CONFIG_DIR=$CLAUDE_CONFIG_DIR"
    mkdir -p "$CLAUDE_CONFIG_DIR"
    # D-13: CLAUDE_CONFIG_DIR 自身も goku 所有にする (/workspace 下なので整合)
    chown "$HOST_UID:$HOST_GID" "$CLAUDE_CONFIG_DIR" 2>/dev/null || true

    if [ -f /root/.claude-shared/.credentials.json ]; then
        ln -sf /root/.claude-shared/.credentials.json "$CLAUDE_CONFIG_DIR/.credentials.json"
        # D-14: symlink 自体の所有者を goku に (chown -h)。実体ファイルは上の chown -R /root/.claude-shared で既に goku 所有
        chown -h "$HOST_UID:$HOST_GID" "$CLAUDE_CONFIG_DIR/.credentials.json" 2>/dev/null || true
        echo "[INFO] 認証情報を symlink: $CLAUDE_CONFIG_DIR/.credentials.json → /root/.claude-shared/.credentials.json (goku 所有)"
    else
        echo "[WARN] /root/.claude-shared/.credentials.json が見つからない。spirit-room auth を実行せよ"
    fi
fi
```

**禁止事項:**
- 既存の `# ── 認証チェック ────` / `# ── catalog.mdの優先順位 ────` ブロックは触らない
- tmux 関連 (L54-77) はこのタスクでは触らない (Task 2.2 で扱う)
- `tail -f /dev/null` を外に出したり su goku に置き換えたりしない (D-09 ⑥: root のまま)
- 冒頭の `set -e` は維持するが、chown 行は `|| true` で安全化済みなので exit 1 になりづらい。`set +e` で一時的にオフにしない
- 既存の `if ! claude auth status &>/dev/null 2>&1; then ... fi` (L36-45) はそのまま残す (goku 化後も root コンテキストで claude auth status が走るが、共有ボリュームの読み取りさえ出来れば判定成立)
  </action>

  <acceptance_criteria>
    - `grep -q 'HOST_UID="${HOST_UID:-1000}"' spirit-room/base/entrypoint.sh` が成功する
    - `grep -q 'HOST_GID="${HOST_GID:-1000}"' spirit-room/base/entrypoint.sh` が成功する
    - `grep -q 'id goku' spirit-room/base/entrypoint.sh` が成功する
    - `grep -q 'useradd -m -u "$HOST_UID" -g "$HOST_GID"' spirit-room/base/entrypoint.sh` が成功する (`-o` 付きでも可: `grep -q 'useradd -m -u "\$HOST_UID" -g "\$HOST_GID" -s /bin/bash -o goku' spirit-room/base/entrypoint.sh`)
    - `grep -q "echo 'goku:spiritroom' | chpasswd" spirit-room/base/entrypoint.sh` が成功する
    - `grep -q '/etc/sudoers.d/goku' spirit-room/base/entrypoint.sh` が成功する
    - `grep -q 'chmod 0440 /etc/sudoers.d/goku' spirit-room/base/entrypoint.sh` が成功する
    - `grep -Eq 'chown -R "\$HOST_UID:\$HOST_GID" /workspace' spirit-room/base/entrypoint.sh` が成功する
    - `grep -Eq 'chown -R "\$HOST_UID:\$HOST_GID" /root/.claude' spirit-room/base/entrypoint.sh` が成功する (通常モード用)
    - `grep -Eq 'chown -R "\$HOST_UID:\$HOST_GID" /root/.claude-shared' spirit-room/base/entrypoint.sh` が成功する (kaio 用)
    - `grep -q 'chown -h' spirit-room/base/entrypoint.sh` が成功する (credentials symlink 用)
    - `grep -q 'CLAUDE_CONFIG_DIR/.credentials.json' spirit-room/base/entrypoint.sh` が成功する (既存の kaio symlink ロジックが残存)
    - `bash -n spirit-room/base/entrypoint.sh` (構文チェック) が exit 0
    - `tail -1 spirit-room/base/entrypoint.sh | grep -q 'tail -f /dev/null'` (PID 1 行が末尾で root のまま残っている)
  </acceptance_criteria>

  <verify>
    <automated>bash -n spirit-room/base/entrypoint.sh && grep -q 'HOST_UID="${HOST_UID:-1000}"' spirit-room/base/entrypoint.sh && grep -q 'id goku' spirit-room/base/entrypoint.sh && grep -Eq 'useradd.*-u.*HOST_UID.*-g.*HOST_GID.*goku' spirit-room/base/entrypoint.sh && grep -q '/etc/sudoers.d/goku' spirit-room/base/entrypoint.sh && grep -q 'chmod 0440 /etc/sudoers.d/goku' spirit-room/base/entrypoint.sh && grep -Eq 'chown -R "\$HOST_UID:\$HOST_GID" /workspace' spirit-room/base/entrypoint.sh && grep -Eq 'chown -R "\$HOST_UID:\$HOST_GID" /root/.claude-shared' spirit-room/base/entrypoint.sh && grep -q 'chown -h' spirit-room/base/entrypoint.sh && tail -1 spirit-room/base/entrypoint.sh | grep -q 'tail -f /dev/null' && echo OK</automated>
  </verify>

  <done>
    - HOST_UID / HOST_GID 受け取りブロック + 1000:1000 fallback が存在
    - goku 冪等作成 (id goku &>/dev/null ガード) + groupadd + useradd -o + chpasswd + sudoers.d/goku 書出し + chmod 0440
    - 認証ボリューム (/root/.claude, /root/.claude-shared, /root/.config/opencode) と /workspace の chown が毎回走る
    - kaio 分岐に chown (CLAUDE_CONFIG_DIR 自身) + chown -h (credentials symlink) が追加
    - 既存の kaio symlink ロジック + 認証チェック + catalog 判定 は温存
    - tail -f /dev/null が末尾に残っている (root のまま)
    - bash -n で構文エラーなし
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2.2: entrypoint.sh の tmux 起動を su - goku -c でラップ + goku HOME への git config ミラー</name>
  <files>spirit-room/base/entrypoint.sh</files>

  <read_first>
    - spirit-room/base/entrypoint.sh (現行全体。特に L54-77 の tmux 3 ペイン構築)
    - .planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-CONTEXT.md の D-08 (tmux を su - goku -c で起動), D-09 (順序), D-12 (goku HOME への git config ミラー), <specifics> L172-184 (tmux を su - goku -c でラップする擬似コード)
    - .planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-PATTERNS.md の Pattern B3 (tmux 3 pane 構築 — ラップ方針 案 A/B), Pattern B4 (PID 1 保持)
    - Task 2.1 で挿入した goku 作成ブロック (HOST_UID/GID / goku が既に存在する前提)
  </read_first>

  <action>
現行の entrypoint.sh 末尾付近 (L54-83 あたり) を編集する。具体的には:

**変更 A:** 既存の `# ── catalog.mdの優先順位: /workspace > /room ─────────────────` ブロックと `# ── tmuxセッション ───` ブロックの**間**に、goku HOME 用 git config ミラーブロックを挿入する。

挿入ブロック (そのまま貼る):
```bash

# ── goku として git config (goku HOME に root 側と同じ設定をミラー) ─
# D-12: Dockerfile L58-64 の root 用 git config はそのまま。goku HOME 側にも
# safe.directory='*' / user.email / user.name / init.defaultBranch を設定し、
# start-training.sh が goku で走っても dubious ownership エラーを出さないようにする。
su - goku -c "git config --global --add safe.directory '*' && \
    git config --global user.email 'spirit-room@localhost' && \
    git config --global user.name 'Spirit Room' && \
    git config --global init.defaultBranch main" || echo "[WARN] goku 用 git config 設定に失敗"
```

**変更 B:** 既存の tmux 起動 (L54-77) を `su - goku -c` で完全にラップする。既存の分岐 (kaio / 精神と時の部屋 / それ以外) と 3 ペイン構築は意味的に保存しつつ、実行ユーザーを goku に切り替える。

現行 (L54-80):
```bash
# ── tmuxセッション ───────────────────────────────────────────
SESSION="spirit-room"
tmux new-session -d -s "$SESSION" -x 220 -y 50

tmux rename-window -t "$SESSION:0" "training"
if [ -n "${CLAUDE_CONFIG_DIR:-}" ] && [ -f /workspace/KAIO-MISSION.md ] && [ ! -f /workspace/.kaio-done ]; then
    # 界王星モード: GSD 駆動チェーン
    tmux send-keys -t "$SESSION:training" "start-training-kaio" C-m
elif [ -f /workspace/MISSION.md ] && [ ! -f /workspace/.done ]; then
    # 精神と時の部屋モード (既存)
    tmux send-keys -t "$SESSION:training" "start-training" C-m
else
    tmux send-keys -t "$SESSION:training" \
        "echo '部屋[$ROOM_NAME] 準備完了 | start-training(-kaio) で修行開始 | status で確認'" C-m
fi

tmux new-window -t "$SESSION" -n "logs"
tmux send-keys -t "$SESSION:logs" \
    "tail -f /workspace/.logs/progress.log 2>/dev/null || (echo 'ログ待機中...'; while true; do sleep 2; tail -f /workspace/.logs/progress.log 2>/dev/null && break; done)" C-m

tmux new-window -t "$SESSION" -n "workspace"
tmux send-keys -t "$SESSION:workspace" \
    "watch -n 2 'tree /workspace -L 3 -I .logs 2>/dev/null || ls -la /workspace'" C-m

tmux select-window -t "$SESSION:training"

echo "[INFO] tmux '$SESSION' 起動完了"
echo "[INFO] 接続: tmux attach -t $SESSION"
```

差し替え後 (heredoc + 親 shell 展開 = クォート無しヒアドキュメントで変数を埋め込む方式 / Pattern B3 案 A):
```bash
# ── tmuxセッション (goku として起動) ─────────────────────────
# D-08/D-09 ⑤: tmux は goku として起動。CLAUDE_CONFIG_DIR など環境変数は親 shell で展開して埋め込む
# (su - は login shell のため env が goku デフォルトにリセットされる)
SESSION="spirit-room"

# 分岐判定は親 shell (root) 側でフラグ化し、ヒアドキュメント内で展開する
_TRAINING_CMD="echo '部屋[${ROOM_NAME}] 準備完了 | start-training(-kaio) で修行開始 | status で確認'"
if [ -n "${CLAUDE_CONFIG_DIR:-}" ] && [ -f /workspace/KAIO-MISSION.md ] && [ ! -f /workspace/.kaio-done ]; then
    _TRAINING_CMD="start-training-kaio"
elif [ -f /workspace/MISSION.md ] && [ ! -f /workspace/.done ]; then
    _TRAINING_CMD="start-training"
fi

# kaio モード時は CLAUDE_CONFIG_DIR を goku セッション内でも使えるよう ~/.profile 経由で export
# (su - goku が login shell なので .profile が読まれる。冪等化のため重複追加を避ける)
if [ -n "${CLAUDE_CONFIG_DIR:-}" ]; then
    su - goku -c "grep -q 'export CLAUDE_CONFIG_DIR=' ~/.profile 2>/dev/null || echo 'export CLAUDE_CONFIG_DIR=${CLAUDE_CONFIG_DIR}' >> ~/.profile" || true
fi

su - goku -c "bash" << EOF
set -e
tmux new-session -d -s "${SESSION}" -x 220 -y 50
tmux rename-window -t "${SESSION}:0" "training"
tmux send-keys -t "${SESSION}:training" "${_TRAINING_CMD}" C-m

tmux new-window -t "${SESSION}" -n "logs"
tmux send-keys -t "${SESSION}:logs" "tail -f /workspace/.logs/progress.log 2>/dev/null || (echo 'ログ待機中...'; while true; do sleep 2; tail -f /workspace/.logs/progress.log 2>/dev/null && break; done)" C-m

tmux new-window -t "${SESSION}" -n "workspace"
tmux send-keys -t "${SESSION}:workspace" "watch -n 2 'tree /workspace -L 3 -I .logs 2>/dev/null || ls -la /workspace'" C-m

tmux select-window -t "${SESSION}:training"
EOF

echo "[INFO] tmux '$SESSION' 起動完了 (user=goku)"
echo "[INFO] 接続: tmux attach -t $SESSION (goku で接続すること)"
```

**重要ポイント:**
- ヒアドキュメント `<< EOF` (**クォート無し**) にすることで `${SESSION}` / `${_TRAINING_CMD}` / `${ROOM_NAME}` は**親 shell (root) 側**で展開され、goku セッションにリテラルとして埋め込まれる。goku 側で再度展開する必要がない。
- `su - goku -c "bash"` と `<< EOF` の組合せで、login shell の PATH + HOME 初期化を活かしつつコマンド列を渡す。
- `_TRAINING_CMD` にシングルクォート文字列を入れるのは `echo '部屋[...]...'` のみ。内側クォートの衝突を避けるため、ヒアドキュメント内では `"${_TRAINING_CMD}"` をダブルクォートで囲み、`$'...'` 等の拡張は使わない。
- CLAUDE_CONFIG_DIR が設定されていれば goku の ~/.profile に冪等 export する (start-training-kaio 実行時に必要)。`grep -q` ガードで重複追加を防ぐ。
- PID 1 (`tail -f /dev/null`) はヒアドキュメントの**外**、親 shell (root) で実行されるので、末尾の `tail -f /dev/null` はそのまま残る。

**禁止事項:**
- `tail -f /dev/null` を goku に移さない (D-09 ⑥)
- `service ssh start` / `service redis-server start` を goku に移さない (root 特権が必要)
- 既存の 3 ペイン構成 (training / logs / workspace) を壊さない
- ヒアドキュメントを `<< 'EOF'` (クォート付き) にしない — 変数が展開されないため `${SESSION}` がリテラル `${SESSION}` として goku 側に渡ってしまう
- ヒアドキュメント内で `\$` エスケープを多用しない (親 shell 展開を意図しているため)
- 既存の Task 2.1 で挿入したブロック (HOST_UID 受け取り / goku 作成 / chown / kaio symlink chown) を壊さない
  </action>

  <acceptance_criteria>
    - `grep -q "su - goku -c" spirit-room/base/entrypoint.sh` が成功する
    - `grep -q "git config --global --add safe.directory" spirit-room/base/entrypoint.sh` が成功する (goku HOME 側のミラー)
    - `grep -q "git config --global user.email 'spirit-room@localhost'" spirit-room/base/entrypoint.sh` が成功する
    - `grep -q 'tmux new-session -d -s' spirit-room/base/entrypoint.sh` が成功する (既存 tmux ロジックが保存されている)
    - `grep -q 'tmux rename-window -t' spirit-room/base/entrypoint.sh` が成功する
    - `grep -q 'start-training-kaio' spirit-room/base/entrypoint.sh` が成功する (kaio 分岐が保存されている)
    - `grep -q 'start-training"' spirit-room/base/entrypoint.sh || grep -q '"start-training"' spirit-room/base/entrypoint.sh` が成功する (精神と時の部屋分岐が保存されている)
    - `grep -q '/workspace/.logs/progress.log' spirit-room/base/entrypoint.sh` が成功する (logs ペイン保存)
    - `grep -q "watch -n 2 'tree /workspace" spirit-room/base/entrypoint.sh` が成功する (workspace ペイン保存)
    - `tail -5 spirit-room/base/entrypoint.sh | grep -q 'tail -f /dev/null'` (末尾 5 行内に PID 1 行がある)
    - `! grep -qE "^tmux new-session" spirit-room/base/entrypoint.sh` (root 直接の tmux コマンドが行頭に無い = すべてヒアドキュメント内 or su - goku -c の中)
    - `bash -n spirit-room/base/entrypoint.sh` が exit 0 (構文エラーなし)
    - `grep -q "export CLAUDE_CONFIG_DIR=" spirit-room/base/entrypoint.sh` が成功する (kaio モード時の env 引き継ぎ処理)
  </acceptance_criteria>

  <verify>
    <automated>bash -n spirit-room/base/entrypoint.sh && grep -q 'su - goku -c' spirit-room/base/entrypoint.sh && grep -q "git config --global --add safe.directory '\*'" spirit-room/base/entrypoint.sh && grep -q 'tmux new-session -d -s' spirit-room/base/entrypoint.sh && grep -q 'start-training-kaio' spirit-room/base/entrypoint.sh && grep -q '/workspace/.logs/progress.log' spirit-room/base/entrypoint.sh && tail -5 spirit-room/base/entrypoint.sh | grep -q 'tail -f /dev/null' && ! grep -qE "^tmux new-session" spirit-room/base/entrypoint.sh && grep -q 'export CLAUDE_CONFIG_DIR=' spirit-room/base/entrypoint.sh && echo OK</automated>
  </verify>

  <done>
    - goku HOME 用 git config ブロック (safe.directory='*' / user.email / user.name / init.defaultBranch) が挿入済み
    - tmux 起動が `su - goku -c "bash" << EOF ... EOF` のヒアドキュメントでラップされている
    - 既存 3 ペイン (training / logs / workspace) と分岐 (kaio / 精神と時の部屋 / それ以外) が保存されている
    - kaio モード時、CLAUDE_CONFIG_DIR が goku の ~/.profile に冪等 export される
    - ファイル末尾の `tail -f /dev/null` が残っており、root で実行される
    - bash -n でエラーなし
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.3: 再ビルドしコンテナ起動で goku 切替 + /workspace 所有権を検証</name>
  <files>spirit-room/base/entrypoint.sh (verify target), spirit-room/build-base.sh (invoke), /tmp/spirit-room-phase5-test (scratch workspace)</files>
  <action>ユーザーが手元で下の <how-to-verify> の 9 ステップ (build-base.sh 実行 → docker run で手動に HOST_UID/GID 渡し → whoami / 所有権 / sudo / SSH 分岐 / kaio credentials symlink 所有者を検証) を実行する。Claude 側は結果待ち。問題があれば docker logs 出力を元に Task 2.1 / Task 2.2 を修正する。</action>
  <verify>
    <automated>bash -n spirit-room/base/entrypoint.sh && grep -q 'su - goku -c' spirit-room/base/entrypoint.sh && grep -q 'useradd' spirit-room/base/entrypoint.sh</automated>
  </verify>
  <what-built>entrypoint.sh の完全改修 (HOST_UID 受け取り / goku 冪等作成 / chown / kaio symlink chown / goku HOME git config / tmux を su - goku -c でラップ)。Plan 03 の CLI 改修はまだ入っていないため、手動で `docker run` に `-e HOST_UID=$(id -u) -e HOST_GID=$(id -g)` を渡して検証する。</what-built>

  <read_first>
    - spirit-room/base/entrypoint.sh (改修後)
    - spirit-room/build-base.sh
    - .planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-CONTEXT.md の <specifics> 検証手順 (L233-241)
  </read_first>

  <how-to-verify>
    ホスト側のターミナルで手動実行。

    1. ベースイメージを再ビルド:
       ```
       cd spirit-room && ./build-base.sh
       ```

    2. テスト用フォルダを作成して手動 docker run で起動:
       ```
       mkdir -p /tmp/spirit-room-phase5-test
       docker run -d --name spirit-room-phase5-test \
           --hostname spirit-room-phase5-test \
           -e ROOM_NAME=phase5-test \
           -e HOST_UID=$(id -u) \
           -e HOST_GID=$(id -g) \
           -p 2299:22 \
           -v /tmp/spirit-room-phase5-test:/workspace \
           -v spirit-room-auth:/root/.claude \
           -v spirit-room-opencode-auth:/root/.config/opencode \
           spirit-room-base:latest
       ```

    3. entrypoint ログを確認:
       ```
       docker logs spirit-room-phase5-test 2>&1 | head -30
       ```
       期待出力に含まれるもの:
       - `[INFO] Redis起動完了`
       - `[INFO] HOST_UID/GID=...:...`
       - `[INFO] goku ユーザーを作成 (UID=... GID=...)` (初回) or `[INFO] goku ユーザーは既に存在 (skip)` (2 回目以降)
       - `[INFO] /workspace と認証ボリュームを ...:... 所有に切替`
       - `[INFO] tmux 'spirit-room' 起動完了 (user=goku)`

    4. コンテナ内で goku として動いているか確認:
       ```
       docker exec spirit-room-phase5-test bash -c "ps -o user= -p \$(pgrep -f 'tmux new-session')"
       ```
       期待出力: `goku`

    5. /workspace への書込みが goku 所有になるか:
       ```
       docker exec -u goku spirit-room-phase5-test touch /workspace/test-phase5.txt
       ls -la /tmp/spirit-room-phase5-test/test-phase5.txt
       ```
       期待: ホスト側の自分のユーザー (`$(whoami)`) 所有で見える。`sudo chown` 不要で rm できる:
       ```
       rm /tmp/spirit-room-phase5-test/test-phase5.txt
       ```

    6. sudo 動作確認:
       ```
       docker exec -u goku spirit-room-phase5-test sudo whoami
       ```
       期待出力: `root`

    7. sshd_config 経由で goku SSH ログインが通るか:
       ```
       ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p 2299 goku@localhost whoami
       # パスワード: spiritroom
       ```
       期待出力: `goku`。root ログインは失敗することも確認:
       ```
       ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p 2299 root@localhost whoami
       # → "Permission denied" 相当
       ```

    8. (kaio 非回帰) 手動 kaio モードテスト:
       ```
       mkdir -p /tmp/spirit-room-phase5-kaio
       docker run -d --name spirit-room-phase5-kaio \
           -e ROOM_NAME=phase5-kaio \
           -e CLAUDE_CONFIG_DIR=/workspace/.claude-home \
           -e HOST_UID=$(id -u) \
           -e HOST_GID=$(id -g) \
           -p 2298:22 \
           -v /tmp/spirit-room-phase5-kaio:/workspace \
           -v spirit-room-auth:/root/.claude-shared \
           spirit-room-base:latest
       docker exec spirit-room-phase5-kaio ls -la /workspace/.claude-home/.credentials.json
       ```
       期待: symlink 表示で所有者が `$(id -un)` (自分の goku マッピング)、実体は `/root/.claude-shared/.credentials.json`。

    9. クリーンアップ:
       ```
       docker stop spirit-room-phase5-test spirit-room-phase5-kaio
       docker rm spirit-room-phase5-test spirit-room-phase5-kaio
       rm -rf /tmp/spirit-room-phase5-test /tmp/spirit-room-phase5-kaio
       ```

    問題なければ「approved」。問題があれば docker logs 出力を貼る。
  </how-to-verify>

  <resume-signal>「approved」と入力、または問題内容を記述</resume-signal>

  <done>
    - 再ビルド成功
    - コンテナ起動ログに goku 作成 / chown メッセージが出る
    - tmux プロセスが goku で走っている
    - /workspace への書込みがホスト側で自分のユーザー所有として見える (sudo chown 不要)
    - sudo が goku で NOPASSWD 動作
    - SSH が goku では通り、root では拒否される
    - kaio モードで credentials symlink が goku 所有になっている
  </done>
</task>

</tasks>

<verification>
### このプラン完了時の確認
- entrypoint.sh の改修が D-09 の順序を守っている (service → HOST_UID → goku 作成 → chown → kaio symlink + chown -h → goku git config → tmux を su - goku -c → tail -f は root)
- bash -n で構文エラーなし
- 手動 docker run での実機検証で goku 切替 + /workspace 所有権 + sudo + SSH 分岐が通る
- Phase 4 の kaio credentials symlink 設計が破壊されていない
- AUTH / LOOP 要件が非回帰 (共有ボリュームは破棄せず chown で吸収)
</verification>

<success_criteria>
- [ ] 現行コード上のすべての grep acceptance_criteria が通る
- [ ] 実機で goku ユーザーに切替わり /workspace がホスト自分ユーザー所有に見える
- [ ] sudo が NOPASSWD で動作
- [ ] SSH 分岐 (goku 成功 / root 拒否) が効く
- [ ] kaio モードの credentials symlink が goku 所有
</success_criteria>

<output>
完了後、`.planning/phases/05-goku-uid-gid-root-workspace-sudo-chown-entrypoint-sh-host-ui/05-02-SUMMARY.md` を生成する。SUMMARY には以下を含めること:
- entrypoint.sh の挿入位置と差分サマリ (Task 2.1 + 2.2)
- 実機検証で確認したユーザー切替、所有権、SSH 挙動
- gosu 切替が必要になったかどうか (su - goku -c で問題なく動いたか)
- Plan 03 へ引き継ぐ前提 (CLI が -e HOST_UID を渡せば entrypoint は期待動作する)
</output>
