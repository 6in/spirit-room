---
phase: 04-gsd-claude-config-dir-symlink-gsd-autonomous
plan: 05
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
  - "04-04"
files_modified:
  - spirit-room/base/Dockerfile
autonomous: false
requirements:
  - KAIO-E2E-01

must_haves:
  truths:
    - "ベースイメージが再ビルドされ、新しい entrypoint.sh / start-training-kaio.sh / KAIO-MISSION.md.template を含む"
    - "Mr.ポポ経由で界王星モードを選び、KAIO-MISSION.md が生成される"
    - "`spirit-room kaio <folder>` が起動し、CLAUDE_CONFIG_DIR と認証 symlink が正しく設定されている"
    - "部屋内で `claude -p 'hello'` が認証エラーなく通る"
    - "start-training-kaio が GSD をインストールし、/gsd-new-project → /gsd-autonomous を非対話で回し、.kaio-done が作られる"
    - "`.planning/` 配下に GSD の成果物 (ROADMAP.md / REQUIREMENTS.md / phases/) が生成されている"
  artifacts:
    - path: "~/projects/kaio-smoke-test/KAIO-MISSION.md"
      provides: "E2E 検証用の trivial な界王星ミッション"
    - path: "~/projects/kaio-smoke-test/.kaio-done"
      provides: "完走フラグ"
    - path: "~/projects/kaio-smoke-test/.planning/ROADMAP.md"
      provides: "GSD が作った ROADMAP"
  key_links:
    - from: "Mr.ポポ (spirit-room-manager)"
      to: "spirit-room kaio ~/projects/kaio-smoke-test"
      via: "モード選択→KAIO-MISSION.md→kaio 起動"
      pattern: "spirit-room kaio"
    - from: "start-training-kaio"
      to: ".planning/ + .kaio-done"
      via: "非対話 GSD チェーン完走"
      pattern: "\\.kaio-done"
---

<objective>
Phase 4 全体を貫通する E2E 検証。ベースイメージ再ビルド → Mr.ポポで界王星モード選択 → 部屋起動 → 非対話 GSD チェーン完走 → 成果物確認までを 1 本のシナリオで確認する。

Purpose: Phase 4 の「成功条件」(CONTEXT decisions 最後のブロック 1〜6) を全て手動で検証し、本当に動くことを保証する。

Output: 動作ログ (progress.log), `.kaio-done` フラグ, `.planning/` 配下のアーティファクト, チェックポイント検証レポート。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-01-PLAN.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-02-PLAN.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-03-PLAN.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-04-PLAN.md
@spirit-room/base/Dockerfile
@spirit-room/build-base.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: ベースイメージを再ビルドし、Dockerfile に新スクリプトが含まれることを確認</name>
  <files>spirit-room/base/Dockerfile</files>
  <action>
`spirit-room/base/Dockerfile` を Read して、`COPY scripts/ /room/scripts/` (または同等の行) が存在し、`start-training-kaio.sh` と `KAIO-MISSION.md.template` がビルド時に自動的に含まれることを確認せよ。

もし COPY がファイル指定で `start-training.sh` と `MISSION.md.template` だけを個別にコピーしている場合は、ディレクトリ単位コピーに書き換えるか、新ファイル 2 つを個別に追加するかのどちらかを行え。

また `chmod +x` が `/room/scripts/*.sh` に適用されているかも確認し、新スクリプトが実行可能になるようにせよ。

最終的に PATH に `/room/scripts` が入っていて `start-training-kaio` コマンドが叩けるか、それとも `/usr/local/bin/start-training-kaio` へのシンボリックリンクが必要か確認。既存の `start-training` がどう見えるようになっているかを真似る。

再ビルド:
```bash
cd spirit-room && ./build-base.sh
```

ビルドが成功したら、以下を確認:
```bash
docker run --rm --entrypoint /bin/bash spirit-room-base:latest -c 'ls -la /room/scripts/start-training-kaio.sh /room/scripts/KAIO-MISSION.md.template && which start-training-kaio'
```

両ファイルが存在し、`start-training-kaio` が PATH で解決できること。
  </action>
  <verify>
    <automated>
docker image inspect spirit-room-base:latest > /dev/null && \
docker run --rm --entrypoint /bin/bash spirit-room-base:latest -c 'test -f /room/scripts/start-training-kaio.sh && test -f /room/scripts/KAIO-MISSION.md.template && which start-training-kaio'
    </automated>
  </verify>
  <done>
    - spirit-room-base:latest イメージがビルドされている
    - 新スクリプトがイメージ内に存在する
    - start-training-kaio が PATH で解決する (または /room/scripts を直接叩ける)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Mr.ポポ経由の E2E 手動検証</name>
  <what-built>
Phase 4 全体 — 界王星モード 1 本のフロー:
1. Plan 01: entrypoint の CLAUDE_CONFIG_DIR 分岐 + symlink
2. Plan 02: spirit-room kaio サブコマンド
3. Plan 03: KAIO-MISSION.md.template
4. Plan 04: start-training-kaio.sh + tmux 分岐 + Mr.ポポ Step 0
5. Plan 05 Task 1: 再ビルド済みイメージ
  </what-built>
  <how-to-verify>
## 検証シナリオ (trivial な界王星ミッション)

### 前提確認
```bash
# イメージが最新か
docker images spirit-room-base:latest

# 認証ボリュームに credentials があるか
docker run --rm -v spirit-room-auth:/chk alpine ls -la /chk/.credentials.json || \
  spirit-room auth  # 未整備なら実行
```

### Step A: Mr.ポポで界王星ミッション作成
```bash
cd spirit-room-manager
claude   # Mr.ポポセッション開始
```

Mr.ポポに対して:
1. 初回挨拶「よく来たな。ここは精神と時の部屋だ。」が出ること
2. **Step 0 のモード選択質問が出ること**を確認
3. 「2. 界王星」と答える
4. trivial なミッションをヒアリング通り回答 (例: 「現在日時と uptime を表示する bash スクリプト `today.sh` を作る。pytest 相当のテストも書く」)
5. Mr.ポポが `~/projects/kaio-smoke-test/KAIO-MISSION.md` を生成する
6. Mr.ポポが `spirit-room kaio ~/projects/kaio-smoke-test` を実行する
7. `spirit-room list` で `spirit-room-kaio-smoke-test-kaio` (または類似名) が出る

### Step B: 部屋内の認証と起動確認
```bash
# ホストから:
spirit-room list   # 界王星コンテナが見える
docker exec spirit-room-kaio-smoke-test-kaio bash -c 'echo $CLAUDE_CONFIG_DIR'
# → /workspace/.claude-home が出る

docker exec spirit-room-kaio-smoke-test-kaio bash -c 'ls -la $CLAUDE_CONFIG_DIR/.credentials.json'
# → symlink で /root/.claude-shared/.credentials.json を指している

docker exec spirit-room-kaio-smoke-test-kaio bash -c 'CLAUDE_CONFIG_DIR=/workspace/.claude-home claude -p "say hello in one word"'
# → 認証エラーなく "hello" 等の応答が返る
```

### Step C: 非対話 GSD チェーン観察
```bash
spirit-room logs ~/projects/kaio-smoke-test
# → GSD インストール → /gsd-new-project → /gsd-autonomous の進行が見える
```

数分〜十数分放置。

### Step D: 完走確認
```bash
test -f ~/projects/kaio-smoke-test/.kaio-done && echo "DONE"
test -f ~/projects/kaio-smoke-test/.kaio-prepared && echo "PREPARED"
ls ~/projects/kaio-smoke-test/.planning/
cat ~/projects/kaio-smoke-test/.planning/ROADMAP.md 2>/dev/null
cd ~/projects/kaio-smoke-test && git tag --list
# → v1.0 などタグが見える
test -f ~/projects/kaio-smoke-test/today.sh && bash today.sh
```

### Step E: 後方互換回帰チェック
```bash
# 既存の精神と時の部屋モードが壊れていないことを確認
spirit-room open /tmp/legacy-check   # 別フォルダで open (事前に MISSION.md 置く)
docker exec spirit-room-legacy-check bash -c 'echo "${CLAUDE_CONFIG_DIR:-UNSET}"'
# → UNSET が出る (界王星分岐に入っていない)
spirit-room close /tmp/legacy-check
```

### 期待する結果
- モード選択が出る
- 界王星コンテナに CLAUDE_CONFIG_DIR と symlink が正しく設定されている
- claude -p が認証通る
- `.kaio-done` と `.planning/` 成果物が揃う
- 精神と時の部屋モードは挙動が変わっていない
  </how-to-verify>
  <resume-signal>
検証が通ったら "approved" と返答。問題があれば、どのステップで何が起きたかを報告。
問題の内容により Plan 01-04 の修正プランを追加する必要があるかもしれない。
  </resume-signal>
</task>

</tasks>

<verification>
- すべての Step (A〜E) が期待通り
- CONTEXT.md の decisions セクション「成功条件 (Phase 4 としての)」の 1〜6 すべて満たす
- 既存 spirit-room open 経路が回帰していない
</verification>

<success_criteria>
Phase 4 全体完了:
1. ✅ spirit-room kaio でコンテナ起動、CLAUDE_CONFIG_DIR=/workspace/.claude-home 設定済み
2. ✅ entrypoint が credentials.json を symlink し、claude -p が認証通る
3. ✅ 初回起動で GSD が CLAUDE_CONFIG_DIR 配下にインストールされる
4. ✅ start-training-kaio が KAIO-MISSION.md を読み、/gsd-new-project → /gsd-autonomous を非対話実行し .done を作る
5. ✅ Mr.ポポが界王星モードを選べる
6. ✅ E2E: Mr.ポポ → KAIO-MISSION.md → spirit-room kaio → 完走 → .planning/ 成果物あり
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-05-SUMMARY.md`
</output>
