---
phase: 04-gsd-claude-config-dir-symlink-gsd-autonomous
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/spirit-room
autonomous: true
requirements:
  - KAIO-CLI-01

must_haves:
  truths:
    - "ユーザーは `spirit-room kaio [folder]` で界王星モードの部屋を起動できる"
    - "界王星モードの部屋は CLAUDE_CONFIG_DIR=/workspace/.claude-home 環境変数を持つ"
    - "界王星モードの部屋は共有認証ボリュームを /root/.claude-shared にマウントする"
    - "既存の `spirit-room open` / enter / list / close / logs / auth は一切挙動が変わらない"
    - "`spirit-room kaio` と `spirit-room open` は別コンテナ名になるか、同じフォルダでも共存/切替できる方針が明示されている"
  artifacts:
    - path: "spirit-room/spirit-room"
      provides: "cmd_kaio サブコマンド"
      contains: "cmd_kaio"
  key_links:
    - from: "spirit-room/spirit-room"
      to: "docker run"
      via: "cmd_kaio 内の docker run 呼び出し"
      pattern: "CLAUDE_CONFIG_DIR=/workspace/.claude-home"
---

<objective>
ホスト CLI `spirit-room` に `kaio` サブコマンドを追加し、界王星モードの部屋を起動できるようにする。`cmd_open` と極力共通化しつつ、以下の点だけ差し替える:

1. `-e CLAUDE_CONFIG_DIR=/workspace/.claude-home`
2. 共有認証ボリュームのマウント先を `/root/.claude-shared` に変更 (既存の `/root/.claude` 直接マウントは使わない)
3. 部屋内での起動メッセージを界王星モード仕様に
4. コンテナ名プレフィックスで既存 `spirit-room open` と衝突しない (例: `kaio-` prefix、または末尾サフィックス `-kaio`)

Purpose: 既存の精神と時の部屋フローと完全に分離したまま、界王星モード専用の起動経路を提供する (CONTEXT D: 実行経路の分岐)。

Output: `spirit-room/spirit-room` に `cmd_kaio()` 追加 + dispatcher 追加 + usage 更新。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md
@CLAUDE.md
@spirit-room/spirit-room

# 既存の cmd_open (lines 50-91) を壊さず、別関数 cmd_kaio として追加すること。
# find_free_port() / folder_to_name() などのヘルパーは流用する。
# AUTH_VOLUME (spirit-room-auth) は共用してよいが、マウント先パスだけ変える。
</context>

<tasks>

<task type="auto">
  <name>Task 1: cmd_kaio 関数と dispatcher を追加</name>
  <files>spirit-room/spirit-room</files>
  <action>
`spirit-room/spirit-room` の `cmd_open()` の直後に `cmd_kaio()` 関数を追加せよ。基本構造は `cmd_open` のコピーだが、以下だけ違う:

1. **コンテナ名**: `folder_to_name` の結果に `-kaio` サフィックスを付ける。既存の `spirit-room open` とコンテナ名が衝突しないようにする:
   ```bash
   local name="$(folder_to_name "$folder")-kaio"
   ```

2. **docker run の差分**:
   - `-e CLAUDE_CONFIG_DIR=/workspace/.claude-home` を追加
   - 共有認証ボリュームを `/root/.claude-shared` にマウント (従来の `/root/.claude` は使わない):
     `-v "${AUTH_VOLUME}:/root/.claude-shared"`
   - opencode 認証ボリュームは界王星モードでは Phase 4 スコープ外 (deferred)。**マウントしない**
   - `-v "${HOME}/.claude.json:/root/.claude.json:ro"` は現状の POC では必須ではないので**外す** (deferred セクションで .claude.json 共有は後回しと明記)

3. **起動メッセージ**: 「部屋を開く」ではなく「界王星を開く (重力10倍)」など世界観に沿った表示にする。CLAUDE.md のナーミング規約 (Dragon Ball) を守ること。

4. **ガード**: 既存の `spirit-room open` で開いた同一フォルダのコンテナ (名前サフィックスなし) は検知しつつ、界王星コンテナの重複起動だけ弾けば良い。

5. `usage()` 関数に `spirit-room kaio [フォルダ]  界王星を開く（GSD駆動の本格開発モード）` の1行を追加。

6. メインの `case` 文 (L177-185) に `kaio) cmd_kaio "${2:-}" ;;` を追加。

**制約:**
- `cmd_open` の中身は 1 行も書き換えない (純粋に追記のみ)
- 既存の `AUTH_VOLUME` 定数は流用する (共有)
- `find_free_port()`, `folder_to_name()` はそのまま再利用
- bash の関数スタイル・インデント (4スペース)・メッセージ形式 (`[INFO]` / `[ERROR]` prefix) を守る
  </action>
  <verify>
    <automated>
bash -n spirit-room/spirit-room && \
grep -q 'cmd_kaio' spirit-room/spirit-room && \
grep -q 'CLAUDE_CONFIG_DIR=/workspace/.claude-home' spirit-room/spirit-room && \
grep -q '/root/.claude-shared' spirit-room/spirit-room && \
grep -q 'kaio)' spirit-room/spirit-room && \
grep -q 'kaio' spirit-room/spirit-room | head -1
    </automated>
  </verify>
  <done>
    - `bash -n` が通る
    - `cmd_kaio` 関数と `kaio)` case ブランチが存在する
    - `CLAUDE_CONFIG_DIR=/workspace/.claude-home` と `/root/.claude-shared` マウントが含まれる
    - `cmd_open` の実装は 1 行も変更されていない (`diff` で確認可能)
    - usage に kaio の説明行が追加されている
  </done>
</task>

</tasks>

<verification>
- `spirit-room` コマンド単体で `spirit-room` と叩くと usage が出て、その中に kaio の説明行が見える
- `bash -n` で syntax error なし
- `grep -c 'cmd_open' spirit-room/spirit-room` の結果が変更前と同じ (破壊なしの確認)
</verification>

<success_criteria>
1. `spirit-room kaio [folder]` が新しいコンテナを起動でき、`docker inspect` で `CLAUDE_CONFIG_DIR=/workspace/.claude-home` が見える
2. 共有認証ボリュームが `/root/.claude-shared` にマウントされており、Plan 01 の entrypoint 分岐と整合する
3. 既存の `spirit-room open` 経路は一切影響を受けない
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-02-SUMMARY.md`
</output>
