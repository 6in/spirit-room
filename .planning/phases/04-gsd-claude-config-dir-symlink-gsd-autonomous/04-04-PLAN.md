---
phase: 04-gsd-claude-config-dir-symlink-gsd-autonomous
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - spirit-room/base/scripts/start-training-kaio.sh
  - spirit-room/base/entrypoint.sh
  - spirit-room-manager/skills/MR_POPO.md
autonomous: true
requirements:
  - KAIO-TRAINING-LOOP-01
  - KAIO-MRPOPO-MODE-01

must_haves:
  truths:
    - "start-training-kaio.sh が存在し、実行すると KAIO-MISSION.md を読んで GSD 駆動の非対話チェーンを開始する"
    - "初回実行時に GSD が CLAUDE_CONFIG_DIR 配下に自動インストールされる (既にあれば skip)"
    - "`.kaio-prepared` (GSD インストール済) と `.kaio-done` (完走) の2フラグで idempotency を担保する"
    - "entrypoint.sh は CLAUDE_CONFIG_DIR 設定時、tmux の training ペインで `start-training-kaio` を自動起動する"
    - "Mr.ポポは最初のヒアリングで『精神と時の部屋 / 界王星』のモード選択を行い、モードに応じて生成物と起動コマンドを切り替える"
    - "既存 start-training.sh は一切変更されていない"
  artifacts:
    - path: "spirit-room/base/scripts/start-training-kaio.sh"
      provides: "界王星モード training loop"
      min_lines: 50
      contains: "CLAUDE_CONFIG_DIR"
    - path: "spirit-room-manager/skills/MR_POPO.md"
      provides: "モード分岐 Step 0 を含むヒアリング手順"
      contains: "界王星"
    - path: "spirit-room/base/entrypoint.sh"
      provides: "界王星モード時の tmux 起動分岐"
      contains: "start-training-kaio"
  key_links:
    - from: "start-training-kaio.sh"
      to: "claude -p"
      via: "非対話モード指示を含むプロンプト"
      pattern: "permission-mode bypassPermissions"
    - from: "start-training-kaio.sh"
      to: "/workspace/.kaio-done"
      via: "タグ作成 or /gsd-autonomous 完走検知"
      pattern: "\\.kaio-done"
    - from: "spirit-room-manager/skills/MR_POPO.md"
      to: "spirit-room kaio"
      via: "界王星モード選択時の起動コマンド"
      pattern: "spirit-room kaio"
---

<objective>
界王星モードの実働部分を作る:

1. `start-training-kaio.sh` — 非対話で GSD チェーンを回す training ループ
2. `entrypoint.sh` に tmux 起動時の界王星モード分岐を追加 (Plan 01 で入れた CLAUDE_CONFIG_DIR 分岐の続き)
3. `spirit-room-manager/skills/MR_POPO.md` にモード選択 Step 0 を追加

Purpose: Phase 4 の頭脳部分。POC で検証済のプロンプト (CONTEXT specifics 参照) を正式にスクリプト化し、Mr.ポポが界王星モードを選んだユーザーに対して正しいフローを案内できるようにする。

Output: training-kaio スクリプト、entrypoint.sh の tmux ブロックに分岐追加、Mr.ポポスキルに Step 0 (モード選択) 追加。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-01-PLAN.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-02-PLAN.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-03-PLAN.md
@CLAUDE.md
@spirit-room/base/scripts/start-training.sh
@spirit-room/base/entrypoint.sh
@spirit-room-manager/skills/MR_POPO.md
@spirit-room-manager/CLAUDE.md
@spirit-room/base/scripts/KAIO-MISSION.md.template

# POC で使った具体プロンプトは 04-CONTEXT.md の `<specifics>` セクションに原文あり。そのまま流用可。
# start-training.sh の構造 (log / run_claude / フラグで while ループ) を真似るが、**別ファイル**として新規作成する。
# 既存 start-training.sh は絶対に変更しない。
</context>

<tasks>

<task type="auto">
  <name>Task 1: start-training-kaio.sh を新規作成</name>
  <files>spirit-room/base/scripts/start-training-kaio.sh</files>
  <action>
`spirit-room/base/scripts/start-training-kaio.sh` を新規作成せよ。構造:

```bash
#!/bin/bash
set -e

LOG_DIR="/workspace/.logs"
LOG_FILE="$LOG_DIR/progress.log"
MISSION_FILE="/workspace/KAIO-MISSION.md"
PREPARED_FLAG="/workspace/.kaio-prepared"
DONE_FLAG="/workspace/.kaio-done"

# CLAUDE_CONFIG_DIR は entrypoint で設定されている前提。念のため default を用意
: "${CLAUDE_CONFIG_DIR:=/workspace/.claude-home}"
export CLAUDE_CONFIG_DIR

mkdir -p "$LOG_DIR" "$CLAUDE_CONFIG_DIR"

log() { echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" | tee -a "$LOG_FILE"; }

# ── KAIO-MISSION.md 存在確認 ─────────────────────────────
if [ ! -f "$MISSION_FILE" ]; then
    log "[ERROR] KAIO-MISSION.md が見つからない: $MISSION_FILE"
    log "  テンプレート: /room/scripts/KAIO-MISSION.md.template"
    exit 1
fi

# ════════════════════════════════════════════════════════
# PHASE KAIO-1: GSD インストール (idempotent)
# ════════════════════════════════════════════════════════
log "=== 界王星 PHASE 1: GSD セットアップ ==="
if [ -f "$PREPARED_FLAG" ]; then
    log "GSD セットアップ済、スキップ"
else
    log "CLAUDE_CONFIG_DIR=$CLAUDE_CONFIG_DIR に GSD をインストール中..."
    # POC で検証済: 73 skills + get-shit-done + hooks + settings.json が配置される
    CLAUDE_CONFIG_DIR="$CLAUDE_CONFIG_DIR" npx -y get-shit-done-cc@latest 2>&1 | tee -a "$LOG_FILE"
    touch "$PREPARED_FLAG"
    log "GSD セットアップ完了"
fi

# ════════════════════════════════════════════════════════
# PHASE KAIO-2: 非対話で /gsd-new-project → /gsd-autonomous
# ════════════════════════════════════════════════════════
log "=== 界王星 PHASE 2: GSD 駆動チェーン ==="

while true; do
    [ -f "$DONE_FLAG" ] && { log "修行完了済み"; break; }

    log "claude -p で /gsd-new-project → /gsd-autonomous を非対話起動"

    # POC validated prompt (04-CONTEXT.md specifics セクション原文を使用)
    CLAUDE_CONFIG_DIR="$CLAUDE_CONFIG_DIR" claude \
        --permission-mode bypassPermissions \
        -p "Context: This is a POC verifying whether GSD workflows can run end-to-end without human input, from inside an automated training environment. You are running via 'claude -p' so there is NO human available to answer AskUserQuestion prompts — any question you ask will just block.

Task: Read /workspace/KAIO-MISSION.md. Run /gsd-new-project using the mission content as answers. Then run /gsd-autonomous to completion.

Rules:
- Never ask the user anything. For any question the skill would normally ask, pick the recommended/default answer and keep going.
- Prefer the simplest, shortest path to a completed phase.
- If a step genuinely cannot proceed without input, make the most reasonable assumption, log it in a comment, and continue.

Completion signal: once /gsd-autonomous has produced a git tag (e.g. v1.0) for the completed milestone, write an empty file /workspace/.kaio-done and exit." \
        2>&1 | tee -a "$LOG_FILE"

    if [ -f "$DONE_FLAG" ]; then
        log "界王星修行完了"
        break
    fi

    # タグがあれば完了とみなす (ラッパー保険)
    if cd /workspace && git tag --list | grep -qE '^v[0-9]'; then
        log "git tag 検知、.kaio-done を作成"
        touch "$DONE_FLAG"
        break
    fi

    log "未完了、リトライ..."
    sleep 5
done

echo "
╔══════════════════════════════════════════════╗
║       界王星修行完了！重力10倍を乗り越えた  ║
╚══════════════════════════════════════════════╝
" | tee -a "$LOG_FILE"
```

さらに `chmod +x` 相当の shebang + Dockerfile 側で実行権限が付与されるか確認 (既存の scripts と同じディレクトリなので、ビルド時の COPY/chmod で引き継がれるはずだが、必要なら Dockerfile の `chmod +x /room/scripts/*.sh` 行を確認)。

**制約:**
- POC で検証済のプロンプト本文は 04-CONTEXT.md specifics セクションに書かれているものをベースに、末尾の「Completion signal」部分だけ追記する
- `start-training.sh` は 1 行も変更しない
- エラー時の sleep は 5 秒 (既存 start-training.sh と同程度)
- `.kaio-prepared` / `.kaio-done` の 2 フラグで idempotency を担保
  </action>
  <verify>
    <automated>
test -f spirit-room/base/scripts/start-training-kaio.sh && \
bash -n spirit-room/base/scripts/start-training-kaio.sh && \
grep -q 'CLAUDE_CONFIG_DIR' spirit-room/base/scripts/start-training-kaio.sh && \
grep -q 'get-shit-done-cc' spirit-room/base/scripts/start-training-kaio.sh && \
grep -q 'gsd-new-project' spirit-room/base/scripts/start-training-kaio.sh && \
grep -q 'gsd-autonomous' spirit-room/base/scripts/start-training-kaio.sh && \
grep -q 'bypassPermissions' spirit-room/base/scripts/start-training-kaio.sh && \
grep -q '\.kaio-done' spirit-room/base/scripts/start-training-kaio.sh && \
grep -q '\.kaio-prepared' spirit-room/base/scripts/start-training-kaio.sh && \
diff spirit-room/base/scripts/start-training.sh spirit-room/base/scripts/start-training.sh > /dev/null
    </automated>
  </verify>
  <done>
    - start-training-kaio.sh が存在し、bash -n 通過
    - 2フラグ (.kaio-prepared / .kaio-done) が実装されている
    - POC プロンプトが埋め込まれている
    - start-training.sh は未変更
  </done>
</task>

<task type="auto">
  <name>Task 2: entrypoint.sh の tmux 起動部分に界王星モード分岐を追加</name>
  <files>spirit-room/base/entrypoint.sh</files>
  <action>
`spirit-room/base/entrypoint.sh` の tmux ペイン起動ブロック (現状 L42-47):

```bash
tmux rename-window -t "$SESSION:0" "training"
if [ -f /workspace/MISSION.md ] && [ ! -f /workspace/.done ]; then
    tmux send-keys -t "$SESSION:training" "start-training" C-m
else
    tmux send-keys -t "$SESSION:training" \
        "echo '部屋[$ROOM_NAME] 準備完了 | start-training で修行開始 | status で確認'" C-m
fi
```

を、以下のように界王星モード分岐を追加した形に書き換えよ:

```bash
tmux rename-window -t "$SESSION:0" "training"
if [ -n "${CLAUDE_CONFIG_DIR:-}" ] && [ -f /workspace/KAIO-MISSION.md ] && [ ! -f /workspace/.kaio-done ]; then
    # 界王星モード: GSD 駆動チェーン
    tmux send-keys -t "$SESSION:training" "start-training-kaio" C-m
elif [ -f /workspace/MISSION.md ] && [ ! -f /workspace/.done ]; then
    # 精神と時の部屋モード (既存)
    tmux send-keys -t "$SESSION:training" "start-training" C-m
else
    tmux send-keys -t "$SESSION:training" \
        "echo '部屋[$ROOM_NAME] 準備完了 | start-training(-kaio) で修行開始 | status で確認'" C-m
fi
```

**制約:**
- 精神と時の部屋モードの分岐 (elif) は既存の挙動と等価であること (CLAUDE_CONFIG_DIR 未設定時は界王星ブランチに入らないので、従来通り `start-training` が起動する)
- else の echo メッセージだけ微修正可 (hint を追加)
- Plan 01 で追加した CLAUDE_CONFIG_DIR 分岐ブロックとは別の場所 (tmux ブロック内) なので競合しない
  </action>
  <verify>
    <automated>
bash -n spirit-room/base/entrypoint.sh && \
grep -q 'start-training-kaio' spirit-room/base/entrypoint.sh && \
grep -q 'KAIO-MISSION.md' spirit-room/base/entrypoint.sh && \
grep -q '.kaio-done' spirit-room/base/entrypoint.sh
    </automated>
  </verify>
  <done>
    - entrypoint.sh の tmux ブロックに界王星モード分岐が追加されている
    - 精神と時の部屋モード (CLAUDE_CONFIG_DIR 未設定) の動作パスが従来と等価
    - bash -n 通過
  </done>
</task>

<task type="auto">
  <name>Task 3: Mr.ポポスキルにモード選択 Step 0 を追加</name>
  <files>spirit-room-manager/skills/MR_POPO.md</files>
  <action>
`spirit-room-manager/skills/MR_POPO.md` の「ヒアリング手順」セクションの先頭に **Step 0: モード選択** を追加せよ。既存の Step 1 / Step 2 / Step 3 (POC 向けヒアリング) は、精神と時の部屋モード選択時のフローとして残す。界王星モード選択時は別フローに分岐する。

追加内容の指針:

```markdown
### Step 0: モードの選択 (最初に必ず聞け)

まずユーザーに次のように問え:

> 修行には 2 つのモードがある。どちらを選ぶか:
>
> 1. **精神と時の部屋** (POC速攻型) — 時間を圧縮して素振り量を稼ぐ。フレームワークを触って動く POC が欲しいとき
> 2. **界王星** (重力10倍) — 基礎から段階的に本格実装したい。GSD ワークフローで requirements → phases → verify → tag まで回す

- **「1. 精神と時の部屋」選択時**: 下記 Step 1 → Step 2 → Step 3 の既存ヒアリングへ進む。最終的に `MISSION.md` を生成し `spirit-room open` で起動する。
- **「2. 界王星」選択時**: 下記 **界王星ヒアリング** へ進む。最終的に `KAIO-MISSION.md` を生成し `spirit-room kaio` で起動する。

---

### 界王星ヒアリング (Step 0 で 界王星を選んだときのみ)

`/room/scripts/KAIO-MISSION.md.template` (部屋内) または `spirit-room/base/scripts/KAIO-MISSION.md.template` (リポジトリ) を参考に、次を順に聞け:

#### K1. プロジェクトの目的
- 何を作るか (一言で)
- 誰のどんな問題を解くか

#### K2. 機能要件 (大まか)
- 3〜5 個の機能を箇条書きで。GSD が細分化してくれるので粒度はざっくりで OK

#### K3. フェーズ分割の示唆 (任意)
- Phase 1 / Phase 2 / Phase 3 と段階を意識したヒントがあれば。なければ GSD 任せで良い

#### K4. 成功条件
- テスト可能なコマンドの形で (例: `pytest -q` が pass, `git tag v1.0` が存在する)

#### K5. 制約
- 言語・ランタイム / 外部 API / 依存追加方針

ヒアリング完了後:
1. フォルダ名を提案 (命名規則: `kaio-[プロジェクト名]`)
2. `~/projects/[フォルダ名]/` を作成し `KAIO-MISSION.md` を生成
3. `spirit-room kaio ~/projects/[フォルダ名]` を実行
4. 起動確認: `spirit-room list` に `spirit-room-[フォルダ名]-kaio` が出る
5. 報告: フォルダ / コンテナ / ログコマンド (`spirit-room logs ~/projects/[フォルダ名]`)
```

**制約:**
- 既存の Step 1 / Step 2 / Step 3 / MISSION.md 生成ルール / 部屋の起動手順は残す (精神と時の部屋モード専用として残す)
- 挨拶「よく来たな。ここは精神と時の部屋だ。」は変えない (CLAUDE.md で固定されている)
- 日本語一次
- CONTEXT.md decisions 「Mr.ポポのヒアリング分岐」に沿う
  </action>
  <verify>
    <automated>
grep -q 'Step 0' spirit-room-manager/skills/MR_POPO.md && \
grep -q '界王星' spirit-room-manager/skills/MR_POPO.md && \
grep -q 'spirit-room kaio' spirit-room-manager/skills/MR_POPO.md && \
grep -q 'KAIO-MISSION' spirit-room-manager/skills/MR_POPO.md && \
grep -q 'よく来たな' spirit-room-manager/skills/MR_POPO.md && \
grep -q 'Step 1' spirit-room-manager/skills/MR_POPO.md && \
grep -q 'Step 3' spirit-room-manager/skills/MR_POPO.md
    </automated>
  </verify>
  <done>
    - Step 0 が追加され、モード選択質問になっている
    - 界王星ヒアリング (K1-K5) が追加されている
    - 既存 Step 1/2/3 と挨拶行は保持されている
    - `spirit-room kaio` コマンドへの分岐が明示されている
  </done>
</task>

</tasks>

<verification>
- `bash -n` で start-training-kaio.sh と entrypoint.sh が通る
- `git diff spirit-room/base/scripts/start-training.sh` が空 (未変更)
- Mr.ポポスキルを読み返して、モード選択 → 界王星ヒアリング → spirit-room kaio の流れが一貫している
</verification>

<success_criteria>
1. `start-training-kaio.sh` が GSD インストール → claude -p 非対話実行 → .kaio-done 作成を実装している
2. entrypoint が CLAUDE_CONFIG_DIR + KAIO-MISSION.md を検出したとき自動で start-training-kaio を起動する
3. Mr.ポポが最初のヒアリングでモード選択を行い、界王星選択時に KAIO-MISSION.md と `spirit-room kaio` に分岐する
4. 既存の精神と時の部屋フローは一切変わっていない
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-04-SUMMARY.md`
</output>
