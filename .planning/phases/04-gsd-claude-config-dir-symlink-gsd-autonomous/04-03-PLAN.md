---
phase: 04-gsd-claude-config-dir-symlink-gsd-autonomous
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/scripts/KAIO-MISSION.md.template
autonomous: true
requirements:
  - KAIO-MISSION-TEMPLATE-01

must_haves:
  truths:
    - "KAIO-MISSION.md.template が存在し、Mr.ポポが界王星モードで参照できる"
    - "テンプレートは既存 MISSION.md.template とは別物で、GSD 駆動の段階開発向けの追加項目を含む"
    - "追加項目: 要件の粒度ヒント / フェーズ分割の示唆 / 成功条件の具体性"
    - "既存 MISSION.md.template は一切変更されていない"
  artifacts:
    - path: "spirit-room/base/scripts/KAIO-MISSION.md.template"
      provides: "界王星モードの KAIO-MISSION.md テンプレート"
      min_lines: 40
      contains: "要件"
  key_links:
    - from: "KAIO-MISSION.md.template"
      to: "GSD /gsd-new-project の事前回答"
      via: "Mr.ポポが生成したインスタンスを start-training-kaio が claude -p プロンプトに埋め込む"
      pattern: "KAIO-MISSION"
---

<objective>
界王星モード向けの `KAIO-MISSION.md.template` を新規作成する。既存の `MISSION.md.template` (POC速攻型) とは別物として、GSD 駆動の本格開発向けに「要件の粒度ヒント」「フェーズ分割の示唆」「テスト可能な成功条件」を追加項目として持つ。

Purpose: Mr.ポポが界王星モードのヒアリングで使うテンプレートを確定させ、かつ `start-training-kaio.sh` が `/gsd-new-project` の事前回答として流用できる構造にする (CONTEXT D: KAIO-MISSION.md テンプレート)。

Output: `spirit-room/base/scripts/KAIO-MISSION.md.template` の新規作成。日本語・project 憲法に沿った命名。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md
@CLAUDE.md
@spirit-room/base/scripts/MISSION.md.template

# 既存 MISSION.md.template はそのまま残すこと。全く別のファイルとして新規作成する。
# 日本語一次、GSD の requirements / phase 分解の思想を内包する。
</context>

<tasks>

<task type="auto">
  <name>Task 1: KAIO-MISSION.md.template を新規作成</name>
  <files>spirit-room/base/scripts/KAIO-MISSION.md.template</files>
  <action>
`spirit-room/base/scripts/KAIO-MISSION.md.template` を新規作成せよ。以下のセクション構成 (日本語) を持つこと:

```markdown
# KAIO-MISSION: [プロジェクト名]

> 界王星モードの修行課題。GSD (Get Shit Done) ワークフローで段階開発を進める前提で書くこと。

## 修行スタイル

この部屋は **界王星モード** だ。重力10倍で基礎から鍛え上げる。
- 実行エンジン: GSD (/gsd-new-project → /gsd-autonomous) の非対話チェーン
- 進行方式: requirements → phases → plans → execute → verify → audit → complete → tag を自動で回す
- 成果物: タグ付きリリース (例: v1.0) まで到達する

## プロジェクトの目的

[何を作るか、一言で]

例: CLI 体重管理ツール「weight-log」を、SQLite バックエンド + pytest カバレッジ付きで実装する

---

## 背景・達成したい価値

- [誰のどんな問題を解くか]
- [このプロジェクトで理解・確立したいこと]

---

## 機能要件 (大まか)

GSD がこれを分解して detailed requirements と phases に落とし込むので、ユーザーはざっくりで良い。

- [ ] [機能1]
- [ ] [機能2]
- [ ] [機能3]

### 要件の粒度ヒント

- 「ログイン機能」は大きすぎる → 「パスワード認証のログイン」「セッションCookie維持」くらいに割る
- 逆に「ボタンの色を青に」は細かすぎる → 機能単位まで抽象化する
- 困ったら GSD の提案デフォルトに任せてよい (非対話モードなので自動で最適化される)

---

## フェーズ分割の示唆 (任意)

どう段階的に作りたいか、ヒントがあれば書く。なければ GSD が勝手に分解する。

- Phase 1: [基盤 — 例: DB schema + migration]
- Phase 2: [コア機能 — 例: CRUD API]
- Phase 3: [仕上げ — 例: CLI UX + docs]

---

## 成功条件 (テスト可能な形で)

GSD は verify 段階で各条件を自動チェックする。**必ず自動検証可能なコマンドで書くこと。**

- [ ] `cd /workspace && pytest -q` が exit code 0
- [ ] `cd /workspace && ./bin/weight-log add 70.5` が exit code 0 で SQLite に 1 行追加される
- [ ] `cd /workspace && ./bin/weight-log list` が追加したレコードを表示する
- [ ] git tag `v1.0` が存在する (GSD /gsd-autonomous の最終ステップで作られる)
- [ ] `/workspace/.planning/ROADMAP.md` に全 phase が complete マークされている

---

## 制約

- 言語・ランタイム: [例: Python 3.x / Node 20 / bash のみ]
- 外部API: [使用可否、使う場合は何か]
- 依存の追加方針: [自由に追加可 / 最小限に / 指定なし]
- 成果物は `/workspace/` 配下に置くこと
- 進捗は `/workspace/.logs/progress.log` と git commit で追跡されること

---

## 参考情報

[公式ドキュメント URL、既存実装、設計メモ等]

---

## GSD への指示 (固定・変更不要)

- このファイルを `/gsd-new-project` の事前回答として使うこと
- 続けて `/gsd-autonomous` を実行し、タグ付けまで完走させること
- 人間はいない。AskUserQuestion 禁止。迷ったら推奨デフォルトで進行せよ
```

**制約:**
- 既存の `MISSION.md.template` は触らない
- 日本語一次 (コードブロック内の英語コマンドは可)
- 40行以上、100〜130行を目安とする
- CONTEXT.md の specifics「KAIO-MISSION.md テンプレート」の3追加項目 (要件の粒度 / フェーズ分割 / 成功条件の具体性) をすべて含むこと
  </action>
  <verify>
    <automated>
test -f spirit-room/base/scripts/KAIO-MISSION.md.template && \
test $(wc -l < spirit-room/base/scripts/KAIO-MISSION.md.template) -ge 40 && \
grep -q '要件の粒度' spirit-room/base/scripts/KAIO-MISSION.md.template && \
grep -q 'フェーズ分割' spirit-room/base/scripts/KAIO-MISSION.md.template && \
grep -q '成功条件' spirit-room/base/scripts/KAIO-MISSION.md.template && \
grep -q '界王星' spirit-room/base/scripts/KAIO-MISSION.md.template && \
grep -q 'gsd-new-project\|/gsd-autonomous' spirit-room/base/scripts/KAIO-MISSION.md.template && \
diff -q spirit-room/base/scripts/MISSION.md.template /dev/null > /dev/null 2>&1 || test -f spirit-room/base/scripts/MISSION.md.template
    </automated>
  </verify>
  <done>
    - ファイルが存在する
    - 3 つの追加項目キーワードが含まれる
    - 既存 MISSION.md.template が変更されていない
    - GSD コマンド (/gsd-new-project, /gsd-autonomous) への言及がある
  </done>
</task>

</tasks>

<verification>
- テンプレートを `cat` して人間が読んで意味が通る (Mr.ポポのヒアリングがこれを元に項目を埋められる)
- `git status` で既存ファイルの変更がないこと (新規 1 ファイルのみ)
</verification>

<success_criteria>
1. `spirit-room/base/scripts/KAIO-MISSION.md.template` が存在する
2. Mr.ポポが界王星モードで使える構造になっている (Plan 04 で Mr.ポポがこのテンプレートを参照する)
3. `start-training-kaio.sh` が生成された KAIO-MISSION.md を `/gsd-new-project` の事前回答として扱える形式になっている
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-03-SUMMARY.md`
</output>
