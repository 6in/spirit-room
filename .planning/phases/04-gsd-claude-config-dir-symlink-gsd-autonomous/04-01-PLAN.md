---
phase: 04-gsd-claude-config-dir-symlink-gsd-autonomous
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spirit-room/base/entrypoint.sh
autonomous: true
requirements:
  - KAIO-ENTRYPOINT-01
  - KAIO-AUTH-SYMLINK-01

must_haves:
  truths:
    - "界王星モード用にコンテナ起動時、$CLAUDE_CONFIG_DIR が設定されていれば entrypoint はそのディレクトリを初期化する"
    - "共有認証ボリューム (spirit-room-auth) が /root/.claude-shared にマウントされ、既存の /root/.claude 直接マウント経路は併存する (後方互換)"
    - "界王星モード起動時、$CLAUDE_CONFIG_DIR/.credentials.json が /root/.claude-shared/.credentials.json を指す symlink として作成される"
    - "精神と時の部屋モード (CLAUDE_CONFIG_DIR 未設定) では entrypoint の挙動が従来と完全に等価"
  artifacts:
    - path: "spirit-room/base/entrypoint.sh"
      provides: "CLAUDE_CONFIG_DIR 分岐と credentials symlink 処理"
      contains: "CLAUDE_CONFIG_DIR"
  key_links:
    - from: "spirit-room/base/entrypoint.sh"
      to: "/root/.claude-shared/.credentials.json"
      via: "ln -sf"
      pattern: "ln -sf.*credentials\\.json"
---

<objective>
Phase 4 界王星モードの基盤として、部屋の entrypoint に `CLAUDE_CONFIG_DIR` 分岐と認証 symlink 処理を追加する。既存の精神と時の部屋モード (CLAUDE_CONFIG_DIR 未設定時) の挙動は一切変えない。

Purpose: CLAUDE_CONFIG_DIR を部屋ローカルに切り替えても、共有認証ボリューム上の `.credentials.json` を symlink 経由で参照することで、トークンリフレッシュが全部屋に伝播する状態を作る (CONTEXT D: 認証の引き継ぎ戦略)。

Output: `entrypoint.sh` に CLAUDE_CONFIG_DIR 分岐ブロック追加 + 動作確認 (精神と時の部屋モード回帰 / 界王星モード新経路)。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-CONTEXT.md
@CLAUDE.md
@spirit-room/base/entrypoint.sh
@spirit-room/spirit-room

# 既存の精神と時の部屋ループを壊さないこと。CLAUDE_CONFIG_DIR 未設定時の entrypoint 動作は従来と等価でなければならない。
</context>

<tasks>

<task type="auto">
  <name>Task 1: entrypoint.sh に CLAUDE_CONFIG_DIR 分岐と symlink 処理を追加</name>
  <files>spirit-room/base/entrypoint.sh</files>
  <action>
`spirit-room/base/entrypoint.sh` の「サービス起動」ブロックの直後、「認証チェック」ブロックの直前に、以下の分岐ロジックを追加せよ:

```bash
# ── 界王星モード: CLAUDE_CONFIG_DIR 分岐 ─────────────────────
# CLAUDE_CONFIG_DIR が設定されている = 界王星モード。
# 共有認証ボリュームは /root/.claude-shared にマウントされている前提 (spirit-room kaio 側で -v 指定)。
# その中の .credentials.json を $CLAUDE_CONFIG_DIR/.credentials.json に symlink することで、
# トークンリフレッシュが共有ボリュームに戻り、他の部屋にも伝播する。
if [ -n "${CLAUDE_CONFIG_DIR:-}" ]; then
    echo "[INFO] 界王星モード: CLAUDE_CONFIG_DIR=$CLAUDE_CONFIG_DIR"
    mkdir -p "$CLAUDE_CONFIG_DIR"

    if [ -f /root/.claude-shared/.credentials.json ]; then
        ln -sf /root/.claude-shared/.credentials.json "$CLAUDE_CONFIG_DIR/.credentials.json"
        echo "[INFO] 認証情報を symlink: $CLAUDE_CONFIG_DIR/.credentials.json → /root/.claude-shared/.credentials.json"
    else
        echo "[WARN] /root/.claude-shared/.credentials.json が見つからない。spirit-room auth を実行せよ"
    fi
fi
```

さらに、界王星モード時は既存の「認証チェック」ブロック (`claude auth status` 呼び出し) をそのまま残してよい — symlink 経由で同じ .credentials.json が見えるので普通に認証済みと判定されるはず。

tmux セッション起動部分は触らない (start-training の起動条件はこのプランのスコープ外。Plan 04 で対応)。

**制約:**
- CLAUDE_CONFIG_DIR が未設定 (既存の精神と時の部屋モード) のときに一切の副作用を発生させないこと (if 条件で完全にガード)
- `set -e` が効いているので、`mkdir -p` や `ln -sf` が失敗しても落ちない経路は作らない
- 既存の `-v spirit-room-auth:/root/.claude` マウント経路とは独立させる (界王星モードは `-v spirit-room-auth:/root/.claude-shared` 側を使う。Plan 02 で対応)
  </action>
  <verify>
    <automated>
bash -n spirit-room/base/entrypoint.sh && \
grep -q 'CLAUDE_CONFIG_DIR' spirit-room/base/entrypoint.sh && \
grep -q 'claude-shared/.credentials.json' spirit-room/base/entrypoint.sh && \
grep -q 'ln -sf' spirit-room/base/entrypoint.sh
    </automated>
  </verify>
  <done>
    - `bash -n` が syntax error なしで通る
    - CLAUDE_CONFIG_DIR ブロックが追加され、未設定時はスキップされる構造になっている
    - symlink ターゲットが `/root/.claude-shared/.credentials.json` になっている
    - 既存の精神と時の部屋モード経路 (CLAUDE_CONFIG_DIR 未設定) で bash -n が通り、tmux ブロックが変更されていない
  </done>
</task>

</tasks>

<verification>
- `bash -n spirit-room/base/entrypoint.sh` が success
- diff で見て、CLAUDE_CONFIG_DIR ブロックが唯一の追加であり、既存行は 1 行も削除・改変されていない (追加のみ)
- CONTEXT.md の specifics セクション「認証 symlink (entrypoint 内)」で示された 2 行のコマンドがそのままコード内に存在する
</verification>

<success_criteria>
1. entrypoint.sh に界王星モード分岐が存在する
2. CLAUDE_CONFIG_DIR 未設定時は従来と完全等価 (回帰なし)
3. symlink 先が共有ボリューム上の .credentials.json である (コピーではない)
4. 次のプラン (Plan 02) で `-v spirit-room-auth:/root/.claude-shared` マウントが加わった瞬間に界王星モードの認証が通る準備が整っている
</success_criteria>

<output>
After completion, create `.planning/phases/04-gsd-claude-config-dir-symlink-gsd-autonomous/04-01-SUMMARY.md`
</output>
