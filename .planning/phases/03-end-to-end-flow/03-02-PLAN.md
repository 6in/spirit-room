---
phase: 03-end-to-end-flow
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ~/projects/langgraph-poc/MISSION.md
autonomous: false
requirements: [E2E-01, E2E-02, E2E-03]
user_setup: []

must_haves:
  truths:
    - "spirit-room-manager で Claude を起動すると Mr. ポポとしてヒアリングが始まる"
    - "ヒアリング完了後、~/projects/[名前]/MISSION.md が生成される"
    - "Mr. ポポが呼び出す spirit-room open でコンテナが起動し、tmux training ペインで start-training が自動実行される"
    - "PHASE1 が pip install --break-system-packages で依存を正常インストールし .prepared を作成する"
    - "PHASE2 で Claude Code が LangGraph POC 実装を完走し .done を作成する"
  artifacts:
    - path: "~/projects/langgraph-poc/MISSION.md"
      provides: "E2E 検証用のサンプル MISSION（Mr. ポポ生成物）"
      contains: "LangGraph"
    - path: "~/projects/langgraph-poc/.prepared"
      provides: "PHASE1 完了フラグ"
    - path: "~/projects/langgraph-poc/.done"
      provides: "PHASE2 完了フラグ（E2E 完走の証拠）"
  key_links:
    - from: "Mr. ポポ（spirit-room-manager で起動した Claude）"
      to: "~/projects/langgraph-poc/MISSION.md"
      via: "MR_POPO.md のヒアリングフロー → MISSION.md.template 展開"
      pattern: "MISSION.*LangGraph"
    - from: "spirit-room open ~/projects/langgraph-poc"
      to: "コンテナ内 start-training 自動実行"
      via: "新 entrypoint.sh の MISSION.md 検知 → tmux send-keys start-training"
      pattern: "tmux send-keys.*start-training"
    - from: "PHASE2 Claude Code"
      to: "~/projects/langgraph-poc/.done"
      via: "MISSION.md の完了条件を満たした時点での touch .done"
      pattern: "touch.*\\.done"
---

<objective>
Wave 1 の修正を踏まえ、実際に Mr. ポポを起動してヒアリング→MISSION.md 生成→`spirit-room open`→自動 start-training→POC 完走までの全経路を人手介入なしで走らせ、E2E-01/02/03 の成立を実測確認する。

Purpose: Phase 3 のゴール「Mr.ポポにフレームワーク名と目的を伝えたら、Claude Code が自律的に POC を実装して動くところまで完成させる」を実地で検証する。
Output: 検証済みの ~/projects/langgraph-poc/ 配下（MISSION.md / .prepared / .done / 実装ファイル）、および VERIFICATION.md。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-end-to-end-flow/03-RESEARCH.md
@.planning/phases/03-end-to-end-flow/03-01-PLAN.md
@spirit-room-manager/CLAUDE.md
@spirit-room-manager/skills/MR_POPO.md
@spirit-room/base/scripts/MISSION.md.template
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Mr. ポポ起動 → ヒアリング → MISSION.md 生成（E2E-01）</name>
  <read_first>
    - spirit-room-manager/CLAUDE.md
    - spirit-room-manager/skills/MR_POPO.md
    - .planning/phases/03-end-to-end-flow/03-RESEARCH.md (Pattern 3, 「E2E-03 用サンプル MISSION.md」節)
  </read_first>
  <what-built>
    Wave 1 で整ったインフラを前提に、Mr. ポポ（spirit-room-manager 配下の Claude インスタンス）を起動してユーザー自身がヒアリングに応答し、`~/projects/langgraph-poc/MISSION.md` を生成する。Mr. ポポが続けて `spirit-room open ~/projects/langgraph-poc` を実行してコンテナを起動するところまでが本タスク。

    このタスクはユーザーが別ターミナルで Claude セッションを立ち上げる必要があるため human-action。
  </what-built>
  <how-to-verify>
    1. 別ターミナルで以下を実行:
       ```bash
       cd /home/parallels/workspaces/spirit-room-full/spirit-room-manager
       claude
       ```
    2. Claude が Mr. ポポとしてヒアリングを開始することを確認（「どのフレームワーク」「何を学びたいか」「制約」の3問を聞かれる）
    3. 以下の回答例を与える（RESEARCH の推奨サンプルに準拠）:
       - フレームワーク: `LangGraph`
       - 目的: `基本的な StateGraph と状態遷移を理解する。mock LLM で動作する2ノードグラフを作る`
       - 制約: `外部API使用しない / Python / 成果物は /workspace/ 配下`
    4. Mr. ポポが ~/projects/langgraph-poc/MISSION.md を生成することを確認
    5. Mr. ポポが `spirit-room open ~/projects/langgraph-poc` を実行することを確認
    6. ホストで `test -f ~/projects/langgraph-poc/MISSION.md` が成功
    7. ホストで `docker ps --filter name=spirit-room-langgraph` がコンテナを1つ表示
    8. 元のターミナルに戻って以下を確認:
       ```bash
       cat ~/projects/langgraph-poc/MISSION.md   # LangGraph 記載あり
       docker ps --filter name=spirit-room
       ```
  </how-to-verify>
  <files>~/projects/langgraph-poc/MISSION.md</files>
  <action>ユーザーが別ターミナルで spirit-room-manager 配下の Claude を起動し、Mr. ポポとしてヒアリングに応答。Mr. ポポが MR_POPO.md の手順に従い ~/projects/langgraph-poc/MISSION.md を生成し、続けて spirit-room open ~/projects/langgraph-poc を実行する。</action>
  <verify><automated>test -f ~/projects/langgraph-poc/MISSION.md && grep -q 'LangGraph' ~/projects/langgraph-poc/MISSION.md && docker ps --format '{{.Names}}' | grep -q 'spirit-room-langgraph'</automated></verify>
  <done>MISSION.md が生成され、spirit-room-langgraph-* コンテナが起動中</done>
  <acceptance_criteria>
    - `test -f ~/projects/langgraph-poc/MISSION.md` が exit 0
    - `grep -q 'LangGraph' ~/projects/langgraph-poc/MISSION.md` が exit 0
    - `grep -q '完了条件' ~/projects/langgraph-poc/MISSION.md` が exit 0
    - `docker ps --format '{{.Names}}' | grep -q 'spirit-room-langgraph'` が exit 0
  </acceptance_criteria>
  <resume-signal>"approved" または検出した問題を記述</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 自動 start-training 起動 + PHASE1 完走確認（E2E-02）</name>
  <read_first>
    - spirit-room/base/entrypoint.sh (Wave 1 で修正済み)
    - spirit-room/base/scripts/start-training.sh
    - ~/projects/langgraph-poc/MISSION.md (Task 1 生成物)
  </read_first>
  <what-built>
    Task 1 でコンテナが起動すると、新しい entrypoint.sh の条件分岐により tmux training ペインで `start-training` が自動実行される。PHASE1 で catalog.md の `--break-system-packages` 指示に従って pip install が成功し、`.prepared` フラグが作成されることを確認する。
  </what-built>
  <how-to-verify>
    1. 数分待ってから以下で状態確認:
       ```bash
       spirit-room logs ~/projects/langgraph-poc | tail -50
       ```
    2. ログに PHASE1 開始メッセージが含まれることを確認
    3. `.prepared` フラグの作成を待つ（最大 5〜10 分。PHASE1 は pip install 次第）:
       ```bash
       test -f ~/projects/langgraph-poc/.prepared && echo "PHASE1 完了"
       ```
    4. tmux training ペインに自動で `start-training` が入力されていることを確認するため、必要に応じて:
       ```bash
       spirit-room enter ~/projects/langgraph-poc
       # tmux attach 後、training ウィンドウで PHASE の進行を視認
       # Ctrl+b d でデタッチ
       ```
    5. ログ中に `externally-managed-environment` エラーが含まれないことを確認:
       ```bash
       grep -i 'externally-managed-environment' ~/projects/langgraph-poc/.logs/progress.log || echo "OK: エラーなし"
       ```
  </how-to-verify>
  <files>~/projects/langgraph-poc/.prepared, ~/projects/langgraph-poc/.logs/progress.log</files>
  <action>Wave 1 の新 entrypoint.sh により MISSION.md 検知時に start-training が自動実行されるはず。ユーザーは spirit-room logs で PHASE1 進行を監視し、pip install --break-system-packages が成功して .prepared フラグが作成されるのを待つ。externally-managed-environment エラーが出ないことも検証する。</action>
  <verify><automated>test -f ~/projects/langgraph-poc/.prepared && ! grep -qi 'externally-managed-environment' ~/projects/langgraph-poc/.logs/progress.log</automated></verify>
  <done>.prepared が作成され、progress.log に pip エラーなし、ユーザー手動介入なしで PHASE1 が完走</done>
  <acceptance_criteria>
    - `test -f ~/projects/langgraph-poc/.prepared` が exit 0
    - `~/projects/langgraph-poc/.logs/progress.log` に文字列 `PHASE1` または `PHASE 1` が含まれる
    - `~/projects/langgraph-poc/.logs/progress.log` に文字列 `externally-managed-environment` が **含まれない**（エラーなし）
    - ユーザーが手動で `start-training` を叩いていない（自動起動の確認）
  </acceptance_criteria>
  <resume-signal>"approved" または検出した問題を記述</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PHASE2 POC 実装完走 + .done 確認（E2E-03）</name>
  <read_first>
    - ~/projects/langgraph-poc/MISSION.md
    - ~/projects/langgraph-poc/.logs/progress.log (進行状況)
  </read_first>
  <what-built>
    PHASE2 で Claude Code が MISSION.md の完了条件（`python3 test_graph.py` が exit 0 / README.md が存在）を満たすまで LangGraph POC を自律実装し、`.done` フラグを作成する。Phase 3 の最終ゴール地点。
  </what-built>
  <how-to-verify>
    1. PHASE2 の進行を定期確認（最大 20〜30 分目安）:
       ```bash
       spirit-room logs ~/projects/langgraph-poc | tail -80
       ```
    2. `.done` フラグの作成を待つ:
       ```bash
       test -f ~/projects/langgraph-poc/.done && echo "E2E 完走"
       ```
    3. 成果物の実体を確認:
       ```bash
       ls -la ~/projects/langgraph-poc/
       cat ~/projects/langgraph-poc/README.md
       ```
    4. MISSION.md の完了条件（test_graph.py 等）がコンテナ内で exit 0 で走ることを検証:
       ```bash
       docker exec spirit-room-langgraph-poc bash -c 'cd /workspace && python3 test_graph.py' && echo "OK"
       ```
       （ファイル名は Claude の実装次第なので実測確認）
    5. 最後にコンテナを停止:
       ```bash
       spirit-room close ~/projects/langgraph-poc
       ```
  </how-to-verify>
  <files>~/projects/langgraph-poc/.done, ~/projects/langgraph-poc/README.md</files>
  <action>PHASE2 で Claude Code が LangGraph POC を自律実装するのを最大 20〜30 分待機。.done フラグ作成後、MISSION.md の完了条件スクリプトを docker exec で手動再実行し exit 0 を確認。最後に spirit-room close でコンテナ停止。</action>
  <verify><automated>test -f ~/projects/langgraph-poc/.done && test -f ~/projects/langgraph-poc/README.md && ls ~/projects/langgraph-poc/*.py 2>/dev/null | head -1 | grep -q '\.py$'</automated></verify>
  <done>.done と README.md と Python 実装ファイルが存在、完了条件スクリプトが exit 0</done>
  <acceptance_criteria>
    - `test -f ~/projects/langgraph-poc/.done` が exit 0
    - `test -f ~/projects/langgraph-poc/README.md` が exit 0
    - `~/projects/langgraph-poc/` 配下に Python ソースファイル（*.py）が1つ以上存在
    - `~/projects/langgraph-poc/.logs/progress.log` に文字列 `PHASE2` または `PHASE 2` が含まれる
    - 最終的な完了条件スクリプトがコンテナ内で exit 0（手動実行で確認）
  </acceptance_criteria>
  <resume-signal>"approved" または検出した問題を記述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ユーザー自由入力→Mr. ポポ | ヒアリング回答がそのまま MISSION.md に展開される |
| MISSION.md→Claude Code プロンプト | 攻撃者が MISSION.md を改変すれば Claude に任意指示を通せる |
| コンテナ→ホスト | /workspace マウント経由でホスト ~/projects 下にファイル書き込み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | MISSION.md に認証情報が紛れる | accept | ローカル検証のみ、ヒアリング回答は本人入力で漏洩面なし |
| T-03-07 | Tampering | /workspace マウントでの任意ファイル書込 | mitigate | ~/projects/langgraph-poc 配下に限定された書き込み。既存認証ファイル配下は別マウント |
| T-03-08 | Denial of Service | PHASE2 無限ループ（MISSION.md 完了条件満たせず） | mitigate | 検証時タイムアウト（20〜30分）を設定し、超過時は手動 close で停止 |
| T-03-09 | Repudiation | 検証結果の真偽不明 | mitigate | `.done` フラグ + progress.log + 完了条件スクリプト手動再実行で三重に確認 |

すべて HIGH 以下。BLOCKING 脅威なし。
</threat_model>

<verification>
1. MISSION.md が ~/projects/langgraph-poc/ に存在し LangGraph が記載されている
2. `spirit-room open` 後、ユーザーが手動で start-training を叩かずに PHASE1 が進行する
3. progress.log に `externally-managed-environment` エラーが含まれない
4. `.prepared` および `.done` の両方が作成される
5. MISSION.md の完了条件スクリプトがコンテナ内で exit 0 で走る
</verification>

<success_criteria>
- E2E-01: spirit-room-manager で Mr. ポポ起動 → ヒアリング → MISSION.md 生成まで完了
- E2E-02: 生成された MISSION.md で `spirit-room open` → 修行ループが**自動起動**（手動 start-training なし）
- E2E-03: Claude Code が LangGraph POC 実装を完走し `.done` を作成、完了条件が exit 0
- Phase 3 全要件充足 → ROADMAP Phase 3 をクローズ可能
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-flow/03-02-SUMMARY.md`
</output>
